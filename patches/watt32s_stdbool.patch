diff -Naur old/inc/sys/uio.h new/inc/sys/uio.h
--- old/inc/sys/uio.h	2003-01-23 15:29:10.000000000 +0100
+++ new/inc/sys/uio.h	1970-01-01 01:00:00.000000000 +0100
@@ -1,11 +0,0 @@
-/*!\file sys/uio.h
- * Dummy header.
- */
-
-#ifndef __SYS_UIO_H
-#define __SYS_UIO_H
-
-/* This dummy header is required while building e.g. tcpdump
- */
-
-#endif
diff -Naur old/inc/sys/wtypes.h new/inc/sys/wtypes.h
--- old/inc/sys/wtypes.h	2026-04-09 07:13:58.552819378 +0200
+++ new/inc/sys/wtypes.h	2026-04-09 07:18:47.489872214 +0200
@@ -273,11 +273,11 @@
   typedef unsigned long   DWORD;   /**<\typedef 32 bits   */
   typedef unsigned int    UINT;    /**<\typedef 16/32 bit */
 
-  /* Hack for building Lynx; it defines BOOL to BOOLEAN (char)
+  /* Hack for building Lynx; it defines bool to BOOLEAN (char)
    * in it's HTUtils.h.
    */
-  #if !defined(BOOLEAN_DEFINED) && !defined(BOOL)
-    typedef int           BOOL;    /**<\typedef Boolean */
+  #if !defined(BOOLEAN_DEFINED) && !defined(bool)
+    #include <stdbool.h>    /**<\typedef Boolean */
   #endif
 #endif
 
diff -Naur old/inc/tcp.h new/inc/tcp.h
--- old/inc/tcp.h	2018-05-06 17:11:04.000000000 +0200
+++ new/inc/tcp.h	2026-04-09 07:18:47.485872146 +0200
@@ -216,7 +216,7 @@
                                            const char       *secret,
                                            BYTE             *buf);
 
-W32_FUNC BOOL W32_CALL check_md5_signature (const tcp_Socket *s,
+W32_FUNC bool W32_CALL check_md5_signature (const tcp_Socket *s,
                                             const in_Header *ip);
 
 /*
@@ -260,10 +260,10 @@
 W32_DATA WORD      last_cookie;
 W32_DATA DWORD     cookies [MAX_COOKIES];
 
-W32_DATA BOOL      survive_eth;
-W32_DATA BOOL      survive_bootp;
-W32_DATA BOOL      survive_dhcp;
-W32_DATA BOOL      survive_rarp;
+W32_DATA bool      survive_eth;
+W32_DATA bool      survive_bootp;
+W32_DATA bool      survive_dhcp;
+W32_DATA bool      survive_rarp;
 
 W32_DATA unsigned  tcp_OPEN_TO;
 W32_DATA unsigned  tcp_CLOSE_TO;
@@ -318,7 +318,7 @@
  * name domain constants, etc.
  */
 W32_DATA WORD          dns_timeout;
-W32_DATA BOOL          dns_recurse;
+W32_DATA bool          dns_recurse;
 W32_DATA WORD         _watt_handle_cbreak;      /* ^C/^Break handle mode */
 W32_DATA volatile int _watt_cbroke;             /* ^C/^Break occured */
 W32_DATA unsigned     _mtu, _mss;
@@ -342,8 +342,8 @@
  * DNS stuff
  */
 W32_DATA int (W32_CALL *_resolve_hook)(void);
-W32_DATA BOOL           _resolve_exit;
-W32_DATA BOOL           _resolve_timeout;
+W32_DATA bool           _resolve_exit;
+W32_DATA bool           _resolve_timeout;
 
 W32_DATA char            defaultdomain [MAX_HOSTLEN+1];
 W32_DATA char           *def_domain;
@@ -498,14 +498,14 @@
      DHCP_OP_ERASE = 2
    };
 
-W32_DATA BOOL DHCP_did_gratuitous_arp;
+W32_DATA bool DHCP_did_gratuitous_arp;
 
 typedef int (W32_CALL * WattDHCPConfigFunc) (enum DHCP_config_op op,
                                              struct DHCP_config *cfg);
 
 W32_FUNC WattDHCPConfigFunc W32_CALL DHCP_set_config_func (WattDHCPConfigFunc fn);
 
-W32_FUNC void  W32_CALL DHCP_release (BOOL force);
+W32_FUNC void  W32_CALL DHCP_release (bool force);
 W32_FUNC int   W32_CALL DHCP_read_config (void);
 W32_FUNC DWORD W32_CALL DHCP_get_server (void);
 
@@ -538,7 +538,7 @@
 W32_FUNC void  W32_CALL _eth_release      (void);
 W32_FUNC void *W32_CALL _eth_formatpacket (const void *eth_dest, WORD eth_type);
 W32_FUNC void  W32_CALL _eth_free         (const void *buf);
-W32_FUNC void *W32_CALL _eth_arrived      (WORD *type, BOOL *brdcast);
+W32_FUNC void *W32_CALL _eth_arrived      (WORD *type, bool *brdcast);
 W32_FUNC int   W32_CALL _eth_send         (WORD len, const void *sock, const char *file, unsigned line);
 W32_FUNC int   W32_CALL _eth_set_addr     (const void *addr);
 W32_FUNC BYTE  W32_CALL _eth_get_hwtype   (BYTE *hwtype, BYTE *hwlen);
@@ -555,7 +555,7 @@
 /*
  * Some applications (tracert.c) may need pcarp.c functions.
  */
-W32_FUNC BOOL W32_CALL _arp_resolve (DWORD ina, eth_address *eth);
+W32_FUNC bool W32_CALL _arp_resolve (DWORD ina, eth_address *eth);
 
 /*
  * Stuff for Windows only
@@ -626,7 +626,7 @@
 W32_FUNC DWORD W32_CALL aton           (const char *name);
 W32_FUNC int   W32_CALL isaddr         (const char *name);
 W32_FUNC DWORD W32_CALL aton_dotless   (const char *str);
-W32_FUNC BOOL  W32_CALL isaddr_dotless (const char *str, DWORD *ip);
+W32_FUNC bool  W32_CALL isaddr_dotless (const char *str, DWORD *ip);
 
 W32_FUNC int   W32_CALL addwattcpd  (VoidProc p);
 W32_FUNC int   W32_CALL delwattcpd  (VoidProc p);
@@ -861,8 +861,8 @@
 W32_FUNC int          W32_CALL pkt_get_api_ver   (WORD *ver);
 W32_FUNC int          W32_CALL pkt_set_rcv_mode  (int mode);
 W32_FUNC int          W32_CALL pkt_get_rcv_mode  (void);
-W32_FUNC BOOL         W32_CALL pkt_check_address (DWORD my_ip);
-W32_FUNC BOOL         W32_CALL pkt_is_init       (void);
+W32_FUNC bool         W32_CALL pkt_check_address (DWORD my_ip);
+W32_FUNC bool         W32_CALL pkt_is_init       (void);
 W32_FUNC int          W32_CALL pkt_get_params    (struct PktParameters *params);
 W32_FUNC int          W32_CALL pkt_get_stats     (struct PktStats *stats, struct PktStats *total);
 W32_FUNC DWORD        W32_CALL pkt_dropped       (void);
diff -Naur old/src/accept.c new/src/accept.c
--- old/src/accept.c	2015-12-29 23:56:22.000000000 +0100
+++ new/src/accept.c	2026-04-09 07:18:47.341869671 +0200
@@ -63,7 +63,7 @@
   volatile int     newsock = -1;
   volatile int     que_idx;
   volatile int     maxconn;
-  volatile BOOL    is_ip6;
+  volatile bool    is_ip6;
 
   socket = _socklist_find (s);
 
@@ -334,7 +334,7 @@
  */
 static int alloc_addr (Socket *socket, Socket *clone)
 {
-  BOOL is_ip6 = (socket->so_family == AF_INET6);
+  bool is_ip6 = (socket->so_family == AF_INET6);
   int  sa_len = is_ip6 ? sizeof(struct sockaddr_in6) :
                          sizeof(struct sockaddr_in);
 
diff -Naur old/src/bind.c new/src/bind.c
--- old/src/bind.c	2015-12-29 23:56:53.000000000 +0100
+++ new/src/bind.c	2026-04-09 07:18:47.345869740 +0200
@@ -65,9 +65,9 @@
   Socket              *socket = _socklist_find (s);
   struct sockaddr_in  *addr   = (struct sockaddr_in*) myaddr;
   struct sockaddr_in6 *addr6  = (struct sockaddr_in6*) myaddr;
-  BOOL   bind_any, is_bcast, is_mcast, our_addr;
+  bool   bind_any, is_bcast, is_mcast, our_addr;
   WORD   local_port;
-  BOOL   is_ip6;
+  bool   is_ip6;
   int    sa_len;
 
   SOCK_PROLOGUE (socket, "\nbind:%d", s);
@@ -121,7 +121,7 @@
     struct in6_addr *ip6 = &((struct sockaddr_in6*) socket->local_addr)->sin6_addr;
     u_short port  = socket->local_addr->sin_port;
     u_long  ip4   = socket->local_addr->sin_addr.s_addr;
-    BOOL    equal = (!is_ip6 && ip4 == addr->sin_addr.s_addr) ||
+    bool    equal = (!is_ip6 && ip4 == addr->sin_addr.s_addr) ||
                    (is_ip6 && IN6_ARE_ADDR_EQUAL(ip6,&addr6->sin6_addr));
 
     if ((addr->sin_port != IPPORT_ANY && addr->sin_port == port) ||
diff -Naur old/src/bsddbug.c new/src/bsddbug.c
--- old/src/bsddbug.c	2015-09-30 16:04:22.000000000 +0200
+++ new/src/bsddbug.c	2026-04-09 07:18:47.345869740 +0200
@@ -49,7 +49,7 @@
 
 static void (W32_CALL *prev_hook) (const char*, const char*);
 
-static BOOL   dbg_use_ods  = FALSE;
+static bool   dbg_use_ods  = FALSE;
 static FILE  *dbg_file     = NULL;
 static char   dbg_omode[5] = "w+";           /* max "w+tc" */
 static char   dbg_fname[MAX_PATHLEN+1] = "";
@@ -127,7 +127,7 @@
   return (len);
 }
 
-BOOL _sock_dbug_active (void)
+bool _sock_dbug_active (void)
 {
   return (dbg_file != NULL || dbg_use_ods);
 }
diff -Naur old/src/bsddbug.h new/src/bsddbug.h
--- old/src/bsddbug.h	2015-10-05 15:51:18.000000000 +0200
+++ new/src/bsddbug.h	2026-04-09 07:18:47.345869740 +0200
@@ -17,7 +17,7 @@
   extern void  _sock_dbug_flush  (void);
   extern void  _sock_dbug_init   (void);
   extern void  _sock_dbug_open   (void);
-  extern BOOL  _sock_dbug_active (void);
+  extern bool  _sock_dbug_active (void);
   extern void  _sock_enter_scope (void);
   extern void  _sock_leave_scope (void);
 
diff -Naur old/src/bugtrap.h new/src/bugtrap.h
--- old/src/bugtrap.h	2015-02-04 09:49:42.000000000 +0100
+++ new/src/bugtrap.h	2026-04-09 07:18:47.345869740 +0200
@@ -394,7 +394,7 @@
  * @brief Read application name and version number from version info block.
  * @note @a hModule can be set to NULL for the main executable.
  */
-BUGTRAP_API BOOL APIENTRY BT_ReadVersionInfo(HMODULE hModule);
+BUGTRAP_API bool APIENTRY BT_ReadVersionInfo(HMODULE hModule);
 
 /** @} */
 
@@ -606,7 +606,7 @@
 /**
  * @brief This function enumerates log file entries that will be attached to the report.
  */
-BUGTRAP_API DWORD APIENTRY BT_GetLogFileEntry(INT_PTR nLogFileIndexOrName, BOOL bGetByIndex, BUGTRAP_LOGTYPE* peLogType, PDWORD pdwLogEntrySize, PVOID pLogEntry);
+BUGTRAP_API DWORD APIENTRY BT_GetLogFileEntry(INT_PTR nLogFileIndexOrName, bool bGetByIndex, BUGTRAP_LOGTYPE* peLogType, PDWORD pdwLogEntrySize, PVOID pLogEntry);
 
 /** @} */
 
@@ -646,12 +646,12 @@
 /**
  * @brief Close custom log file. This function is thread safe.
  */
-BUGTRAP_API BOOL APIENTRY BT_CloseLogFile(INT_PTR iHandle);
+BUGTRAP_API bool APIENTRY BT_CloseLogFile(INT_PTR iHandle);
 /**
  * @brief Flush contents of the log file.
  * @note This function is optional and not required in normal conditions.
  */
-BUGTRAP_API BOOL APIENTRY BT_FlushLogFile(INT_PTR iHandle);
+BUGTRAP_API bool APIENTRY BT_FlushLogFile(INT_PTR iHandle);
 /**
  * @brief Get custom log file name. This function is thread safe.
  */
@@ -663,7 +663,7 @@
 /**
  * @brief Set maximum log file size in records. This function is thread safe.
  */
-BUGTRAP_API BOOL APIENTRY BT_SetLogSizeInEntries(INT_PTR iHandle, DWORD dwLogSizeInEntries);
+BUGTRAP_API bool APIENTRY BT_SetLogSizeInEntries(INT_PTR iHandle, DWORD dwLogSizeInEntries);
 /**
  * @brief Get maximum log file size in bytes. This function is thread safe.
  */
@@ -671,7 +671,7 @@
 /**
  * @brief Set maximum log file size in bytes. This function is thread safe.
  */
-BUGTRAP_API BOOL APIENTRY BT_SetLogSizeInBytes(INT_PTR iHandle, DWORD dwLogSizeInEntries);
+BUGTRAP_API bool APIENTRY BT_SetLogSizeInBytes(INT_PTR iHandle, DWORD dwLogSizeInEntries);
 /**
  * @brief Return true if time stamp is added to every log entry.
  */
@@ -679,7 +679,7 @@
 /**
  * @brief Set true if time stamp is added to every log entry.
  */
-BUGTRAP_API BOOL APIENTRY BT_SetLogFlags(INT_PTR iHandle, DWORD dwLogFlags);
+BUGTRAP_API bool APIENTRY BT_SetLogFlags(INT_PTR iHandle, DWORD dwLogFlags);
 /**
  * @brief Return minimal log level accepted by tracing functions.
  */
@@ -687,7 +687,7 @@
 /**
  * @brief Set minimal log level accepted by tracing functions.
  */
-BUGTRAP_API BOOL APIENTRY BT_SetLogLevel(INT_PTR iHandle, BUGTRAP_LOGLEVEL eLogLevel);
+BUGTRAP_API bool APIENTRY BT_SetLogLevel(INT_PTR iHandle, BUGTRAP_LOGLEVEL eLogLevel);
 /**
  * @brief Get echo mode.
  */
@@ -695,35 +695,35 @@
 /**
  * @brief Set echo mode.
  */
-BUGTRAP_API BOOL APIENTRY BT_SetLogEchoMode(INT_PTR iHandle, DWORD dwLogEchoMode);
+BUGTRAP_API bool APIENTRY BT_SetLogEchoMode(INT_PTR iHandle, DWORD dwLogEchoMode);
 /**
  * @brief Clear log file. This function is thread safe.
  */
-BUGTRAP_API BOOL APIENTRY BT_ClearLog(INT_PTR iHandle);
+BUGTRAP_API bool APIENTRY BT_ClearLog(INT_PTR iHandle);
 /**
  * @brief Insert entry into the beginning of custom log file. This function is thread safe.
  */
-BUGTRAP_API BOOL CDECL BT_InsLogEntryF(INT_PTR iHandle, BUGTRAP_LOGLEVEL eLogLevel, LPCTSTR pszFormat, ...);
+BUGTRAP_API bool CDECL BT_InsLogEntryF(INT_PTR iHandle, BUGTRAP_LOGLEVEL eLogLevel, LPCTSTR pszFormat, ...);
 /**
  * @brief Append entry to the end of custom log file. This function is thread safe.
  */
-BUGTRAP_API BOOL CDECL BT_AppLogEntryF(INT_PTR iHandle, BUGTRAP_LOGLEVEL eLogLevel, LPCTSTR pszFormat, ...);
+BUGTRAP_API bool CDECL BT_AppLogEntryF(INT_PTR iHandle, BUGTRAP_LOGLEVEL eLogLevel, LPCTSTR pszFormat, ...);
 /**
  * @brief Insert entry into the beginning of custom log file. This function is thread safe.
  */
-BUGTRAP_API BOOL APIENTRY BT_InsLogEntryV(INT_PTR iHandle, BUGTRAP_LOGLEVEL eLogLevel, LPCTSTR pszFormat, va_list argList);
+BUGTRAP_API bool APIENTRY BT_InsLogEntryV(INT_PTR iHandle, BUGTRAP_LOGLEVEL eLogLevel, LPCTSTR pszFormat, va_list argList);
 /**
  * @brief Append entry to the end of custom log file. This function is thread safe.
  */
-BUGTRAP_API BOOL APIENTRY BT_AppLogEntryV(INT_PTR iHandle, BUGTRAP_LOGLEVEL eLogLevel, LPCTSTR pszFormat, va_list argList);
+BUGTRAP_API bool APIENTRY BT_AppLogEntryV(INT_PTR iHandle, BUGTRAP_LOGLEVEL eLogLevel, LPCTSTR pszFormat, va_list argList);
 /**
  * @brief Insert entry into the beginning of custom log file. This function is thread safe.
  */
-BUGTRAP_API BOOL APIENTRY BT_InsLogEntry(INT_PTR iHandle, BUGTRAP_LOGLEVEL eLogLevel, LPCTSTR pszEntry);
+BUGTRAP_API bool APIENTRY BT_InsLogEntry(INT_PTR iHandle, BUGTRAP_LOGLEVEL eLogLevel, LPCTSTR pszEntry);
 /**
  * @brief Append entry to the end of custom log file. This function is thread safe.
  */
-BUGTRAP_API BOOL APIENTRY BT_AppLogEntry(INT_PTR iHandle, BUGTRAP_LOGLEVEL eLogLevel, LPCTSTR pszEntry);
+BUGTRAP_API bool APIENTRY BT_AppLogEntry(INT_PTR iHandle, BUGTRAP_LOGLEVEL eLogLevel, LPCTSTR pszEntry);
 
 /** @} */
 
@@ -746,31 +746,31 @@
  * @brief Explicitly intercepts SetUnhandledExceptionFilter() in a module.
  * This function could be useful for dynamically loaded modules.
  */
-BUGTRAP_API void BT_InterceptSUEF(HMODULE hModule, BOOL bOverride);
+BUGTRAP_API void BT_InterceptSUEF(HMODULE hModule, bool bOverride);
 /**
  * @brief Take a snapshot of program memory and save it to a file.
  */
-BUGTRAP_API BOOL APIENTRY BT_SaveSnapshot(LPCTSTR pszFileName);
+BUGTRAP_API bool APIENTRY BT_SaveSnapshot(LPCTSTR pszFileName);
 /**
  * @brief Take a snapshot of program memory and save it to a file.
  */
-BUGTRAP_API BOOL APIENTRY BT_SaveSnapshotEx(PEXCEPTION_POINTERS pExceptionPointers, LPCTSTR pszFileName);
+BUGTRAP_API bool APIENTRY BT_SaveSnapshotEx(PEXCEPTION_POINTERS pExceptionPointers, LPCTSTR pszFileName);
 /**
  * @brief Take a snapshot of program memory and send over network.
  */
-BUGTRAP_API BOOL APIENTRY BT_SendSnapshot(void);
+BUGTRAP_API bool APIENTRY BT_SendSnapshot(void);
 /**
  * @brief Take a snapshot of program memory and send over network.
  */
-BUGTRAP_API BOOL APIENTRY BT_SendSnapshotEx(PEXCEPTION_POINTERS pExceptionPointers);
+BUGTRAP_API bool APIENTRY BT_SendSnapshotEx(PEXCEPTION_POINTERS pExceptionPointers);
 /**
  * @brief Take a snapshot of program memory and e-mail it.
  */
-BUGTRAP_API BOOL APIENTRY BT_MailSnapshot(void);
+BUGTRAP_API bool APIENTRY BT_MailSnapshot(void);
 /**
  * @brief Take a snapshot of program memory and e-mail it.
  */
-BUGTRAP_API BOOL APIENTRY BT_MailSnapshotEx(PEXCEPTION_POINTERS pExceptionPointers);
+BUGTRAP_API bool APIENTRY BT_MailSnapshotEx(PEXCEPTION_POINTERS pExceptionPointers);
 /**
  * @brief Executes structured exception filter.
  * @note Don't call this function directly in your code.
diff -Naur old/src/close.c new/src/close.c
--- old/src/close.c	2014-01-03 12:20:30.000000000 +0100
+++ new/src/close.c	2026-04-09 07:18:47.345869740 +0200
@@ -95,8 +95,8 @@
 static int close_stream (Socket *socket)
 {
   sock_type *sk       = (sock_type*)socket->tcp_sock;
-  BOOL abort_it       = FALSE;
-  BOOL blocking_close = FALSE;
+  bool abort_it       = FALSE;
+  bool blocking_close = FALSE;
   int  i, s;
 
   if ((socket->so_state & SS_ISDISCONNECTING) && socket->close_time)
diff -Naur old/src/connect.c new/src/connect.c
--- old/src/connect.c	2015-12-30 00:03:15.000000000 +0100
+++ new/src/connect.c	2026-04-09 07:18:47.353869877 +0200
@@ -57,7 +57,7 @@
   struct   sockaddr_in6 *addr6  = (struct sockaddr_in6*) servaddr;
   struct   Socket       *socket = _socklist_find (s);
   volatile int rc, sa_len;
-  BOOL     is_ip6;
+  bool     is_ip6;
 
   SOCK_PROLOGUE (socket, "\nconnect:%d", s);
 
diff -Naur old/src/cpumodel.h new/src/cpumodel.h
--- old/src/cpumodel.h	2016-12-12 12:30:26.000000000 +0100
+++ new/src/cpumodel.h	2026-04-09 07:18:47.353869877 +0200
@@ -108,8 +108,8 @@
   extern WORD  SYSCALL cdecl MY_SS   (void);
   extern DWORD SYSCALL cdecl Get_CR4 (void);
 
-  extern BOOL  SYSCALL cdecl SelWriteable (WORD sel);
-  extern BOOL  SYSCALL cdecl SelReadable (WORD sel);
+  extern bool  SYSCALL cdecl SelWriteable (WORD sel);
+  extern bool  SYSCALL cdecl SelReadable (WORD sel);
 #endif
 
 #if defined(__WATCOMC__) && 0 /* no more need for this */
@@ -334,7 +334,7 @@
   /*
    * All 64-bit CPU's have CPUID
    */
-  W32_INLINE BOOL have_cpuid (void)
+  W32_INLINE bool have_cpuid (void)
   {
     return (TRUE);
   }
@@ -345,7 +345,7 @@
  * Not used at the moment. See cpumodel.asm instead.
  */
 #if defined(__GNUC__) && defined(__i386__) && !defined(__NO_INLINE__)
-  W32_GCC_INLINE BOOL have_cpuid (void)
+  W32_GCC_INLINE bool have_cpuid (void)
   {
     int result = -1;
 
@@ -375,7 +375,7 @@
     #pragma warn(disable:2135)
   #endif
 
-  __declspec(naked) W32_INLINE BOOL have_cpuid (void)
+  __declspec(naked) W32_INLINE bool have_cpuid (void)
   {
     _asm {
       pushfd
diff -Naur old/src/crc.c new/src/crc.c
--- old/src/crc.c	2014-02-24 19:26:00.000000000 +0100
+++ new/src/crc.c	2026-04-09 07:18:47.353869877 +0200
@@ -75,7 +75,7 @@
   return (accum);
 }
 
-BOOL crc_init (void)
+bool crc_init (void)
 {
   if (crc_table)
      return (TRUE);
diff -Naur old/src/dynip.c new/src/dynip.c
--- old/src/dynip.c	2015-06-29 17:48:30.000000000 +0200
+++ new/src/dynip.c	2026-04-09 07:18:47.353869877 +0200
@@ -53,14 +53,14 @@
        char *host;
        char *get_req;
        WORD  port;
-       BOOL  on_heap;
+       bool  on_heap;
      };
 
 static char dyndns_user    [MAX_VALUELEN+1] = "";
 static char dyndns_passwd  [MAX_VALUELEN+1] = "";
 static char dyn_myhostname [MAX_VALUELEN+1] = "";
 static char dyn_myip [20]  = "";
-static BOOL dynip_enable   = FALSE;
+static bool dynip_enable   = FALSE;
 static int  dyndns_refresh = 3600;
 static int  trace_level    = 0;
 static char config_file [MAX_PATHLEN+1] = "$(TEMP)\\W32DYNIP.TMP";
@@ -82,7 +82,7 @@
 static int         chkip_method (const char *value);
 static int         dyndns_params (const char *value);
 static void        url_free (struct URL *url);
-static BOOL        url_parse (struct URL *res, const char *url);
+static bool        url_parse (struct URL *res, const char *url);
 
 
 /**
@@ -296,7 +296,7 @@
  * Simple URL parser; accepts only "http://" prefixes (optional).
  * Extracts host, port and request parts ("/" if not present).
  */
-static BOOL url_parse (struct URL *res, const char *url)
+static bool url_parse (struct URL *res, const char *url)
 {
   char  scheme [20+1];
   char  buf [256+1];
@@ -474,7 +474,7 @@
  * Return FALSE if header isn't correct.
  * Return TRUE and extract "Content-Length" field.
  */
-static BOOL get_header (void *sock, long *cont_len_ptr)
+static bool get_header (void *sock, long *cont_len_ptr)
 {
   int    len, response;
   long   content_length;
diff -Naur old/src/echo.c new/src/echo.c
--- old/src/echo.c	2015-06-22 21:34:22.000000000 +0200
+++ new/src/echo.c	2026-04-09 07:18:47.353869877 +0200
@@ -51,11 +51,11 @@
 static _udp_Socket *udp_echo_sock, *udp_disc_sock;
 static _tcp_Socket *tcp_echo_sock, *tcp_disc_sock;
 
-static BOOL  do_echo   = 0;
+static bool  do_echo   = 0;
 static WORD  echo_port = 7;
 static DWORD echo_host = 0;
 
-static BOOL  do_disc   = 0;
+static bool  do_disc   = 0;
 static WORD  disc_port = 9;
 static DWORD disc_host = 0;
 
diff -Naur old/src/enum_cp.c new/src/enum_cp.c
--- old/src/enum_cp.c	2016-02-08 09:25:46.000000000 +0100
+++ new/src/enum_cp.c	2026-04-09 07:18:47.357869946 +0200
@@ -1,7 +1,7 @@
 #include <stdio.h>
 #include <windows.h>
 
-static BOOL CALLBACK callback (LPSTR cp)
+static bool CALLBACK callback (LPSTR cp)
 {
   printf ("\t callback(): cp: \"%s\", valid: %d\n",
           cp, IsValidCodePage(atoi(cp)));
diff -Naur old/src/fortify.c new/src/fortify.c
--- old/src/fortify.c	2015-06-28 10:50:26.000000000 +0200
+++ new/src/fortify.c	2026-04-09 07:18:47.357869946 +0200
@@ -188,7 +188,7 @@
 #define ST_OUTPUT(s) do { if (st_Output) (*st_Output)(s); } while (0)
 #endif
 
-static void message (BOOL warn, const char *fmt, ...)
+static void message (bool warn, const char *fmt, ...)
 {
   va_list args;
   char   *pos = st_Buffer;
@@ -324,7 +324,7 @@
 
   #pragma data;
 
-  static void heap_integrity_check (BOOL on)
+  static void heap_integrity_check (bool on)
   {
     if (on)
     {
@@ -1925,7 +1925,7 @@
   return (ptr ? GlobalLock(ptr) : NULL);
 }
 
-BOOL Fortify_GlobalFree (void *ptr, const char *file, DWORD line)
+bool Fortify_GlobalFree (void *ptr, const char *file, DWORD line)
 {
   HGLOBAL hnd;
 
diff -Naur old/src/fortify.h new/src/fortify.h
--- old/src/fortify.h	2017-10-04 14:19:57.000000000 +0200
+++ new/src/fortify.h	2026-04-09 07:18:47.357869946 +0200
@@ -266,7 +266,7 @@
       #include <windowsx.h>
 
       void *Fortify_GlobalAlloc (UINT flags, DWORD size, const char *file, DWORD line);
-      BOOL  Fortify_GlobalFree (void *ptr, const char *file, DWORD line);
+      bool  Fortify_GlobalFree (void *ptr, const char *file, DWORD line);
 
       #undef  GlobalAllocPtr
       #define GlobalAllocPtr(flags, size) \
diff -Naur old/src/get_ai.c new/src/get_ai.c
--- old/src/get_ai.c	2015-06-30 10:35:46.000000000 +0200
+++ new/src/get_ai.c	2026-04-09 07:18:47.357869946 +0200
@@ -90,7 +90,7 @@
      { -1,       0,           0,           NULL,  0 }
    };
 
-BOOL called_from_getai = FALSE;
+bool called_from_getai = FALSE;
 
 static struct afd afd_list [3];
 
@@ -265,7 +265,7 @@
  * Initialise afd_list[].
  * Give IPv6 protocol highest priority if 'ip6_prio' is TRUE.
  */
-static void afd_list_init (BOOL ip6_prio)
+static void afd_list_init (bool ip6_prio)
 {
   static const char in4addr_loopback[] = { 127, 0, 0, 1 };
   static const char in4addr_addrany[]  = { 0, 0, 0, 0 };
@@ -308,7 +308,7 @@
 int W32_CALL getaddrinfo (const char *hostname, const char *servname,
                           const struct addrinfo *hints, struct addrinfo **res)
 {
-  static BOOL init = FALSE;
+  static bool init = FALSE;
   struct addrinfo      *cur, *pai, ai, ai0, sentinel;
   const struct afd     *afd;
   const struct explore *ex;
@@ -930,7 +930,7 @@
   struct servent *sp;
   const  char    *proto;
   WORD   port;
-  BOOL   allownumeric;
+  bool   allownumeric;
 
   if (!servname ||
       (ai->ai_family != AF_INET && ai->ai_family != AF_INET6))
diff -Naur old/src/geteth.c new/src/geteth.c
--- old/src/geteth.c	2014-07-21 13:20:48.000000000 +0200
+++ new/src/geteth.c	2026-04-09 07:18:47.357869946 +0200
@@ -187,7 +187,7 @@
 {
   size_t   len, i;
   unsigned eth [sizeof(*e)];
-  BOOL     ok, colon;
+  bool     ok, colon;
   char    *token = strltrim (in_buf);
   char    *ip_name;
   char    *tok_buf = NULL;
diff -Naur old/src/gethost6.c new/src/gethost6.c
--- old/src/gethost6.c	2017-11-22 10:14:58.000000000 +0100
+++ new/src/gethost6.c	2026-04-09 07:18:47.365870084 +0200
@@ -65,17 +65,17 @@
 
 static char             *host6Fname = NULL;
 static FILE             *host6file  = NULL;
-static BOOL              hostClose  = FALSE;
+static bool              hostClose  = FALSE;
 static struct _hostent6 *host0      = NULL;
 
-static BOOL did_lookup = FALSE;   /* tried a DNS lookup */
-static BOOL is_addr    = FALSE;   /* name is simply an IPv6 address */
+static bool did_lookup = FALSE;   /* tried a DNS lookup */
+static bool is_addr    = FALSE;   /* name is simply an IPv6 address */
 
-static BOOL gethostbyname6_internal (const char *name,
+static bool gethostbyname6_internal (const char *name,
                                      const char **alias,
                                      struct _hostent6 *ret);
 
-static BOOL gethostbyaddr6_internal (const char *addr,
+static bool gethostbyaddr6_internal (const char *addr,
                                      struct _hostent6 *ret);
 
 static struct hostent *fill_hostent6 (const struct _hostent6 *h);
@@ -94,7 +94,7 @@
 
 void W32_CALL ReadHosts6File (const char *fname)
 {
-  static BOOL been_here = FALSE;
+  static bool been_here = FALSE;
 
   if (!fname || !*fname)
      return;
@@ -275,7 +275,7 @@
   return (NULL);
 }
 
-static BOOL gethostbyname6_internal (const char *name, const char **alias,
+static bool gethostbyname6_internal (const char *name, const char **alias,
                                      struct _hostent6 *ret)
 {
   static char our_name[MAX_HOSTLEN];
@@ -444,7 +444,7 @@
   return (NULL);
 }
 
-static BOOL gethostbyaddr6_internal (const char *addr, struct _hostent6 *ret)
+static bool gethostbyaddr6_internal (const char *addr, struct _hostent6 *ret)
 {
   static char name [MAX_HOSTLEN];
   struct _hostent6 *h;
@@ -624,7 +624,7 @@
  * Reverse the 'host0' list before printing it (a bit nicer and intuitive).
  * I.e. on same order the host-names where put in the list by add_hostent().
  */
-static BOOL ReverseHosts6List (void)
+static bool ReverseHosts6List (void)
 {
   struct _hostent6 *h, **list;
   int    i, num_total, num_dynamic;
diff -Naur old/src/gethost.c new/src/gethost.c
--- old/src/gethost.c	2017-11-22 10:13:18.000000000 +0100
+++ new/src/gethost.c	2026-04-09 07:18:47.357869946 +0200
@@ -81,18 +81,18 @@
 
 static char            *hostFname  = NULL;
 static FILE            *hostFile   = NULL;
-static BOOL             hostClose  = FALSE;
+static bool             hostClose  = FALSE;
 static const char      *from_where = NULL;
 static struct _hostent *host0      = NULL;
 
-static BOOL did_lookup = FALSE;   /* tried a DNS lookup */
-static BOOL is_addr    = FALSE;   /* name is simply an IPv4 address */
+static bool did_lookup = FALSE;   /* tried a DNS lookup */
+static bool is_addr    = FALSE;   /* name is simply an IPv4 address */
 
-static BOOL gethostbyname_internal (const char *name,
+static bool gethostbyname_internal (const char *name,
                                     const char **alias,
                                     struct _hostent *ret);
 
-static BOOL gethostbyaddr_internal (const char *addr_name, int len, int type,
+static bool gethostbyaddr_internal (const char *addr_name, int len, int type,
                                     struct _hostent *ret);
 
 static struct hostent  *fill_hostent (const struct _hostent *h);
@@ -100,7 +100,7 @@
 static struct _hostent *add_hostent (struct _hostent *h,
                                      const char *name, const char *cname,
                                      DWORD *alist, DWORD addr, DWORD ttl,
-                                     BOOL windns);
+                                     bool windns);
 
 static void W32_CALL _endhostent (void)
 {
@@ -109,7 +109,7 @@
 
 void W32_CALL ReadHostsFile (const char *fname)
 {
-  static BOOL been_here = FALSE;
+  static bool been_here = FALSE;
 
   if (!fname || !*fname)
      return;
@@ -302,7 +302,7 @@
   return (NULL);
 }
 
-static BOOL gethostbyname_internal (const char *name, const char **alias,
+static bool gethostbyname_internal (const char *name, const char **alias,
                                     struct _hostent *ret)
 {
   static char our_name [MAX_HOSTLEN];
@@ -508,13 +508,13 @@
 }
 
 
-static BOOL gethostbyaddr_internal (const char *addr_name, int len, int type,
+static bool gethostbyaddr_internal (const char *addr_name, int len, int type,
                                     struct _hostent *ret)
 {
   static char name [MAX_HOSTLEN];
   struct _hostent *h = NULL;
   DWORD  addr;
-  BOOL   rc;
+  bool   rc;
   time_t now;
 
   h_errno = HOST_NOT_FOUND;
@@ -678,7 +678,7 @@
         DWORD           *alist,  /* List of alternate addresses */
         DWORD            addr,   /* Main IP-address */
         DWORD            ttl,    /* Time-to-live (sec) */
-        BOOL             windns) /* answer came from WinDNS */
+        bool             windns) /* answer came from WinDNS */
 {
   DWORD real_ttl = ttl;
   int   i;
@@ -765,7 +765,7 @@
  * Reverse the 'host0' list before printing it. I.e. on same order
  * the host-names where put in the list by add_hostent().
  */
-static BOOL ReverseHostsList (void)
+static bool ReverseHostsList (void)
 {
   struct _hostent *h, **list;
   int    i, num_total, num_dynamic;
diff -Naur old/src/get_ip.c new/src/get_ip.c
--- old/src/get_ip.c	2026-04-09 07:13:58.552819378 +0200
+++ new/src/get_ip.c	2026-04-09 07:18:47.357869946 +0200
@@ -36,7 +36,7 @@
                                      const struct hostent *he2,
                                      int af, int *err);
 
-static __inline void scan_interface (BOOL *have_v4, BOOL *have_v6)
+static __inline void scan_interface (bool *have_v4, bool *have_v6)
 {
   if (have_v4)
      *have_v4 = TRUE;
@@ -67,8 +67,8 @@
   struct hostent *he2 = NULL;
   struct in_addr  in4;
   struct in6_addr in6;
-  BOOL   have_v4 = TRUE, have_v6 = TRUE;
-  BOOL   v4 = FALSE, v6 = FALSE;
+  bool   have_v4 = TRUE, have_v6 = TRUE;
+  bool   v4 = FALSE, v6 = FALSE;
   int    tmp_err;
 
   SOCK_DEBUGF (("\ngetipnodebyname: %s ", name));
diff -Naur old/src/getnet.c new/src/getnet.c
--- old/src/getnet.c	2017-11-22 10:09:48.000000000 +0100
+++ new/src/getnet.c	2026-04-09 07:18:47.365870084 +0200
@@ -51,7 +51,7 @@
 static struct _netent *network0 = NULL;
 static char   *networkFname     = NULL;
 static FILE   *networkFile      = NULL;
-static BOOL    networkClose     = FALSE;
+static bool    networkClose     = FALSE;
 
 static __inline struct netent *fill_netent (const struct _netent *n)
 {
@@ -74,7 +74,7 @@
 
 void W32_CALL ReadNetworksFile (const char *fname)
 {
-  static BOOL been_here = FALSE;
+  static bool been_here = FALSE;
 
   if (!fname || !*fname)
      return;
diff -Naur old/src/get_ni.c new/src/get_ni.c
--- old/src/get_ni.c	2015-12-30 00:08:04.000000000 +0100
+++ new/src/get_ni.c	2026-04-09 07:18:47.357869946 +0200
@@ -76,7 +76,7 @@
 /*
  * Initialise afd_list[] with IPv6 protocol at highest priority.
  */
-static void afd_list_init (BOOL ip6_prio)
+static void afd_list_init (bool ip6_prio)
 {
   int i = (ip6_prio ? 0 : 1);
 
@@ -114,7 +114,7 @@
                           char *host, socklen_t hostlen,
                           char *serv, socklen_t servlen, int flags)
 {
-  static BOOL init = FALSE;
+  static bool init = FALSE;
   struct afd *afd;
   sa_family_t family = -1;
   DWORD       v4a;
diff -Naur old/src/getopt.c new/src/getopt.c
--- old/src/getopt.c	2015-09-29 19:56:36.000000000 +0200
+++ new/src/getopt.c	2026-04-09 07:18:47.365870084 +0200
@@ -25,7 +25,7 @@
 static char *next_opt;         /* Next character in cluster of options */
 static char *empty = "";       /* Empty string */
 
-static BOOL done;
+static bool done;
 static char sw_char;
 
 static char **options;         /* List of entries which are options */
diff -Naur old/src/getprot.c new/src/getprot.c
--- old/src/getprot.c	2017-11-22 10:10:19.000000000 +0100
+++ new/src/getprot.c	2026-04-09 07:18:47.365870084 +0200
@@ -51,7 +51,7 @@
 static struct _protoent *proto0 = NULL;
 static char   *protoFname       = NULL;
 static FILE   *protoFile        = NULL;
-static BOOL    protoClose       = FALSE;
+static bool    protoClose       = FALSE;
 
 static __inline struct protoent *fill_protoent (const struct _protoent *p)
 {
@@ -74,7 +74,7 @@
 
 void W32_CALL ReadProtoFile (const char *fname)
 {
-  static BOOL been_here = FALSE;
+  static bool been_here = FALSE;
 
   if (!fname || !*fname)
      return;
diff -Naur old/src/getserv.c new/src/getserv.c
--- old/src/getserv.c	2017-11-22 10:16:13.000000000 +0100
+++ new/src/getserv.c	2026-04-09 07:18:47.365870084 +0200
@@ -55,7 +55,7 @@
 static struct _servent *serv0 = NULL;
 static char   *servFname      = NULL;
 static FILE   *servFile       = NULL;
-static BOOL    servClose      = FALSE;
+static bool    servClose      = FALSE;
 
 /* For DOSX targets we use a small hash table to speed-up searches.
  */
@@ -98,7 +98,7 @@
  */
 void W32_CALL ReadServFile (const char *fname)
 {
-  static BOOL been_here = FALSE;
+  static bool been_here = FALSE;
 
   if (!fname || !*fname)
      return;
@@ -263,7 +263,7 @@
 
   for (s = serv0; s; s = s->s_next)
   {
-    BOOL chk_prot = FALSE;
+    bool chk_prot = FALSE;
 
     if (s->s_name && !stricmp(s->s_name,serv))
        chk_prot = TRUE;
diff -Naur old/src/gettod.c new/src/gettod.c
--- old/src/gettod.c	2018-02-09 12:55:05.000000000 +0100
+++ new/src/gettod.c	2026-04-09 07:18:47.365870084 +0200
@@ -121,7 +121,7 @@
   time_t        t;
   const struct  tm *tm;
   static char   buf[30];
-  static        BOOL tz_set = FALSE;
+  static        bool tz_set = FALSE;
 
   if (!tz_set)
      tzset();
@@ -386,7 +386,7 @@
 /*
  * Return a 'timeval' from a CPU timestamp.
  */
-BOOL get_tv_from_tsc (const struct ulong_long *tsc, struct timeval *tv)
+bool get_tv_from_tsc (const struct ulong_long *tsc, struct timeval *tv)
 {
   uint64 usecs;
 
@@ -490,7 +490,7 @@
 #include "printk.h"
 
 #ifdef __MSDOS__
-  static __inline BOOL day_rollover (void)
+  static __inline bool day_rollover (void)
   {
     return PEEKB (0, 0x470);
   }
@@ -526,8 +526,8 @@
 int main (int argc, char **argv)
 {
   DWORD loops   = 0;
-  BOOL  do_cmp  = FALSE;
-  BOOL  use_isr = FALSE;
+  bool  do_cmp  = FALSE;
+  bool  use_isr = FALSE;
   int   ch;
 
   putenv ("USE_RDTSC=0");
diff -Naur old/src/gettod.h new/src/gettod.h
--- old/src/gettod.h	2018-02-09 12:56:39.000000000 +0100
+++ new/src/gettod.h	2026-04-09 07:18:47.365870084 +0200
@@ -6,7 +6,7 @@
 extern void set_utc_offset (void);
 
 #if (DOSX) && defined(HAVE_UINT64)
-extern BOOL get_tv_from_tsc (const struct ulong_long *tsc,
+extern bool get_tv_from_tsc (const struct ulong_long *tsc,
                              struct timeval *tv);
 #endif
 
diff -Naur old/src/get_xby.h new/src/get_xby.h
--- old/src/get_xby.h	2013-08-18 12:36:06.000000000 +0200
+++ new/src/get_xby.h	2026-04-09 07:18:47.357869946 +0200
@@ -105,7 +105,7 @@
   struct _servent *s_next;
 };
 
-extern BOOL     called_from_getai;
+extern bool     called_from_getai;
 extern unsigned netdbCacheLife;
 
 /* These are not in any ./inc headers (<netdb.h> etc.). We export
diff -Naur old/src/highc.c new/src/highc.c
--- old/src/highc.c	2014-01-03 12:20:30.000000000 +0100
+++ new/src/highc.c	2026-04-09 07:18:47.365870084 +0200
@@ -170,7 +170,7 @@
 static int   level = 0;
 static const char *caller [MAX_LEVEL];
 
-static BOOL is_watt32_func (const char *func) /* incomplete */
+static bool is_watt32_func (const char *func) /* incomplete */
 {
   if (!strcmp(func,"watt_sock_init"))
      return (TRUE);
diff -Naur old/src/idna.c new/src/idna.c
--- old/src/idna.c	2015-06-29 10:50:34.000000000 +0200
+++ new/src/idna.c	2026-04-09 07:18:47.365870084 +0200
@@ -154,7 +154,7 @@
 /**
  * Initialise iconv; find codepage and mapping functions to use.
  */
-BOOL iconv_init (WORD cp)
+bool iconv_init (WORD cp)
 {
   int i;
 
@@ -380,7 +380,7 @@
  * Ref. http://www.imc.org/idna/do-idna.cgi
  *      http://www.norid.no/domenenavnbaser/ace/ace_technical.en.html
  */
-BOOL IDNA_convert_to_ACE (
+bool IDNA_convert_to_ACE (
           char   *name,   /* IN/OUT: native ASCII/ACE name */
           size_t *size)   /* IN:     length of name buf, */
 {                         /* OUT:    ACE encoded length */
@@ -447,7 +447,7 @@
  * 3) Repeat for all labels with "xn--" prefix.
  * 4) Collect Unicode strings and convert to original codepage.
  */
-BOOL IDNA_convert_from_ACE (
+bool IDNA_convert_from_ACE (
           char   *name,   /* IN/OUT: ACE/native ASCII name */
           size_t *size)   /* IN:     ACE raw string length, */
 {                         /* OUT:    ASCII deccoded length */
diff -Naur old/src/idna.h new/src/idna.h
--- old/src/idna.h	2015-06-29 10:46:50.000000000 +0200
+++ new/src/idna.h	2026-04-09 07:18:47.369870152 +0200
@@ -20,10 +20,10 @@
 #define IDNA_convert_from_ACE W32_NAMESPACE (IDNA_convert_from_ACE)
 
 extern const char *iconv_strerror (int rc);
-extern BOOL        iconv_init (WORD code_page);
+extern bool        iconv_init (WORD code_page);
 
-extern BOOL IDNA_convert_to_ACE   (char *name, size_t *size);
-extern BOOL IDNA_convert_from_ACE (char *name, size_t *size);
+extern bool IDNA_convert_to_ACE   (char *name, size_t *size);
+extern bool IDNA_convert_from_ACE (char *name, size_t *size);
 
 #ifndef MAX_LABELS
 #define MAX_LABELS 8
diff -Naur old/src/ioctl.c new/src/ioctl.c
--- old/src/ioctl.c	2014-01-03 12:20:30.000000000 +0100
+++ new/src/ioctl.c	2026-04-09 07:18:47.369870152 +0200
@@ -338,7 +338,7 @@
           */
          if (socket->so_type == SOCK_PACKET)
          {
-           BOOL rc;
+           bool rc;
 
            if (ifr->ifr_flags & IFF_PROMISC)
                 rc = _sock_set_promisc_rx_mode();
diff -Naur old/src/ip4_frag.c new/src/ip4_frag.c
--- old/src/ip4_frag.c	2017-11-29 14:18:15.000000000 +0100
+++ new/src/ip4_frag.c	2026-04-09 07:18:47.369870152 +0200
@@ -103,8 +103,8 @@
      };
 
 struct frag_bucket {
-       BOOL            used;     /* this bucket is taken */
-       BOOL            got_ofs0; /* we've received ofs-0 fragment */
+       bool            used;     /* this bucket is taken */
+       bool            got_ofs0; /* we've received ofs-0 fragment */
        int             active;   /* # of active fragments */
        used_map        map;      /* map of bits received */
        mac_address     mac_src;  /* remember for icmp_send_timexceed() */
@@ -119,7 +119,7 @@
      };
 
 struct frag_ctrl {
-       BOOL         used;       /* this position in table is in use */
+       bool         used;       /* this position in table is in use */
        DWORD        timer;      /* expiry for this fragment */
        struct hole *hole_first;
        in_Header   *ip;
@@ -214,7 +214,7 @@
  * If found, set '*bucket' to matching bucket and '*slot' to free
  * slot in '*bucket' and return TRUE.
  */
-static BOOL match_frag (const in_Header *ip, int *slot, int *bucket, int *free_bucket)
+static bool match_frag (const in_Header *ip, int *slot, int *bucket, int *free_bucket)
 {
   struct frag_key fk;
   int    i, b;
@@ -259,7 +259,7 @@
 /*
  * Check and report if fragment data 'ofs' and 'end' are okay
  */
-static __inline BOOL check_frag_ofs (const in_Header *ip, DWORD ofs, DWORD end)
+static __inline bool check_frag_ofs (const in_Header *ip, DWORD ofs, DWORD end)
 {
   if (ofs + end <= MAX_FRAG_SIZE &&   /* fragment offset okay, < 65528 */
       ofs + end <= USHRT_MAX-8)       /* must not wrap around 64kB */
@@ -290,7 +290,7 @@
 /*
  * Prepare and setup for reassembly
  */
-static BOOL setup_first_frag (const in_Header *ip, int idx)
+static bool setup_first_frag (const in_Header *ip, int idx)
 {
   struct frag_ctrl   *fc;
   struct frag_bucket *fb  = frag_buckets + idx; /* do fragment hanlding in this bucket */
@@ -423,8 +423,8 @@
   struct hole        *prev_hole = NULL;
   const in_Header    *ip        = *ip_ptr;
 
-  BOOL  found    = FALSE;
-  BOOL  got_hole = FALSE;
+  bool  found    = FALSE;
+  bool  got_hole = FALSE;
   int   slot, bucket, free_bucket;
 
   /* Not a fragment (or part of a chain) if offset is 0 and !IP_MF.
diff -Naur old/src/ip4_in.c new/src/ip4_in.c
--- old/src/ip4_in.c	2012-12-06 11:18:00.000000000 +0100
+++ new/src/ip4_in.c	2026-04-09 07:18:47.369870152 +0200
@@ -31,7 +31,7 @@
  *              - check the fragment chain queue for a match.
  *              - Demultiplex packet to correct protocol handler.
  */
-int _ip4_handler (const in_Header *ip, BOOL broadcast)
+int _ip4_handler (const in_Header *ip, bool broadcast)
 {
 #if defined(USE_FRAGMENTS)
   const in_Header *in_ip = ip;
@@ -39,7 +39,7 @@
   sock_type *s = NULL;
   DWORD  frag_ofs;
   WORD   frag_flg;
-  BOOL   delivered = TRUE;   /* assume yes */
+  bool   delivered = TRUE;   /* assume yes */
 
   SIO_TRACE (("_ip4_handler"));
 
diff -Naur old/src/ip4_in.h new/src/ip4_in.h
--- old/src/ip4_in.h	2004-07-07 13:56:54.000000000 +0200
+++ new/src/ip4_in.h	2026-04-09 07:18:47.369870152 +0200
@@ -27,7 +27,7 @@
 #define IN_EXPERIMENTAL(ip) ((ip & 0xF0000000UL) == 0xF0000000UL)
 #endif
 
-extern int _ip4_handler (const in_Header *ip, BOOL broadcast);
+extern int _ip4_handler (const in_Header *ip, bool broadcast);
 extern int _chk_ip4_header (const in_Header *ip);
 
 extern int _ip4_is_local_addr (DWORD ip);
diff -Naur old/src/ip4_out.c new/src/ip4_out.c
--- old/src/ip4_out.c	2014-01-03 12:20:30.000000000 +0100
+++ new/src/ip4_out.c	2026-04-09 07:18:47.369870152 +0200
@@ -53,7 +53,7 @@
 BYTE _default_tos      = 0;      /* Type-Of-Service */
 int  _default_ttl      = 254;    /* TTL on outgoing packets */
 int  _ip4_id_increment = 1;      /* Some stacks uses 5 */
-BOOL _ip4_dont_frag    = FALSE;  /* Don't set DF bit on every packet */
+bool _ip4_dont_frag    = FALSE;  /* Don't set DF bit on every packet */
 
 static WORD ip4_id = 0;
 
@@ -129,7 +129,7 @@
  * Note: host is on network order.
  * No IP options supported here.
  */
-static __inline in_Header *make_ip_pkt (DWORD host, BOOL first, char **data)
+static __inline in_Header *make_ip_pkt (DWORD host, bool first, char **data)
 {
   eth_address dest;
   in_Header  *ip;
@@ -152,7 +152,7 @@
  * \todo Handle IP/TCP options.
  */
 static in_Header *make_tcp_pkt (const _tcp_Socket *s,
-                                BOOL first, char **data)
+                                bool first, char **data)
 {
   in_Header *ip = (in_Header*) _eth_formatpacket (&s->his_ethaddr, IP4_TYPE);
 
@@ -192,7 +192,7 @@
  * Note: We assume 's->his_ethaddr' is filled by udp_open() or udp_demux().
  *       I.e. socket is active and not a broadcast socket.
  */
-static __inline in_Header *make_udp_pkt (const _udp_Socket *s, BOOL first,
+static __inline in_Header *make_udp_pkt (const _udp_Socket *s, bool first,
                                          unsigned len, char **data)
 {
   in_Header *ip = (in_Header*) _eth_formatpacket (&s->his_ethaddr, IP4_TYPE);
diff -Naur old/src/ip4_out.h new/src/ip4_out.h
--- old/src/ip4_out.h	2003-12-21 11:03:46.000000000 +0100
+++ new/src/ip4_out.h	2026-04-09 07:18:47.369870152 +0200
@@ -3,7 +3,7 @@
 #ifndef _w32_IP_OUT_H
 #define _w32_IP_OUT_H
 
-extern BOOL _ip4_dont_frag;     /*!< Enable the IP_DF bit on output */
+extern bool _ip4_dont_frag;     /*!< Enable the IP_DF bit on output */
 extern int  _ip4_id_increment;  /*!< The IP identifier increment */
 extern int  _default_ttl;       /*!< The default time-to-live for IP */
 extern BYTE _default_tos;       /*!< The default type-of-service for IP */
diff -Naur old/src/ip6_in.c new/src/ip6_in.c
--- old/src/ip6_in.c	2014-01-03 12:20:30.000000000 +0100
+++ new/src/ip6_in.c	2026-04-09 07:18:47.369870152 +0200
@@ -67,7 +67,7 @@
  * _ip6_handler - do a simple check on IPv6 header
  *              - Demultiplex packet to correct protocol handler
  */
-int _ip6_handler (const in6_Header *ip, BOOL broadcast)
+int _ip6_handler (const in6_Header *ip, bool broadcast)
 {
   const struct pkt_split *ps;
 
diff -Naur old/src/ip6_in.h new/src/ip6_in.h
--- old/src/ip6_in.h	2011-03-11 12:59:36.000000000 +0100
+++ new/src/ip6_in.h	2026-04-09 07:18:47.369870152 +0200
@@ -28,7 +28,7 @@
              } ip6_RouteHdr;
 
 #if defined(USE_IPV6)
-  extern int  _ip6_handler (const in6_Header *ip, BOOL broadcast);
+  extern int  _ip6_handler (const in6_Header *ip, bool broadcast);
   extern int  _ip6_init (void);
   extern int  _ip6_pkt_init (void);
   extern void _ip6_post_init (void);
diff -Naur old/src/lang.c new/src/lang.c
--- old/src/lang.c	2015-10-03 08:34:04.000000000 +0200
+++ new/src/lang.c	2026-04-09 07:18:47.373870221 +0200
@@ -600,7 +600,7 @@
 static char   *ptr       = NULL;
 static char   *end       = buf+BUF_SIZE-1;
 static int     line_cont = 0;
-static BOOL    do_ref    = TRUE;  /* We're parsing the English reference string */
+static bool    do_ref    = TRUE;  /* We're parsing the English reference string */
 static DWORD   crc_ref   = 0;     /* The CRC-32 of the English reference string */
 static jmp_buf bail_out;          /* The jmp_buf for getting out of here quickly */
 
diff -Naur old/src/linkaddr.c new/src/linkaddr.c
--- old/src/linkaddr.c	2015-06-28 10:46:30.000000000 +0200
+++ new/src/linkaddr.c	2026-04-09 07:18:47.373870221 +0200
@@ -138,7 +138,7 @@
   char  *out        = obuf;
   const BYTE *in    = (const BYTE*) LLADDR (sdl);
   const BYTE *inlim = in + sdl->sdl_alen;
-  BOOL  firsttime = TRUE;
+  bool  firsttime = TRUE;
 
   if (sdl->sdl_nlen)
   {
diff -Naur old/src/listen.c new/src/listen.c
--- old/src/listen.c	2014-01-03 12:20:30.000000000 +0100
+++ new/src/listen.c	2026-04-09 07:18:47.373870221 +0200
@@ -109,7 +109,7 @@
 {
   struct sockaddr_in addr;
   int    serv_sock, cli_sock = -1;
-  BOOL   quit = FALSE;
+  bool   quit = FALSE;
 
   dbug_init();
 
diff -Naur old/src/misc.c new/src/misc.c
--- old/src/misc.c	2018-05-23 13:34:29.000000000 +0200
+++ new/src/misc.c	2026-04-09 07:18:47.373870221 +0200
@@ -61,9 +61,9 @@
 const char hex_chars_lower[] = "0123456789abcdef";
 const char hex_chars_upper[] = "0123456789ABCDEF";
 
-BOOL win32_dos_box      = FALSE;  /*!< Running under Windows DOS-box */
-BOOL _watt_crtdbg_check = FALSE;  /*!< MSVC/_DEBUG only. See below */
-BOOL _watt_fatal_error  = FALSE;  /*!< We're exiting via an exception handler */
+bool win32_dos_box      = FALSE;  /*!< Running under Windows DOS-box */
+bool _watt_crtdbg_check = FALSE;  /*!< MSVC/_DEBUG only. See below */
+bool _watt_fatal_error  = FALSE;  /*!< We're exiting via an exception handler */
 WORD _watt_os_ver       = 0x622;  /*!< True DOS/WinNT version MSB.LSB */
 char _watt_assert_buf[256];       /*!< Print this in dbug_exit() */
 
@@ -251,7 +251,7 @@
  * Requires an "GenuineIntel" Pentium CPU to call Get_CR4()
  * (crashes on AMD K6-2 etc.).
  */
-static BOOL RDTSC_enabled (void)
+static bool RDTSC_enabled (void)
 {
   const char *env = getenv ("USE_RDTSC");
 
@@ -309,7 +309,7 @@
  */
 #define OffsetOf(x) (unsigned)&(x)
 
-static BOOL check_reg_struct (void)
+static bool check_reg_struct (void)
 {
 #if defined(__WATCOMC__)
   struct IREGS   *r1 = NULL;
@@ -352,7 +352,7 @@
  */
 void W32_CALL init_misc (void)
 {
-  static BOOL init = FALSE;
+  static bool init = FALSE;
 
   if (init)
      return;
@@ -547,7 +547,7 @@
   }
 }
 
-static BOOL got_crt_assert;
+static bool got_crt_assert;
 
 static int __cdecl crtdbg_report (int type, char *message, int *ret_val)
 {
@@ -882,7 +882,7 @@
 
 void os_yield (void)
 {
-  static BOOL do_yield = TRUE;
+  static bool do_yield = TRUE;
 
 #if defined(__DJGPP__)
   if (do_yield)
@@ -905,7 +905,7 @@
 
 
 #if defined(NOT_USED)
-BOOL watt_check_break (void)
+bool watt_check_break (void)
 {
   WORD head = 0x400 + PEEKW (0, 0x41A);
   WORD tail = 0x400 + PEEKW (0, 0x41C);
@@ -1357,7 +1357,7 @@
 }
 #endif /* WIN32 && (__WATCOMC__ || W32_IS_CODEGEARC) */
 
-BOOL is_in_stack (const void *ptr)
+bool is_in_stack (const void *ptr)
 {
   char     x;
   UINT_PTR stack_top = (UINT_PTR) &x;
@@ -1385,7 +1385,7 @@
  * \note MinGW (and other Win32 compilers?) puts 'const' data in
  *       read-only sections (.rdata). Detectable with IsBadWritePtr().
  */
-BOOL valid_addr (const void *addr, unsigned len)
+bool valid_addr (const void *addr, unsigned len)
 {
 #if defined(WIN32)
   /*
@@ -1393,7 +1393,7 @@
    * See the comment section here:
    *   http://msdn.microsoft.com/en-us/library/windows/desktop/aa366716(v=vs.85).aspx
    */
-  BOOL bad;
+  bool bad;
 
   if (_watt_crit_sect.SpinCount == (ULONG_PTR)-1) /* DeleteCriticalSection() was called!? */
      return (FALSE);
@@ -1658,7 +1658,7 @@
 
 #if defined(USE_DEBUG)
 #if defined(NOT_USED)
-BOOL is_big_endian (void)
+bool is_big_endian (void)
 {
  /* From Harbison & Steele.
   */
@@ -1683,7 +1683,7 @@
 
 #include <sys/pack_off.h>
 
-BOOL shell_exec (const char *cmd)
+bool shell_exec (const char *cmd)
 {
   struct cmd_block blk;
   size_t i = sizeof(blk.buf)-1;
@@ -1987,7 +1987,7 @@
 /*
  * Get/set DOS' memory allocation strategy.
  */
-static BOOL get_mem_strat (BYTE *strat)
+static bool get_mem_strat (BYTE *strat)
 {
   IREGS reg;
 
@@ -2000,7 +2000,7 @@
   return (TRUE);
 }
 
-static BOOL set_mem_strat (BYTE strat)
+static bool set_mem_strat (BYTE strat)
 {
   IREGS reg;
 
diff -Naur old/src/misc.h new/src/misc.h
--- old/src/misc.h	2017-07-06 18:01:31.000000000 +0200
+++ new/src/misc.h	2026-04-09 07:18:47.377870290 +0200
@@ -141,16 +141,16 @@
 extern const char hex_chars_lower[];
 extern const char hex_chars_upper[];
 
-extern BOOL     win32_dos_box;
-extern BOOL    _watt_fatal_error;
-extern BOOL    _watt_crtdbg_check;
+extern bool     win32_dos_box;
+extern bool    _watt_fatal_error;
+extern bool    _watt_crtdbg_check;
 extern WORD    _watt_os_ver;
 extern char    _watt_assert_buf[256];
 
 extern void     Wait         (unsigned msec);
-extern BOOL     is_in_stack  (const void *ptr);
+extern bool     is_in_stack  (const void *ptr);
 extern unsigned used_stack   (void);
-extern BOOL     valid_addr   (const void *addr, unsigned len);
+extern bool     valid_addr   (const void *addr, unsigned len);
 
 extern void     os_yield     (void);
 extern void     assert_fail  (const char *file, unsigned line, const char *what);
@@ -273,20 +273,20 @@
   #define get_file_version     W32_NAMESPACE (get_file_version)
 
   extern HANDLE      stdin_hnd, stdout_hnd;
-  extern BOOL       _watt_is_win9x, _watt_is_wow64;
-  extern BOOL       _watt_is_gui_app, _watt_use_bugtrap;
+  extern bool       _watt_is_win9x, _watt_is_wow64;
+  extern bool       _watt_is_gui_app, _watt_use_bugtrap;
 
   extern CONSOLE_SCREEN_BUFFER_INFO console_info;
 
-  extern BOOL        init_win_misc (void);
+  extern bool        init_win_misc (void);
 
-  extern BOOL        WinDnsQuery_A4     (const char *name, DWORD *ip4);
-  extern BOOL        WinDnsQuery_A6     (const char *name, void *ip6);
-  extern BOOL        WinDnsQuery_PTR4   (DWORD ip4, TCHAR *name, size_t size);
-  extern BOOL        WinDnsQuery_PTR6   (const void *ip6, TCHAR *name, size_t size);
-  extern BOOL        WinDnsCachePut_A4  (const char *name, DWORD ip4);
-  extern BOOL        WinDnsCachePut_A6  (const char *name, const void *ip6);
-  extern BOOL        get_file_version   (const char *file, char *buf, size_t buf_len);
+  extern bool        WinDnsQuery_A4     (const char *name, DWORD *ip4);
+  extern bool        WinDnsQuery_A6     (const char *name, void *ip6);
+  extern bool        WinDnsQuery_PTR4   (DWORD ip4, TCHAR *name, size_t size);
+  extern bool        WinDnsQuery_PTR6   (const void *ip6, TCHAR *name, size_t size);
+  extern bool        WinDnsCachePut_A4  (const char *name, DWORD ip4);
+  extern bool        WinDnsCachePut_A6  (const char *name, const void *ip6);
+  extern bool        get_file_version   (const char *file, char *buf, size_t buf_len);
   extern void        print_thread_times (HANDLE thread);
   extern void        print_perf_times   (void);
   extern void        print_process_times(void);
@@ -409,7 +409,7 @@
 extern WORD         _watt_handle_cbreak;
 extern volatile int _watt_cbroke;
 
-extern BOOL          tcp_cbreak_off (void);
+extern bool          tcp_cbreak_off (void);
 extern int           set_cbreak (int want_brk);
 extern void MS_CDECL sig_handler_watt (int sig);
 
@@ -477,7 +477,7 @@
 #define crc_table  W32_NAMESPACE (crc_table)
 
 extern DWORD *crc_table;
-extern BOOL   crc_init  (void);
+extern bool   crc_init  (void);
 extern DWORD  crc_bytes (const char *buf, size_t len);
 
 /*
diff -Naur old/src/netaddr.c new/src/netaddr.c
--- old/src/netaddr.c	2015-04-28 20:05:12.000000000 +0200
+++ new/src/netaddr.c	2026-04-09 07:18:47.377870290 +0200
@@ -126,7 +126,7 @@
  * Accepts 'a.b.c.d' or '[a.b.c.d]' forms.
  *  \retval TRUE if 'str' is an IPv4 address.
  */
-BOOL W32_CALL isaddr (const char *str)
+bool W32_CALL isaddr (const char *str)
 {
   int ch;
 
@@ -146,7 +146,7 @@
  * Accept "[ 0-9\[\]]+". Doesn't accept octal base.
  *  \retval TRUE if 'str' is a dotless address.
  */
-BOOL W32_CALL isaddr_dotless (const char *str, DWORD *ip)
+bool W32_CALL isaddr_dotless (const char *str, DWORD *ip)
 {
   char  buf[10] = { 0 };
   int   ch, i = 0;
@@ -209,7 +209,7 @@
 
 #if (DOSX)
   int  tmp [sizeof(eth_address)];
-  BOOL ok, colon = (src[2] == ':');
+  bool ok, colon = (src[2] == ':');
 
   ok = colon ? (sscanf(src,"%02x:%02x:%02x:%02x:%02x:%02x",
                        &tmp[0], &tmp[1], &tmp[2],
@@ -514,7 +514,7 @@
 #endif  /* USE_BSD_API */
 
 
-BOOL W32_CALL priv_addr (DWORD ip)
+bool W32_CALL priv_addr (DWORD ip)
 {
   /**
    * \brief Check if 'ip' is a private addresses in the
diff -Naur old/src/packet32.c new/src/packet32.c
--- old/src/packet32.c	2018-05-06 20:53:34.000000000 +0200
+++ new/src/packet32.c	2026-04-09 07:18:47.381870358 +0200
@@ -124,10 +124,10 @@
  * The WanPacket stuff; we can probably not send on this interface, but it
  * would be nice to sniff on it.
  */
-static BOOL use_wanpacket = FALSE;
+static bool use_wanpacket = FALSE;
 
-static BOOL PopulateAdaptersInfoList (void);
-static BOOL FreeAdaptersInfoList (void);
+static bool PopulateAdaptersInfoList (void);
+static bool FreeAdaptersInfoList (void);
 
 static void set_char_pointers (const char *adapter)
 {
@@ -182,10 +182,10 @@
  * So this function is used instead. I assume the true file-version of
  * NPF.sys is the same as "DisplayVersion" in Registry.
  */
-static BOOL get_npf_ver_from_registry (char *ret_ver, size_t ver_size)
+static bool get_npf_ver_from_registry (char *ret_ver, size_t ver_size)
 {
   char  str[100];
-  BOOL  rc = FALSE;
+  bool  rc = FALSE;
   DWORD size = sizeof(str);
   HKEY  key = NULL;
   LONG  status;
@@ -223,7 +223,7 @@
  *   SERVICE SID: S-1-5-80-1598306103-1873062032-3786967184-80952375-3176933300
  *   STATUS: Inactive
  */
-static BOOL npf_sc_showsid (char *sid_buf, size_t sid_size)
+static bool npf_sc_showsid (char *sid_buf, size_t sid_size)
 {
 }
 
@@ -232,7 +232,7 @@
  *   [SC] GetServiceDisplayName SUCCESS
  *   Name = WinPcap Packet Driver (NPF)
  */
-static BOOL npf_sc_getdisplayname (char *name_buf, size_t name_size)
+static bool npf_sc_getdisplayname (char *name_buf, size_t name_size)
 {
 }
 #endif
@@ -240,10 +240,10 @@
 /**
  * The winpcap init function (formerly DllMain).
  */
-BOOL PacketInitModule (void)
+bool PacketInitModule (void)
 {
   const struct ADAPTER_INFO *ai;
-  BOOL  rc = FALSE;
+  bool  rc = FALSE;
 
   winpkt_trace_func = "PacketInitModule";
   WINPKT_TRACE ("\n");
@@ -352,13 +352,13 @@
  * access from the NIC driver's memory without performing a copy.
  * This function tries to increase the size of that buffer.
  */
-static BOOL PacketSetMaxLookaheadsize (const ADAPTER *AdapterObject)
+static bool PacketSetMaxLookaheadsize (const ADAPTER *AdapterObject)
 {
   struct {
     PACKET_OID_DATA oidData;
     DWORD           filler;
   } oid;
-  BOOL rc;
+  bool rc;
 
   winpkt_trace_func = "PacketSetMaxLookaheadsize";
 
@@ -400,7 +400,7 @@
  * PacketSetReadEvt3xx() only used by version '3' NPF.SYS drivers.
  * PacketSetReadEvt4xx() used by newer versions.
  */
-static BOOL PacketSetReadEvt3xx (ADAPTER *AdapterObject)
+static bool PacketSetReadEvt3xx (ADAPTER *AdapterObject)
 {
   DWORD BytesReturned;
   char  EventName[40];
@@ -452,7 +452,7 @@
   return (TRUE);
 }
 
-static BOOL PacketSetReadEvt4xx (ADAPTER *AdapterObject)
+static bool PacketSetReadEvt4xx (ADAPTER *AdapterObject)
 {
   DWORD  BytesReturned;
   HANDLE hEvent;
@@ -488,7 +488,7 @@
   return (TRUE);
 }
 
-static BOOL PacketSetReadEvt (ADAPTER *AdapterObject)
+static bool PacketSetReadEvt (ADAPTER *AdapterObject)
 {
   if (npf_drv_ver[0] == '3')
      return PacketSetReadEvt3xx (AdapterObject);
@@ -506,9 +506,9 @@
  * This function installs the driver's service in the system using the
  * CreateService function.
  */
-static BOOL PacketInstallDriver (SC_HANDLE ascmHandle, SC_HANDLE *srvHandle)
+static bool PacketInstallDriver (SC_HANDLE ascmHandle, SC_HANDLE *srvHandle)
 {
-  BOOL  result = FALSE;
+  bool  result = FALSE;
   DWORD error  = 0UL;
 
   winpkt_trace_func = "PacketInstallDriver";
@@ -570,7 +570,7 @@
 static ADAPTER *PacketOpenAdapterNPF (const char *AdapterName)
 {
   ADAPTER       *lpAdapter;
-  BOOL           QuerySStat, Result;
+  bool           QuerySStat, Result;
   DWORD          error, KeyRes;
   SC_HANDLE      svcHandle = NULL;
   HKEY           PathKey;
@@ -775,10 +775,10 @@
  * and unloaded only when all the files on its devices are closed, i.e. when
  * all the applications that use WinPcap close all their adapters.
  */
-BOOL PacketStopDriver (void)
+bool PacketStopDriver (void)
 {
   SC_HANDLE scmHandle, schService;
-  BOOL      rc = FALSE;
+  bool      rc = FALSE;
 
   winpkt_trace_func = "PacketStopDriver";
 
@@ -834,9 +834,9 @@
  * Since we only use one adapter, free the 'adapters_list'
  * and the 'adapters_mutex' too.
  */
-BOOL PacketCloseAdapter (ADAPTER *adapter)
+bool PacketCloseAdapter (ADAPTER *adapter)
 {
-  BOOL rc = FALSE;
+  bool rc = FALSE;
   int  err = 0;
 
   winpkt_trace_func = "PacketCloseAdapter";
@@ -867,9 +867,9 @@
 /**
  * Called from pkt_release()
  */
-BOOL PacketExitModule (void)
+bool PacketExitModule (void)
 {
-  BOOL rc;
+  bool rc;
 
   winpkt_trace_func = "PacketExitModule";
   WINPKT_TRACE ("\n");
@@ -937,7 +937,7 @@
   return (sent);
 }
 
-BOOL PacketGetMacAddress (const ADAPTER *adapter, void *mac)
+bool PacketGetMacAddress (const ADAPTER *adapter, void *mac)
 {
   struct {
     PACKET_OID_DATA oidData;
@@ -1000,10 +1000,10 @@
  *   Windows 9x doesn't offer this possibility, therefore PacketSetMinToCopy
  *   is implemented under these systems only for compatibility.
  */
-BOOL PacketSetMinToCopy (const ADAPTER *AdapterObject, int nbytes)
+bool PacketSetMinToCopy (const ADAPTER *AdapterObject, int nbytes)
 {
   DWORD BytesReturned;
-  BOOL  rc;
+  bool  rc;
 
   rc = DeviceIoControl (AdapterObject->hFile, pBIOCSMINTOCOPY, &nbytes,
                         4, NULL, 0, &BytesReturned, NULL);
@@ -1060,10 +1060,10 @@
  *   example in the WinPcap developer's pack to see how to use statistics
  *   mode.
  */
-BOOL PacketSetMode (const ADAPTER *AdapterObject, DWORD mode)
+bool PacketSetMode (const ADAPTER *AdapterObject, DWORD mode)
 {
   DWORD BytesReturned;
-  BOOL  rc;
+  bool  rc;
 
   rc = DeviceIoControl (AdapterObject->hFile, pBIOCSMODE, &mode,
                         sizeof(mode), NULL, 0, &BytesReturned, NULL);
@@ -1083,10 +1083,10 @@
  * note: when opened, adapters have loopback capture enabled.
  * note: Effective on WinPcap 4.x+ only.
  */
-BOOL PacketSetLoopbackBehavior (const ADAPTER *AdapterObject, UINT behavior)
+bool PacketSetLoopbackBehavior (const ADAPTER *AdapterObject, UINT behavior)
 {
   DWORD BytesReturned;
-  BOOL  rc;
+  bool  rc;
 
   rc = DeviceIoControl (AdapterObject->hFile, pBIOCISETLOBBEH,
                         &behavior, sizeof(UINT), NULL,
@@ -1147,11 +1147,11 @@
  *       mode, and can be used to set the time interval between two
  *       statistic reports.
  */
-BOOL PacketSetReadTimeout (ADAPTER *AdapterObject, int timeout)
+bool PacketSetReadTimeout (ADAPTER *AdapterObject, int timeout)
 {
   DWORD BytesReturned;
   int   driverTimeout = -1;  /* NPF return immediately if a pkt is ready */
-  BOOL  rc;
+  bool  rc;
 
   AdapterObject->ReadTimeOut = timeout;
 
@@ -1183,10 +1183,10 @@
  * opened: the programmer should remember to set it to a proper value. As an
  * example, wpcap sets the buffer size to 1MB at the beginning of a capture.
  */
-BOOL PacketSetBuff (const ADAPTER *AdapterObject, DWORD dim)
+bool PacketSetBuff (const ADAPTER *AdapterObject, DWORD dim)
 {
   DWORD BytesReturned;
-  BOOL  rc;
+  bool  rc;
 
   rc = DeviceIoControl (AdapterObject->hFile, pBIOCSETBUFFERSIZE,
                         &dim, sizeof(dim), NULL, 0, &BytesReturned, NULL);
@@ -1205,11 +1205,11 @@
  *        filled by the function.
  * \return If the function succeeds, the return value is nonzero.
  */
-BOOL PacketGetStatsEx (const ADAPTER *AdapterObject, struct bpf_stat *stat)
+bool PacketGetStatsEx (const ADAPTER *AdapterObject, struct bpf_stat *stat)
 {
   struct bpf_stat tmp_stat;
   DWORD  read;
-  BOOL   rc;
+  bool   rc;
 
   memset (&tmp_stat, 0, sizeof(tmp_stat));
   rc = DeviceIoControl (AdapterObject->hFile,
@@ -1734,13 +1734,13 @@
  * If you use a facultative function, be careful to enclose it in an if
  * statement to check the result.
  */
-BOOL PacketRequest2 (const ADAPTER *AdapterObject, BOOL set,
+bool PacketRequest2 (const ADAPTER *AdapterObject, bool set,
                      PACKET_OID_DATA *oidData,
                      const char *file, unsigned line)
 {
   DWORD  transfered, in_size, out_size;
   DWORD  in_len, out_len;
-  BOOL   rc;
+  bool   rc;
   int    error = 0;
   char   prof_buf[100] = "";
 
@@ -1815,7 +1815,7 @@
  * addresses are dropeed, therefore set its dimension to sizeof(npf_if_addr)
  * if you want only the first address.
  */
-static BOOL GetAddressesFromRegistry (const char  *AdapterName,
+static bool GetAddressesFromRegistry (const char  *AdapterName,
                                       npf_if_addr *buffer,
                                       int         *NEntries)
 {
@@ -2088,11 +2088,11 @@
  * Used by PacketGetAdapters(). Queries the registry to fill the
  * '*ADAPTER_INFO' describing the new adapter.
  */
-static BOOL AddAdapter (const char *AdName)
+static bool AddAdapter (const char *AdName)
 {
   ADAPTER_INFO *TmpAdInfo = NULL;
   ADAPTER      *adapter   = NULL;
-  BOOL          rc = FALSE;
+  bool          rc = FALSE;
 
   winpkt_trace_func = "AddAdapter";
   WINPKT_TRACE ("adapter %s\n", AdName);
@@ -2171,7 +2171,7 @@
  * This function populates the list of adapter descriptions, retrieving
  * the information from the registry.
  */
-static BOOL PacketGetAdapters (void)
+static bool PacketGetAdapters (void)
 {
   HKEY  LinkageKey, AdapKey, OneAdapKey;
   DWORD RegKeySize = 0;
@@ -2353,9 +2353,9 @@
  * This function populates the list of adapter descriptions, invoking
  * PacketGetAdapters(). Called from PacketInitModule() only.
  */
-static BOOL PopulateAdaptersInfoList (void)
+static bool PopulateAdaptersInfoList (void)
 {
-  BOOL rc = TRUE;
+  bool rc = TRUE;
 
   winpkt_trace_func = "PopulateAdaptersInfoList";
   WINPKT_TRACE ("\n");
@@ -2378,7 +2378,7 @@
 /**
  * Free the list of the adapters.
  */
-static BOOL FreeAdaptersInfoList (void)
+static bool FreeAdaptersInfoList (void)
 {
   ADAPTER_INFO *next, *ai;
 
@@ -2393,7 +2393,7 @@
   return (TRUE);
 }
 
-BOOL WanPacketSetBpfFilter (WAN_ADAPTER *wan_adapter, PUCHAR FilterCode, DWORD Length)
+bool WanPacketSetBpfFilter (WAN_ADAPTER *wan_adapter, PUCHAR FilterCode, DWORD Length)
 {
   return (use_wanpacket ? (*p_WanPacketSetBpfFilter)(wan_adapter, FilterCode, Length) : FALSE);
 }
@@ -2403,12 +2403,12 @@
   return (use_wanpacket ? (*p_WanPacketOpenAdapter)() : NULL);
 }
 
-BOOL WanPacketCloseAdapter (WAN_ADAPTER *wan_adapter)
+bool WanPacketCloseAdapter (WAN_ADAPTER *wan_adapter)
 {
   return (use_wanpacket ? (*p_WanPacketCloseAdapter)(wan_adapter) : FALSE);
 }
 
-BOOL WanPacketSetBufferSize (WAN_ADAPTER *wan_adapter, DWORD BufferSize)
+bool WanPacketSetBufferSize (WAN_ADAPTER *wan_adapter, DWORD BufferSize)
 {
   return (use_wanpacket ? (*p_WanPacketSetBufferSize)(wan_adapter, BufferSize) : FALSE);
 }
@@ -2418,22 +2418,22 @@
   return (use_wanpacket ? (*p_WanPacketReceivePacket)(wan_adapter, Buffer, BufferSize) : 0UL);
 }
 
-BOOL WanPacketSetMinToCopy (WAN_ADAPTER *wan_adapter, DWORD MinToCopy)
+bool WanPacketSetMinToCopy (WAN_ADAPTER *wan_adapter, DWORD MinToCopy)
 {
   return (use_wanpacket ? (*p_WanPacketSetMinToCopy)(wan_adapter, MinToCopy) : FALSE);
 }
 
-BOOL WanPacketGetStats (WAN_ADAPTER *wan_adapter, struct bpf_stat *s)
+bool WanPacketGetStats (WAN_ADAPTER *wan_adapter, struct bpf_stat *s)
 {
   return (use_wanpacket ? (*p_WanPacketGetStats)(wan_adapter, s) : FALSE);
 }
 
-BOOL WanPacketSetReadTimeout (WAN_ADAPTER *wan_adapter, DWORD ReadTimeout)
+bool WanPacketSetReadTimeout (WAN_ADAPTER *wan_adapter, DWORD ReadTimeout)
 {
   return (use_wanpacket ? (*p_WanPacketSetReadTimeout)(wan_adapter, ReadTimeout) : FALSE);
 }
 
-BOOL WanPacketSetMode (WAN_ADAPTER *wan_adapter, DWORD Mode)
+bool WanPacketSetMode (WAN_ADAPTER *wan_adapter, DWORD Mode)
 {
   return (use_wanpacket ? (*p_WanPacketSetMode)(wan_adapter, Mode) : FALSE);
 }
@@ -2443,7 +2443,7 @@
   return (use_wanpacket ? (*p_WanPacketGetReadEvent)(wan_adapter) : NULL);
 }
 
-BOOL WanPacketTestAdapter (void)
+bool WanPacketTestAdapter (void)
 {
   return (use_wanpacket ? (*p_WanPacketTestAdapter)() : FALSE);
 }
diff -Naur old/src/packet32.h new/src/packet32.h
--- old/src/packet32.h	2018-05-06 17:09:26.000000000 +0200
+++ new/src/packet32.h	2026-04-09 07:18:47.381870358 +0200
@@ -284,29 +284,29 @@
 #define PacketSendPacket          W32_NAMESPACE (PacketSendPacket)
 #define PacketSetLoopbackBehavior W32_NAMESPACE (PacketSetLoopbackBehavior)
 
-BOOL PacketInitModule (void);
-BOOL PacketExitModule (void);
+bool PacketInitModule (void);
+bool PacketExitModule (void);
 
 const ADAPTER      *PacketOpenAdapter (const char *adapter_name);
 const ADAPTER_INFO *PacketFindAdInfo (const char *adapter_name);
 const ADAPTER_INFO *PacketGetAdInfo (void);
-BOOL                PacketCloseAdapter (ADAPTER *adapter);
+bool                PacketCloseAdapter (ADAPTER *adapter);
 const char         *PacketGetDriverVersion (void);
 
-BOOL PacketRequest2 (const ADAPTER *adapter, BOOL Set, PACKET_OID_DATA *OidData,
+bool PacketRequest2 (const ADAPTER *adapter, bool Set, PACKET_OID_DATA *OidData,
                      const char *file, unsigned line);
 
 #define PacketRequest(a,set,oid)  PacketRequest2 (a, set, oid, __FILE__, __LINE__)
 
-BOOL   PacketGetMacAddress (const ADAPTER *adapter, void *mac);
-BOOL   PacketSetMode (const ADAPTER *adapter, DWORD mode);
-BOOL   PacketSetBuff (const ADAPTER *adapter, DWORD dim);
-BOOL   PacketSetMinToCopy (const ADAPTER *adapter, int nbytes);
-BOOL   PacketSetReadTimeout (ADAPTER *adapter, int timeout);
+bool   PacketGetMacAddress (const ADAPTER *adapter, void *mac);
+bool   PacketSetMode (const ADAPTER *adapter, DWORD mode);
+bool   PacketSetBuff (const ADAPTER *adapter, DWORD dim);
+bool   PacketSetMinToCopy (const ADAPTER *adapter, int nbytes);
+bool   PacketSetReadTimeout (ADAPTER *adapter, int timeout);
 HANDLE PacketGetReadEvent (const ADAPTER *adapter);
-BOOL   PacketGetStatsEx (const ADAPTER *adapter, struct bpf_stat *st);
+bool   PacketGetStatsEx (const ADAPTER *adapter, struct bpf_stat *st);
 UINT   PacketSendPacket    (const ADAPTER *adapter, const void *buf, UINT len);
-BOOL   PacketSetLoopbackBehavior (const ADAPTER *adapter, UINT LoopbackBehavior);
+bool   PacketSetLoopbackBehavior (const ADAPTER *adapter, UINT LoopbackBehavior);
 
 UINT MS_CDECL PacketReceivePacket (const ADAPTER *adapter, void *buf, UINT buf_len);
 
@@ -333,16 +333,16 @@
 #define WanPacketGetReadEvent         W32_NAMESPACE (WanPacketGetReadEvent)
 #define WanPacketTestAdapter          W32_NAMESPACE (WanPacketTestAdapter)
 
-BOOL         WanPacketSetBpfFilter (WAN_ADAPTER *wan_adapter, PUCHAR FilterCode, DWORD Length);
+bool         WanPacketSetBpfFilter (WAN_ADAPTER *wan_adapter, PUCHAR FilterCode, DWORD Length);
 WAN_ADAPTER *WanPacketOpenAdapter (void);
-BOOL         WanPacketCloseAdapter (WAN_ADAPTER *wan_adapter);
-BOOL         WanPacketSetBufferSize (WAN_ADAPTER *wan_adapter, DWORD BufferSize);
+bool         WanPacketCloseAdapter (WAN_ADAPTER *wan_adapter);
+bool         WanPacketSetBufferSize (WAN_ADAPTER *wan_adapter, DWORD BufferSize);
 DWORD        WanPacketReceivePacket (WAN_ADAPTER *wan_adapter, PUCHAR Buffer, DWORD BufferSize);
-BOOL         WanPacketSetMinToCopy (WAN_ADAPTER *wan_adapter, DWORD MinToCopy);
-BOOL         WanPacketGetStats (WAN_ADAPTER *wan_adapter, struct bpf_stat *s);
-BOOL         WanPacketSetReadTimeout (WAN_ADAPTER *wan_adapter, DWORD ReadTimeout);
-BOOL         WanPacketSetMode (WAN_ADAPTER *wan_adapter, DWORD Mode);
+bool         WanPacketSetMinToCopy (WAN_ADAPTER *wan_adapter, DWORD MinToCopy);
+bool         WanPacketGetStats (WAN_ADAPTER *wan_adapter, struct bpf_stat *s);
+bool         WanPacketSetReadTimeout (WAN_ADAPTER *wan_adapter, DWORD ReadTimeout);
+bool         WanPacketSetMode (WAN_ADAPTER *wan_adapter, DWORD Mode);
 HANDLE       WanPacketGetReadEvent (WAN_ADAPTER *wan_adapter);
-BOOL         WanPacketTestAdapter (void);
+bool         WanPacketTestAdapter (void);
 
 #endif  /* _w32_PACKET32_H */
diff -Naur old/src/pcarp.c new/src/pcarp.c
--- old/src/pcarp.c	2015-06-28 18:18:32.000000000 +0200
+++ new/src/pcarp.c	2026-04-09 07:18:47.381870358 +0200
@@ -68,13 +68,13 @@
 static int  arp_timeout     = 2;      /* 2 seconds ARP timeout    */
 static int  arp_alive       = 300;    /* 5 min ARP cache lifespan */
 static int  arp_rexmit_to   = 250;    /* 250 milliseconds per try */
-static BOOL dead_gw_detect  = FALSE;  /* Enable Dead Gateway detection */
-static BOOL arp_gratiotous  = FALSE;
+static bool dead_gw_detect  = FALSE;  /* Enable Dead Gateway detection */
+static bool arp_gratiotous  = FALSE;
 
-static BOOL LAN_lookup (DWORD ip, eth_address *eth);
-static BOOL is_on_LAN  (DWORD ip);
+static bool LAN_lookup (DWORD ip, eth_address *eth);
+static bool is_on_LAN  (DWORD ip);
 
-static BOOL          arp_check_common (DWORD ip, eth_address *eth);
+static bool          arp_check_common (DWORD ip, eth_address *eth);
 static void W32_CALL arp_daemon (void);
 
 /**
@@ -188,7 +188,7 @@
 /*
  * Check if a mask is legal for specified address.
  */
-static BOOL legal_mask (DWORD mask, DWORD addr)
+static bool legal_mask (DWORD mask, DWORD addr)
 {
 #if 1
   ARGSUSED (addr);
@@ -245,7 +245,7 @@
  *
  * \todo Move to route.c and rename to route_add().
  */
-BOOL W32_CALL _arp_add_gateway (const char *config_string, DWORD ip)
+bool W32_CALL _arp_add_gateway (const char *config_string, DWORD ip)
 {
   struct gate_entry *gw;
   DWORD  subnet = 0UL;
@@ -380,7 +380,7 @@
 /**
  * Check if we have at least one default gateway.
  */
-BOOL W32_CALL _arp_have_default_gw (void)
+bool W32_CALL _arp_have_default_gw (void)
 {
   int i, num = 0;
 
@@ -453,7 +453,7 @@
 /**
  * Low-level ARP send function.
  */
-static BOOL arp_send (const arp_Header *arp, unsigned line)
+static bool arp_send (const arp_Header *arp, unsigned line)
 {
   TRACE (("_arp_send()\n"));
   ARGSUSED (arp);
@@ -464,7 +464,7 @@
 /**
  * Send broadcast ARP request.
  */
-static BOOL arp_send_request (DWORD ip)
+static bool arp_send_request (DWORD ip)
 {
   arp_Header *arp = (arp_Header*) _eth_formatpacket (&_eth_brdcast[0], ARP_TYPE);
 
@@ -487,7 +487,7 @@
  * Send unicast/broadcast ARP reply.
  * 'src_ip' and 'dst_ip' on host order.
  */
-BOOL W32_CALL _arp_reply (const void *e_dst, DWORD src_ip, DWORD dst_ip)
+bool W32_CALL _arp_reply (const void *e_dst, DWORD src_ip, DWORD dst_ip)
 {
   arp_Header *arp;
 
@@ -538,7 +538,7 @@
  * This is called by 'higher' routines to start an
  * ARP request. This function is non-blocking, i.e. returns 'immediately'.
  */
-static BOOL LAN_start_lookup (DWORD ip)
+static bool LAN_start_lookup (DWORD ip)
 {
   struct arp_entry *ae;
   int    i;
@@ -612,10 +612,10 @@
 /**
  * Lookup host in fixed/dynamic list.
  */
-static BOOL LAN_lookup (DWORD ip, eth_address *eth)
+static bool LAN_lookup (DWORD ip, eth_address *eth)
 {
   int  i;
-  BOOL rc = FALSE;
+  bool rc = FALSE;
 
   /* Check in dynamic + fixed list section
    */
@@ -636,9 +636,9 @@
 /**
  * Lookup host in pending list.
  */
-static BOOL LAN_lookup_pending (DWORD ip)
+static bool LAN_lookup_pending (DWORD ip)
 {
-  BOOL rc = FALSE;
+  bool rc = FALSE;
   int  i;
 
   /* Scan pending list section
@@ -663,7 +663,7 @@
  * checks for entries that have either expired or require a re-send.
  * The 'dynamic' entries are checked for expiry if requested.
  */
-static void arp_check_timeouts (BOOL check_dynamic_entries)
+static void arp_check_timeouts (bool check_dynamic_entries)
 {
   struct arp_entry *ae;
   int    i, num = 0;
@@ -782,7 +782,7 @@
           sizeof(route_list[0]));
 }
 
-static BOOL route_make_new_slot (DWORD host_ip, DWORD gate_ip, DWORD mask)
+static bool route_make_new_slot (DWORD host_ip, DWORD gate_ip, DWORD mask)
 {
   struct route_entry *re;
 
@@ -824,7 +824,7 @@
 /**
  * This should probably go into route.c
  */
-static BOOL is_on_LAN (DWORD ip)
+static bool is_on_LAN (DWORD ip)
 {
 #if 1
   return (((ip ^ my_ip_addr) & sin_mask) == 0);
@@ -841,7 +841,7 @@
  * different gateway is better suited to connect to the specified
  * host than the one we were using.
  */
-BOOL W32_CALL _arp_register (DWORD use_this_gateway_ip, DWORD for_this_host_ip)
+bool W32_CALL _arp_register (DWORD use_this_gateway_ip, DWORD for_this_host_ip)
 {
   int i;
 
@@ -901,9 +901,9 @@
 /**
  * Gets MAC of gateway needed to reach the given host.
  */
-static BOOL route_lookup (DWORD ip, eth_address *eth)
+static bool route_lookup (DWORD ip, eth_address *eth)
 {
-  BOOL rc = FALSE;
+  bool rc = FALSE;
   int  i;
 
   TRACE (("route_lookup (%s), ROUTE_FIRST_DYNAMIC: %d, route_top_dynamic: %d\n",
@@ -915,7 +915,7 @@
   for (i = ROUTE_FIRST_DYNAMIC; i < route_top_dynamic; i++)
   {
     struct route_entry *re = route_list + i;
-    BOOL   same_net = ON_SAME_NETWORK (ip, re->host_ip, re->mask);
+    bool   same_net = ON_SAME_NETWORK (ip, re->host_ip, re->mask);
 
     TRACE (("  route_list[%d]: gate_ip %s, host_ip: %s, ON_SAME_NETWORK: %d\n",
             i, INET_NTOA(re->gate_ip), INET_NTOA(re->host_ip), same_net));
@@ -939,10 +939,10 @@
  * Returns TRUE if the lookup of the gateway (assigned to the
  * supplied ip) is still pending.
  */
-static BOOL route_lookup_pending (DWORD ip)
+static bool route_lookup_pending (DWORD ip)
 {
   const struct route_entry *re = NULL;
-  BOOL  rc = FALSE;
+  bool  rc = FALSE;
   int   i = 0;
 
   /* Scan our pending list for the supplied IP
@@ -964,7 +964,7 @@
 /**
  * Start a route lookup.
  */
-static BOOL route_start_lookup (DWORD ip)
+static bool route_start_lookup (DWORD ip)
 {
   DWORD first_gate_ip;
   DWORD first_gate_mask;
@@ -1044,7 +1044,7 @@
  * If the attempt timed-out, we try the next fitting gateway.
  * If there are no more fitting gateways to try, the connection has failed.
  */
-static void route_check_timeouts (BOOL check_dynamic_entries)
+static void route_check_timeouts (bool check_dynamic_entries)
 {
   struct route_entry *re, temp;
   int    i, j;
@@ -1083,8 +1083,8 @@
     {
       /* Find the gateway that was tried last (the one that just timed out)
        */
-      BOOL found_last_gw = FALSE;
-      BOOL found_next_gw = FALSE;
+      bool found_last_gw = FALSE;
+      bool found_next_gw = FALSE;
 
       for (j = 0; j < gate_top; j++)
       {
@@ -1167,7 +1167,7 @@
  * This is called by 'higher' routines to start an ARP request.
  * This function is non-blocking, i.e. returns 'immediately'.
  */
-BOOL W32_CALL _arp_start_lookup (DWORD ip)
+bool W32_CALL _arp_start_lookup (DWORD ip)
 {
   TRACE (("_arp_start_lookup (%s)\n", INET_NTOA(ip)));
 
@@ -1184,7 +1184,7 @@
  * Lookup MAC-address of 'ip'.
  * \retval TRUE MAC for 'ip' is known.
  */
-BOOL W32_CALL _arp_lookup (DWORD ip, eth_address *eth)
+bool W32_CALL _arp_lookup (DWORD ip, eth_address *eth)
 {
   TRACE (("_arp_lookup (%s), is_on_LAN(): %d\n",
           INET_NTOA(ip), is_on_LAN(ip)));
@@ -1204,7 +1204,7 @@
  * \retval FALSE The IP has either been resolved (_arp_lookup() == TRUE),
  *         or the lookup has timed out, i.e. the host is unreachable.
  */
-BOOL W32_CALL _arp_lookup_pending (DWORD ip)
+bool W32_CALL _arp_lookup_pending (DWORD ip)
 {
   TRACE (("_arp_lookup_pending (%s)\n", INET_NTOA(ip)));
 
@@ -1217,7 +1217,7 @@
  * Lookup fixed MAC-address of 'ip'.
  * \retval TRUE Supplied 'ip' has a fixed MAC entry.
  */
-BOOL W32_CALL _arp_lookup_fixed (DWORD ip, eth_address *eth)
+bool W32_CALL _arp_lookup_fixed (DWORD ip, eth_address *eth)
 {
   int i;
 
@@ -1246,7 +1246,7 @@
 /**
  * Common stuff for _arp_resolve() and _arp_lookup()
  */
-static BOOL arp_check_common (DWORD ip, eth_address *eth)
+static bool arp_check_common (DWORD ip, eth_address *eth)
 {
 #if defined(USE_DEBUG)
   if (arp_trace_level >= 4)
@@ -1277,12 +1277,12 @@
 /**
  * The blocking lookup function visible to higher functions.
  */
-BOOL W32_CALL _arp_resolve (DWORD ip, eth_address *eth)
+bool W32_CALL _arp_resolve (DWORD ip, eth_address *eth)
 {
-  BOOL (*lookup) (DWORD ip, eth_address *eth);
-  BOOL (*start_lookup) (DWORD ip);
-  BOOL (*pending_lookup) (DWORD ip);
-  BOOL   rc = FALSE;
+  bool (*lookup) (DWORD ip, eth_address *eth);
+  bool (*start_lookup) (DWORD ip);
+  bool (*pending_lookup) (DWORD ip);
+  bool   rc = FALSE;
   WORD   brk_mode;
 
   TRACE (("_arp_resolve (%s)\n", INET_NTOA(ip)));
@@ -1350,7 +1350,7 @@
  * Add given IP/Ether address to ARP-cache.
  * \note 'ip' is on host order.
  */
-BOOL W32_CALL _arp_cache_add (DWORD ip, const void *eth, BOOL expires)
+bool W32_CALL _arp_cache_add (DWORD ip, const void *eth, bool expires)
 {
   struct arp_entry *ae;
 
@@ -1419,10 +1419,10 @@
  * Delete given 'ip' address from ARP-cache (dynamic, fixed or pending).
  * \note 'ip' is on host order.
  */
-BOOL W32_CALL _arp_cache_del (DWORD ip)
+bool W32_CALL _arp_cache_del (DWORD ip)
 {
   struct arp_entry *ae;
-  BOOL   rc = FALSE;
+  bool   rc = FALSE;
   int    i;
 
   /* Remove from dynamic list if present
@@ -1494,7 +1494,7 @@
  */
 static void W32_CALL arp_daemon (void)
 {
-  static BOOL  check_dynamic       = TRUE;
+  static bool  check_dynamic       = TRUE;
   static DWORD check_dynamic_timer = 0UL;
 
   arp_check_timeouts   (check_dynamic);
@@ -1564,13 +1564,13 @@
  * Receive ARP handler.
  * This processes incoming 'raw' ARP packets supplied by tcp_tick().
  */
-BOOL W32_CALL _arp_handler (const arp_Header *ah, BOOL brdcast)
+bool W32_CALL _arp_handler (const arp_Header *ah, bool brdcast)
 {
   struct arp_entry  *ae;
   const eth_address *e_src, *e_dst;   /* src/dest eth-addresses */
   DWORD src, dst;                     /* src/dest IP-addresses */
   WORD  hw_needed = intel16 (_eth_get_hwtype(NULL,NULL));
-  BOOL  to_us, do_reply = FALSE;
+  bool  to_us, do_reply = FALSE;
   int   i;
 
   DEBUG_RX (NULL, ah);
@@ -1671,7 +1671,7 @@
 
   for (i = arp_first_dynamic; i < ARP_TOP_DYNAMIC; i++)
   {
-    BOOL  equal;
+    bool  equal;
     DWORD timeout;
 
     ae = arp_list + i;
@@ -1727,10 +1727,10 @@
  * else is replying. Return non-zero if someone replied.
  * \note Blocks waiting for reply or timeout.
  */
-BOOL W32_CALL _arp_check_own_ip (eth_address *other_guy)
+bool W32_CALL _arp_check_own_ip (eth_address *other_guy)
 {
   DWORD save = my_ip_addr;
-  BOOL  rc;
+  bool  rc;
 
   TRACE (("_arp_check_own_ip()\n"));
 
diff -Naur old/src/pcarp.h new/src/pcarp.h
--- old/src/pcarp.h	2014-02-17 22:09:58.000000000 +0100
+++ new/src/pcarp.h	2026-04-09 07:18:47.381870358 +0200
@@ -12,8 +12,8 @@
        DWORD  gate_ip;       /**< The IP address of gateway */
        DWORD  subnet;        /**< The subnet mask of gateway */
        DWORD  mask;          /**< The netmask fgor the above entry */
-       BOOL   is_dead;       /* Dead Gate detection: */
-       BOOL   echo_pending;  /*   echo-reply pending */
+       bool   is_dead;       /* Dead Gate detection: */
+       bool   echo_pending;  /*   echo-reply pending */
        DWORD  chk_timer;     /*   check interval timer */
      };
 
@@ -63,28 +63,28 @@
  */
 W32_FUNC int  W32_CALL _arp_gateways_get (const struct gate_entry **);
 W32_FUNC int  W32_CALL _arp_check_gateways (void);
-W32_FUNC BOOL W32_CALL _arp_add_gateway  (const char* config_string, DWORD ip);
-W32_FUNC BOOL W32_CALL _arp_have_default_gw (void);
+W32_FUNC bool W32_CALL _arp_add_gateway  (const char* config_string, DWORD ip);
+W32_FUNC bool W32_CALL _arp_have_default_gw (void);
 W32_FUNC void W32_CALL _arp_kill_gateways (void);
 
 /** Blocking ARP resolve functions.
  *
  * Doesn't return until success or time-out.
  */
-W32_FUNC BOOL W32_CALL _arp_resolve      (DWORD ina, eth_address *eth);
-W32_FUNC BOOL W32_CALL _arp_check_own_ip (eth_address *eth);
+W32_FUNC bool W32_CALL _arp_resolve      (DWORD ina, eth_address *eth);
+W32_FUNC bool W32_CALL _arp_check_own_ip (eth_address *eth);
 
 /** New non-blocking functions, GvB 2002-09.
  */
-W32_FUNC BOOL W32_CALL _arp_start_lookup   (DWORD ip);
-W32_FUNC BOOL W32_CALL _arp_lookup         (DWORD ip, eth_address *eth);
-W32_FUNC BOOL W32_CALL _arp_lookup_fixed   (DWORD ip, eth_address *eth);
-W32_FUNC BOOL W32_CALL _arp_lookup_pending (DWORD ip);
+W32_FUNC bool W32_CALL _arp_start_lookup   (DWORD ip);
+W32_FUNC bool W32_CALL _arp_lookup         (DWORD ip, eth_address *eth);
+W32_FUNC bool W32_CALL _arp_lookup_fixed   (DWORD ip, eth_address *eth);
+W32_FUNC bool W32_CALL _arp_lookup_pending (DWORD ip);
 
 /** ARP cache functions.
  */
-W32_FUNC BOOL W32_CALL _arp_cache_add (DWORD ip, const void *eth, BOOL expire);
-W32_FUNC BOOL W32_CALL _arp_cache_del (DWORD ip);
+W32_FUNC bool W32_CALL _arp_cache_add (DWORD ip, const void *eth, bool expire);
+W32_FUNC bool W32_CALL _arp_cache_del (DWORD ip);
 W32_FUNC int  W32_CALL _arp_cache_get (const struct arp_entry **);
 W32_FUNC void W32_CALL _arp_cache_dump (void);
 W32_FUNC void W32_CALL _arp_gateways_dump (void);
@@ -94,11 +94,11 @@
 /** 'Internal' interface to pctcp.c & others.
  */
 W32_FUNC void W32_CALL _arp_init (void);
-W32_FUNC BOOL W32_CALL _arp_reply  (const void *mac_dst, DWORD src_ip, DWORD dst_ip);
-W32_FUNC BOOL W32_CALL _arp_handler (const arp_Header *arp, BOOL brdcast);
+W32_FUNC bool W32_CALL _arp_reply  (const void *mac_dst, DWORD src_ip, DWORD dst_ip);
+W32_FUNC bool W32_CALL _arp_handler (const arp_Header *arp, bool brdcast);
 
 /** ICMP redirection.
  */
-W32_FUNC BOOL W32_CALL _arp_register (DWORD use_this_gateway_ip, DWORD for_this_host_ip);
+W32_FUNC bool W32_CALL _arp_register (DWORD use_this_gateway_ip, DWORD for_this_host_ip);
 
 #endif
diff -Naur old/src/pcbootp.c new/src/pcbootp.c
--- old/src/pcbootp.c	2013-08-17 21:18:28.000000000 +0200
+++ new/src/pcbootp.c	2026-04-09 07:18:47.381870358 +0200
@@ -45,7 +45,7 @@
 static sock_type *boot_sock;
 
 static int  bootp_xmit  (struct bootp *bootp_out);
-static BOOL bootp_recv  (struct bootp *bootp_out, struct bootp *bootp_in);
+static bool bootp_recv  (struct bootp *bootp_out, struct bootp *bootp_in);
 static void bootp_parse (const struct bootp *bootp_in, int len);
 
 /**
@@ -107,7 +107,7 @@
   return sock_fastwrite (boot_sock, (BYTE*)bootp_out, sizeof(*bootp_out));
 }
 
-static BOOL bootp_recv (struct bootp *bootp_out, struct bootp *bootp_in)
+static bool bootp_recv (struct bootp *bootp_out, struct bootp *bootp_in)
 {
   DWORD exchange_id = bootp_out->bp_xid;
   DWORD boot_timer  = set_timeout (1000 * _bootp_timeout);
@@ -155,7 +155,7 @@
 {
   const BYTE *p   = &bootp_in->bp_vend[4]; /* Point after magic value */
   const BYTE *end = max + (const BYTE*)bootp_in;
-  BOOL  got_end = FALSE;
+  bool  got_end = FALSE;
 
   while (!got_end && p < end)
   {
diff -Naur old/src/pc_cbrk.c new/src/pc_cbrk.c
--- old/src/pc_cbrk.c	2015-06-18 22:42:54.000000000 +0200
+++ new/src/pc_cbrk.c	2026-04-09 07:18:47.381870358 +0200
@@ -187,7 +187,7 @@
 /*
  * Ref. http://msdn.microsoft.com/en-us/library/ms683242(v=vs.85).aspx
  */
-static BOOL WINAPI console_handler (DWORD event)
+static bool WINAPI console_handler (DWORD event)
 {
   if (event == CTRL_C_EVENT || event == CTRL_BREAK_EVENT)
   {
@@ -250,7 +250,7 @@
 /*
  * Return TRUE if ^C/^Break should be disabled
  */
-BOOL tcp_cbreak_off (void)
+bool tcp_cbreak_off (void)
 {
   return (cbreak_mode & 1);
 }
diff -Naur old/src/pcconfig.c new/src/pcconfig.c
--- old/src/pcconfig.c	2015-09-30 17:08:28.000000000 +0200
+++ new/src/pcconfig.c	2026-04-09 07:18:47.385870427 +0200
@@ -65,7 +65,7 @@
 int  sock_inactive     = 0;    /* defaults to forever */
 int  sock_data_timeout = 0;    /* timeout sending no data (forever) */
 WORD multihomes        = 0;    /* We have more than 1 IP-addresses */
-BOOL dynamic_host      = 0;    /* Reverse resolve assigned IP to true FQDN */
+bool dynamic_host      = 0;    /* Reverse resolve assigned IP to true FQDN */
 int  stkwalk_details   = 1;    /* Win: simple (1) or detailed(2) stack-trace. */
                                /* Effective only with USE_STACKWALKER. */
 int  fortify_fail_rate = 0;    /* The fail-rate for Fortify mallocer. */
@@ -87,7 +87,7 @@
 static char *config_name  = (char*) "WATTCP.CFG";  /* name of config file */
 static char *current_file = NULL;
 static UINT  current_line = 0;
-static BOOL  got_eol      = FALSE;  /* got end-of-line in 'current_line' */
+static bool  got_eol      = FALSE;  /* got end-of-line in 'current_line' */
 
 static const char *environ_names[] = {
                   "WATTCP.CFG",    /* pointer to config-file */
@@ -287,10 +287,10 @@
  * Accept "0x" or "x" prefix for ATOX types only. Accept ATOX values
  * without a prefix too.
  */
-static BOOL set_value (BOOL is_hex, const char *value, void *arg, int size)
+static bool set_value (bool is_hex, const char *value, void *arg, int size)
 {
   long  val = 0;
-  BOOL  ok = FALSE;
+  bool  ok = FALSE;
   const char *s = value;
 
 #if (DOSX)
@@ -958,11 +958,11 @@
   size_t num;            /* # of char in key/value */
   long   num_read;       /* # of bytes read */
   enum   ParseMode mode;
-  BOOL   quotemode;
-  BOOL   stripping;
-  BOOL   eof;
-  BOOL   last_eol = FALSE;
-  BOOL   equal_sign;
+  bool   quotemode;
+  bool   stripping;
+  bool   eof;
+  bool   last_eol = FALSE;
+  bool   equal_sign;
 
   num_read = 0;
   nextch   = '\0';
diff -Naur old/src/pcconfig.h new/src/pcconfig.h
--- old/src/pcconfig.h	2015-06-26 21:24:12.000000000 +0200
+++ new/src/pcconfig.h	2026-04-09 07:18:47.385870427 +0200
@@ -31,7 +31,7 @@
 extern DWORD cookies [MAX_COOKIES];
 extern WORD  last_cookie;
 extern int   ctrace_on;
-extern BOOL  dynamic_host;
+extern bool  dynamic_host;
 extern int   stkwalk_details;
 extern int   fortify_fail_rate;
 
diff -Naur old/src/pcdbug.c new/src/pcdbug.c
--- old/src/pcdbug.c	2018-05-06 16:56:24.000000000 +0200
+++ new/src/pcdbug.c	2026-04-09 07:18:47.385870427 +0200
@@ -113,7 +113,7 @@
 static const BYTE *dns_labels   (const BYTE *, const BYTE *, const BYTE *);
 
 static int write_pcap_header (void);
-static int write_pcap_packet (const void *pkt, BOOL out);
+static int write_pcap_packet (const void *pkt, bool out);
 
 #if defined(USE_PPPOE)
   static unsigned ccp_dump  (const BYTE *bp);
@@ -137,13 +137,13 @@
 
 static jmp_buf dbg_jmp;
 static char    dbg_name [MAX_PATHLEN+1] = "WATTCP.DBG";
-static BOOL    dbg_linebuf = FALSE;
-static BOOL    pcap_mode   = FALSE;
+static bool    dbg_linebuf = FALSE;
+static bool    pcap_mode   = FALSE;
 static DWORD   now;               /* ticks or milli-sec */
-static BOOL    outbound;          /* transmitting packet? */
-static BOOL    in_dbug_dump;
-static BOOL    use_gzip;          /* if using pcap-mode, use gzip-compression */
-static BOOL    use_ods = FALSE;   /* Win32: use OutputDebugString() */
+static bool    outbound;          /* transmitting packet? */
+static bool    in_dbug_dump;
+static bool    use_gzip;          /* if using pcap-mode, use gzip-compression */
+static bool    use_ods = FALSE;   /* Win32: use OutputDebugString() */
 static char    ip4_src [20];
 static char    ip4_dst [20];
 
@@ -196,9 +196,9 @@
  * These are public so they can be set by application if
  * running without a config-file. \todo make them static.
  */
-BOOL dbg_mode_all    = 1;
-BOOL dbg_print_stat  = 1;
-BOOL dbg_dns_details = 1;
+bool dbg_mode_all    = 1;
+bool dbg_print_stat  = 1;
+bool dbg_dns_details = 1;
 
 #if defined(USE_GZIP)
 static DWORD bytes_raw = 0UL;  /* raw uncompressed bytes written */
@@ -207,7 +207,7 @@
 
 /*----------------------------------------------------------------------*/
 
-BOOL dbug_file (void)
+bool dbug_file (void)
 {
   if (pcap_mode)        /* don't let anyone mess with this file */
      return (FALSE);
@@ -309,7 +309,7 @@
 /*
  * Return TRUE if MAC address is multicast (LSB bit 0 set).
  */
-static __inline BOOL MAC_is_mcast (const void *addr)
+static __inline bool MAC_is_mcast (const void *addr)
 {
   return ((*(const BYTE*)addr & 1) == 1);
 }
@@ -317,7 +317,7 @@
 /*
  * Return TRUE if MAC address is broadcast.
  */
-static __inline BOOL MAC_is_bcast (const void *addr)
+static __inline bool MAC_is_bcast (const void *addr)
 {
   return (!memcmp(addr, _eth_brdcast, _eth_mac_len));
 }
@@ -329,7 +329,7 @@
  *
  * To avoid confusion, we print a warning in this case.
  */
-static __inline BOOL is_looped (const in_Header *ip)
+static __inline bool is_looped (const in_Header *ip)
 {
   if (outbound || _pktserial)
      return (FALSE);
@@ -353,7 +353,7 @@
  *  - is broadcast and we don't filter broadcast.
  *  - is multicast and we don't filter multicast.
  */
-static __inline BOOL match_link_destination (const void *addr)
+static __inline bool match_link_destination (const void *addr)
 {
   if (!memcmp(addr, _eth_addr, _eth_mac_len) ||
       (!filter.BCAST && MAC_is_bcast(addr)) ||
@@ -368,7 +368,7 @@
  *  - is broadcast and we don't filter broadcast
  *    (ARP/RARP packets should never use multicast in it's header).
  */
-static __inline BOOL match_arp_rarp (const arp_Header *arp)
+static __inline bool match_arp_rarp (const arp_Header *arp)
 {
   if (!memcmp(&arp->dstEthAddr, _eth_addr, _eth_mac_len) ||
       _ip4_is_local_addr(intel(arp->dstIPAddr)))
@@ -384,7 +384,7 @@
  *  - matches our IP-address.
  *  - is broadcast and we don't filter (directed) IP-broadcast.
  */
-static __inline BOOL match_ip4_dest (const in_Header *ip)
+static __inline bool match_ip4_dest (const in_Header *ip)
 {
   DWORD destin = intel (ip->destination);
 
@@ -394,7 +394,7 @@
 }
 
 #if defined(USE_IPV6)
-static __inline BOOL match_ip6_dest (const in6_Header *ip)
+static __inline bool match_ip6_dest (const in6_Header *ip)
 {
   const void *dest = &ip->destination;
 
@@ -1019,7 +1019,7 @@
   const IGMPv2_packet *igmp2 = (const IGMPv2_packet*) igmp0;
   const IGMPv3_packet *igmp3 = (const IGMPv3_packet*) igmp0;
   WORD  igmp_len             = intel16 (ip->length) - hdr_len;
-  BOOL  is_v[4]              = { FALSE, FALSE, FALSE, FALSE };
+  bool  is_v[4]              = { FALSE, FALSE, FALSE, FALSE };
 
   if (igmp_len == sizeof(*igmp1) && igmp1->version == 1) /* 8 byte */
       is_v[1] = TRUE;
@@ -1753,7 +1753,7 @@
 
 /*----------------------------------------------------------------------*/
 
-static BOOL is_dns_packet (WORD src_port, WORD dst_port)
+static bool is_dns_packet (WORD src_port, WORD dst_port)
 {
   WORD dns_port;
 
@@ -1912,7 +1912,7 @@
                intel16(tcp->urgent), (u_long)seq, (u_long)ack);
   if (sock)
   {
-    BOOL  in_seq_space;
+    bool  in_seq_space;
     UINT  state = sock->state;
     long  delta_seq, delta_ack;
     long  ms_left, ms_right;
@@ -1996,7 +1996,7 @@
 
 /*----------------------------------------------------------------------*/
 
-static BOOL dbug_filter (void)
+static bool dbug_filter (void)
 {
 #if defined(NEED_PKT_SPLIT)
   const struct pkt_split *ps = outbound ? pkt_get_split_out() :
@@ -2175,9 +2175,9 @@
 /*----------------------------------------------------------------------*/
 
 static void dbug_dump (const void *sock, const in_Header *ip,
-                       const char *fname, unsigned line, BOOL out)
+                       const char *fname, unsigned line, bool out)
 {
-  static BOOL print_once = FALSE;
+  static bool print_once = FALSE;
   int    err;
 
   WATT_ASSERT (ip);
@@ -2871,7 +2871,7 @@
  */
 void W32_CALL dbug_init (void)
 {
-  static BOOL init = FALSE;
+  static bool init = FALSE;
 
   if (init)
   {
@@ -3681,7 +3681,7 @@
   return (rc == 0 ? -1 : (int)rc);
 }
 
-static int write_pcap_packet (const void *pkt, BOOL out)
+static int write_pcap_packet (const void *pkt, bool out)
 {
   const struct in_Header *ip = (const struct in_Header*) pkt;
   struct pcap_pkt_header  pc_hdr;
diff -Naur old/src/pcdbug.h new/src/pcdbug.h
--- old/src/pcdbug.h	2015-10-05 15:54:00.000000000 +0200
+++ new/src/pcdbug.h	2026-04-09 07:18:47.385870427 +0200
@@ -17,10 +17,10 @@
 
 extern const char *tcpStateName (UINT state);
 
-extern BOOL dbg_mode_all, dbg_print_stat, dbg_dns_details;
+extern bool dbg_mode_all, dbg_print_stat, dbg_dns_details;
 
 extern   void dbug_open (void);
-extern   BOOL dbug_file (void);
+extern   bool dbug_file (void);
 extern   int  dbug_write (const char *);
 extern   int  dbug_putc (int c);
 extern   void dbug_flush (void);
diff -Naur old/src/pcdbug.orig.c new/src/pcdbug.orig.c
--- old/src/pcdbug.orig.c	2015-11-15 14:08:01.000000000 +0100
+++ new/src/pcdbug.orig.c	2026-04-09 07:18:47.385870427 +0200
@@ -113,7 +113,7 @@
 static const BYTE *dns_labels   (const BYTE *, const BYTE *, const BYTE *);
 
 static int write_pcap_header (void);
-static int write_pcap_packet (const void *pkt, BOOL out);
+static int write_pcap_packet (const void *pkt, bool out);
 
 #if defined(USE_PPPOE)
   static unsigned ccp_dump  (const BYTE *bp);
@@ -136,13 +136,13 @@
 
 static jmp_buf dbg_jmp;
 static char    dbg_name [MAX_PATHLEN+1] = "WATTCP.DBG";
-static BOOL    dbg_linebuf = FALSE;
-static BOOL    pcap_mode   = FALSE;
+static bool    dbg_linebuf = FALSE;
+static bool    pcap_mode   = FALSE;
 static DWORD   now;               /* ticks or milli-sec */
-static BOOL    outbound;          /* transmitting packet? */
-static BOOL    in_dbug_dump;
-static BOOL    use_gzip;          /* if using pcap-mode, use gzip-compression */
-static BOOL    use_ods = FALSE;   /* Win32: use OutputDebugString() */
+static bool    outbound;          /* transmitting packet? */
+static bool    in_dbug_dump;
+static bool    use_gzip;          /* if using pcap-mode, use gzip-compression */
+static bool    use_ods = FALSE;   /* Win32: use OutputDebugString() */
 static char    ip4_src [20];
 static char    ip4_dst [20];
 
@@ -195,9 +195,9 @@
  * These are public so they can be set by application if
  * running without a config-file. \todo make them static.
  */
-BOOL dbg_mode_all    = 1;
-BOOL dbg_print_stat  = 1;
-BOOL dbg_dns_details = 1;
+bool dbg_mode_all    = 1;
+bool dbg_print_stat  = 1;
+bool dbg_dns_details = 1;
 
 #if defined(USE_GZIP)
 static DWORD bytes_raw = 0UL;  /* raw uncompressed bytes written */
@@ -206,7 +206,7 @@
 
 /*----------------------------------------------------------------------*/
 
-BOOL dbug_file (void)
+bool dbug_file (void)
 {
   if (pcap_mode)        /* don't let anyone mess with this file */
      return (FALSE);
@@ -308,7 +308,7 @@
 /*
  * Return TRUE if MAC address is multicast (LSB bit 0 set).
  */
-static __inline BOOL MAC_is_mcast (const void *addr)
+static __inline bool MAC_is_mcast (const void *addr)
 {
   return ((*(const BYTE*)addr & 1) == 1);
 }
@@ -316,7 +316,7 @@
 /*
  * Return TRUE if MAC address is broadcast.
  */
-static __inline BOOL MAC_is_bcast (const void *addr)
+static __inline bool MAC_is_bcast (const void *addr)
 {
   return (!memcmp(addr, _eth_brdcast, _eth_mac_len));
 }
@@ -328,7 +328,7 @@
  *
  * To avoid confusion, we print a warning in this case.
  */
-static __inline BOOL is_looped (const in_Header *ip)
+static __inline bool is_looped (const in_Header *ip)
 {
   if (outbound || _pktserial)
      return (FALSE);
@@ -352,7 +352,7 @@
  *  - is broadcast and we don't filter broadcast.
  *  - is multicast and we don't filter multicast.
  */
-static __inline BOOL match_link_destination (const void *addr)
+static __inline bool match_link_destination (const void *addr)
 {
   if (!memcmp(addr, _eth_addr, _eth_mac_len) ||
       (!filter.BCAST && MAC_is_bcast(addr)) ||
@@ -367,7 +367,7 @@
  *  - is broadcast and we don't filter broadcast
  *    (ARP/RARP packets should never use multicast in it's header).
  */
-static __inline BOOL match_arp_rarp (const arp_Header *arp)
+static __inline bool match_arp_rarp (const arp_Header *arp)
 {
   if (!memcmp(&arp->dstEthAddr, _eth_addr, _eth_mac_len) ||
       _ip4_is_local_addr(intel(arp->dstIPAddr)))
@@ -383,7 +383,7 @@
  *  - matches our IP-address.
  *  - is broadcast and we don't filter (directed) IP-broadcast.
  */
-static __inline BOOL match_ip4_dest (const in_Header *ip)
+static __inline bool match_ip4_dest (const in_Header *ip)
 {
   DWORD destin = intel (ip->destination);
 
@@ -393,7 +393,7 @@
 }
 
 #if defined(USE_IPV6)
-static __inline BOOL match_ip6_dest (const in6_Header *ip)
+static __inline bool match_ip6_dest (const in6_Header *ip)
 {
   const void *dest = &ip->destination;
 
@@ -1018,7 +1018,7 @@
   const IGMPv2_packet *igmp2 = (const IGMPv2_packet*) igmp0;
   const IGMPv3_packet *igmp3 = (const IGMPv3_packet*) igmp0;
   WORD  igmp_len             = intel16 (ip->length) - hdr_len;
-  BOOL  is_v[4]              = { FALSE, FALSE, FALSE, FALSE };
+  bool  is_v[4]              = { FALSE, FALSE, FALSE, FALSE };
 
   if (igmp_len == sizeof(*igmp1) && igmp1->version == 1) /* 8 byte */
       is_v[1] = TRUE;
@@ -1752,7 +1752,7 @@
 
 /*----------------------------------------------------------------------*/
 
-static BOOL is_dns_packet (WORD src_port, WORD dst_port)
+static bool is_dns_packet (WORD src_port, WORD dst_port)
 {
   WORD dns_port;
 
@@ -1911,7 +1911,7 @@
                intel16(tcp->urgent), (u_long)seq, (u_long)ack);
   if (sock)
   {
-    BOOL  in_seq_space;
+    bool  in_seq_space;
     UINT  state = sock->state;
     long  delta_seq, delta_ack;
     long  ms_left, ms_right;
@@ -1995,7 +1995,7 @@
 
 /*----------------------------------------------------------------------*/
 
-static BOOL dbug_filter (void)
+static bool dbug_filter (void)
 {
 #if defined(NEED_PKT_SPLIT)
   const struct pkt_split *ps = outbound ? pkt_get_split_out() :
@@ -2174,9 +2174,9 @@
 /*----------------------------------------------------------------------*/
 
 static void dbug_dump (const void *sock, const in_Header *ip,
-                       const char *fname, unsigned line, BOOL out)
+                       const char *fname, unsigned line, bool out)
 {
-  static BOOL print_once = FALSE;
+  static bool print_once = FALSE;
   int    err;
 
   WATT_ASSERT (ip);
@@ -2870,7 +2870,7 @@
  */
 void W32_CALL dbug_init (void)
 {
-  static BOOL init = FALSE;
+  static bool init = FALSE;
 
   if (init)
   {
@@ -3674,7 +3674,7 @@
   return (rc == 0 ? -1 : (int)rc);
 }
 
-static int write_pcap_packet (const void *pkt, BOOL out)
+static int write_pcap_packet (const void *pkt, bool out)
 {
   const struct in_Header *ip = (const struct in_Header*) pkt;
   struct pcap_pkt_header  pc_hdr;
diff -Naur old/src/pcdhcp.c new/src/pcdhcp.c
--- old/src/pcdhcp.c	2015-06-29 11:56:56.000000000 +0200
+++ new/src/pcdhcp.c	2026-04-09 07:18:47.385870427 +0200
@@ -107,7 +107,7 @@
  */
 #define DHCP_MIN_SIZE  280
 
-BOOL DHCP_did_gratuitous_arp = FALSE;
+bool DHCP_did_gratuitous_arp = FALSE;
 
 static WattDHCPConfigFunc config_func = NULL;
 
@@ -120,12 +120,12 @@
 static DWORD suggest_lease  = 0;
 static DWORD old_ip_addr    = 0;
 
-static BOOL  bcast_flag     = TRUE;
-static BOOL  got_offer      = FALSE;
-static BOOL  configured     = FALSE;
-static BOOL  arp_check_ip   = FALSE;
-static BOOL  cfg_read       = FALSE;
-static BOOL  cfg_saved      = FALSE;
+static bool  bcast_flag     = TRUE;
+static bool  got_offer      = FALSE;
+static bool  configured     = FALSE;
+static bool  arp_check_ip   = FALSE;
+static bool  cfg_read       = FALSE;
+static bool  cfg_saved      = FALSE;
 
 static int   dhcp_timeout   = 10;
 static int   max_retries    = 3;
@@ -138,7 +138,7 @@
 static time_t rebind_timeout;
 static time_t lease_timeout;
 static DWORD  send_timeout;
-static BOOL   trace_on = FALSE;
+static bool   trace_on = FALSE;
 
 static char       config_file [MAX_VALUELEN+1] = "";
 static sock_type *sock = NULL;
@@ -316,7 +316,7 @@
 /**
  * Send a DHCP request message.
  */
-static int DHCP_request (BOOL renew)
+static int DHCP_request (bool renew)
 {
   BYTE *opt = make_boot_header();
 
@@ -418,7 +418,7 @@
  * Optionally (if \b arp_check_ip is TRUE), send an ARP request
  * to check if our assigned IP-address is really vacant.
  */
-static BOOL DHCP_arp_check (DWORD my_ip)
+static bool DHCP_arp_check (DWORD my_ip)
 {
   eth_address eth;
 
@@ -483,7 +483,7 @@
 
       case DHCP_OPT_ROUTERS_ON_SNET:
            {
-             static BOOL gw_added = FALSE;
+             static bool gw_added = FALSE;
 
              if (!gw_added)
                 _arp_kill_gateways(); /* delete gateways from cfg-file */
@@ -496,7 +496,7 @@
 
       case DHCP_OPT_DNS_SRV:
            {
-             static BOOL dns_added = FALSE;
+             static bool dns_added = FALSE;
 
              for (len = 0; len < *(opt+1); len += sizeof(DWORD))
              {
@@ -705,7 +705,7 @@
  * \note Send release if we are configured \b and remaining lease
  *       is below minimum lease (DHCP_MIN_LEASE = 10 sec).
  */
-void W32_CALL DHCP_release (BOOL force)
+void W32_CALL DHCP_release (bool force)
 {
   if (force)
   {
@@ -745,7 +745,7 @@
 /**
  * Allocate and open a UDP socket for DHCP message exchange.
  */
-static sock_type *dhcp_open (const char *msg, BOOL use_broadcast)
+static sock_type *dhcp_open (const char *msg, bool use_broadcast)
 {
   udp_Socket *sock = malloc (sizeof(*sock));
   DWORD       host;
@@ -1193,7 +1193,7 @@
  */
 static int set_request_list (char *options)
 {
-  static BOOL init = FALSE;
+  static bool init = FALSE;
   int    num       = 0;
   int    maxreq    = 312 - 27; /* sizeof(dh_opt) - min size of rest */
   BYTE  *list, *start, *tok, *end;
@@ -1260,7 +1260,7 @@
  */
 static int set_user_class (const char *value)
 {
-  static BOOL init = FALSE;
+  static bool init = FALSE;
   int    total     = 0;
   int    maxreq    = sizeof(dhcp_out.dh_opt) - 27; /* adjust for rest */
   char  *list, *list_start;
@@ -1458,7 +1458,7 @@
  * \retval FALSE if lease times expired.
  * \retval TRUE otherwise.
  */
-static BOOL eval_timers (void)
+static bool eval_timers (void)
 {
   time_t now = time (NULL);
 #if defined(USE_DEBUG)
diff -Naur old/src/pcdns.c new/src/pcdns.c
--- old/src/pcdns.c	2018-01-11 18:29:10.000000000 +0100
+++ new/src/pcdns.c	2026-04-09 07:18:47.389870496 +0200
@@ -57,8 +57,8 @@
 #if defined(USE_BSD_API)
   #include <netdb.h>
 
-  BOOL called_from_resolve = FALSE;
-  BOOL called_from_ghbn    = FALSE;
+  bool called_from_resolve = FALSE;
+  bool called_from_ghbn    = FALSE;
 #endif
 
 #if !defined(WIN32)
@@ -73,9 +73,9 @@
 
 int (W32_CALL *_resolve_hook)(void);  /* user hook for terminating resolve() */
 
-BOOL  _resolve_exit;                  /* user hook interrupted */
-BOOL  _resolve_timeout;               /* (reverse) lookup timeout */
-BOOL  from_windns;                    /* the answer came from WinDNS */
+bool  _resolve_exit;                  /* user hook interrupted */
+bool  _resolve_timeout;               /* (reverse) lookup timeout */
+bool  from_windns;                    /* the answer came from WinDNS */
 DWORD dom_ttl;                        /* Time-to-Live of last response */
 int   dom_errno;                      /* domain errno (DNS_SRV_xx/DNS_CLI_xx) */
 char  dom_cname [MAX_HOSTLEN+1];      /* CNAME of last response */
@@ -83,9 +83,9 @@
 DWORD def_nameservers [MAX_NAMESERVERS];
 WORD  last_nameserver = 0;
 WORD  dns_timeout     = 0;
-BOOL  dns_recurse     = TRUE;    /* Recurse over dotless names */
-BOOL  dns_do_ipv6     = TRUE;    /* Do try to resolve to IPv6 */
-BOOL  dns_do_idna     = FALSE;   /* Convert non-ASCII names to/from ACE */
+bool  dns_recurse     = TRUE;    /* Recurse over dotless names */
+bool  dns_do_ipv6     = TRUE;    /* Do try to resolve to IPv6 */
+bool  dns_do_idna     = FALSE;   /* Convert non-ASCII names to/from ACE */
 WORD  dns_windns      = 0;       /* How to read/write the WinDns cache */
 
 static const char *loc_domain;   /* current subname to be used */
@@ -104,7 +104,7 @@
 /**
  * Initialise the query header and clear the alternate addr-list.
  */
-static void query_init (struct DNS_query *q, BOOL is_ip6)
+static void query_init (struct DNS_query *q, bool is_ip6)
 {
   int i;
 
@@ -125,7 +125,7 @@
 /**
  * Remember alternate address in dom_a?list[].
  */
-static void store_alt_address (const void *data, int idx, BOOL is_ip6)
+static void store_alt_address (const void *data, int idx, bool is_ip6)
 {
   if (idx < 0 || idx >= MAX_ADDRESSES)
      return;
@@ -149,7 +149,7 @@
 {
   const char *p;
   BYTE *q, *savedst;
-  BOOL  dotflag, defflag;
+  bool  dotflag, defflag;
   int   i;
 
   dotflag = defflag = FALSE;
@@ -428,7 +428,7 @@
     {
       if (rr_type == qtype)                /* Record type A or AAAA */
       {
-        BOOL is_ip6 = (qtype == DTYPE_AAAA);
+        bool is_ip6 = (qtype == DTYPE_AAAA);
 
         if (num_addr == 0)   /* prefered address should always be 1st */
         {
@@ -468,7 +468,7 @@
  * Check if the ID in the reply is among our ID(s) sent.
  * If not, ignore the response and keep waiting.
  */
-static __inline BOOL legal_ident (WORD id, const WORD *cache, int num)
+static __inline bool legal_ident (WORD id, const WORD *cache, int num)
 {
   int i;
 
@@ -490,10 +490,10 @@
 static int lookup_domain (
        struct DNS_query *q,           /* the UDP/IPv4 query packet */
        const char       *mname,         /* the host-name to query */
-       BOOL              add_dom,       /* append domain suffix */
+       bool              add_dom,       /* append domain suffix */
        DWORD             nameserver,    /* nameserver to use */
-       BOOL             *timedout,      /* set on timeout */
-       BOOL              is_aaaa,       /* Query A or AAAA record */
+       bool             *timedout,      /* set on timeout */
+       bool              is_aaaa,       /* Query A or AAAA record */
        void             *addr)          /* return address */
 {
   _udp_Socket sock;
@@ -777,9 +777,9 @@
   DWORD       ip4_addr = 0UL;
   int         count;
   char        namebuf [MAX_HOSTLEN], *dot;
-  BOOL        timeout [MAX_NAMESERVERS];
-  BOOL        got_ip  = FALSE;
-  BOOL        recurse = TRUE;
+  bool        timeout [MAX_NAMESERVERS];
+  bool        got_ip  = FALSE;
+  bool        recurse = TRUE;
   unsigned    len;
   static WORD brk_mode;
   struct DNS_query request;
@@ -946,9 +946,9 @@
   int      count;
   unsigned len;
   char     namebuf [MAX_HOSTLEN];
-  BOOL     timeout [MAX_NAMESERVERS];
+  bool     timeout [MAX_NAMESERVERS];
   WORD     brk_mode;
-  BOOL     got_ip = FALSE;
+  bool     got_ip = FALSE;
 
   if (!dns_do_ipv6)
   {
diff -Naur old/src/pcdns.h new/src/pcdns.h
--- old/src/pcdns.h	2018-05-06 16:55:15.000000000 +0200
+++ new/src/pcdns.h	2026-04-09 07:18:47.389870496 +0200
@@ -215,12 +215,12 @@
 
 extern char  dom_cname [MAX_HOSTLEN+1];
 extern DWORD dom_ttl;
-extern BOOL  dns_do_ipv6;
-extern BOOL  dns_do_idna;
+extern bool  dns_do_ipv6;
+extern bool  dns_do_idna;
 extern WORD  dns_windns;
-extern BOOL  called_from_resolve;
-extern BOOL  called_from_ghbn;
-extern BOOL  from_windns;
+extern bool  called_from_resolve;
+extern bool  called_from_ghbn;
+extern bool  from_windns;
 
 extern DWORD       dom_a4list [MAX_ADDRESSES+1];
 extern ip6_address dom_a6list [MAX_ADDRESSES+1];
diff -Naur old/src/pcicmp6.c new/src/pcicmp6.c
--- old/src/pcicmp6.c	2018-05-06 16:57:00.000000000 +0200
+++ new/src/pcicmp6.c	2026-04-09 07:18:47.393870564 +0200
@@ -149,12 +149,12 @@
 /*
  * Add the IPv4 'icmp6_6to4_gateway' address to the ND cache
  */
-BOOL icmp6_add_gateway4 (void)
+bool icmp6_add_gateway4 (void)
 {
   struct icmp6_cache *ce = NULL;
   ip6_address         ip;
   mac_address         eth;
-  BOOL                arp_ok;
+  bool                arp_ok;
 
   arp_ok = _arp_resolve (icmp6_6to4_gateway, &eth);
   if (arp_ok)
@@ -247,7 +247,7 @@
   const union  ICMP6_PKT *icmp;
   int          type, code;
   unsigned     len;
-  BOOL         for_me;
+  bool         for_me;
 
   DEBUG_RX (NULL, ip);
 
diff -Naur old/src/pcicmp6.h new/src/pcicmp6.h
--- old/src/pcicmp6.h	2018-05-06 17:32:16.000000000 +0200
+++ new/src/pcicmp6.h	2026-04-09 07:18:47.393870564 +0200
@@ -150,7 +150,7 @@
 extern int  icmp6_neigh_solic (const void *addr, eth_address *eth);
 extern int  icmp6_neighbor_advert (const eth_address *eth);
 extern int  icmp6_router_solicitation (void);
-extern BOOL icmp6_add_gateway4 (void);
+extern bool icmp6_add_gateway4 (void);
 
 struct icmp6_cache *icmp6_ncache_lookup (const void *ip);
 struct icmp6_cache *icmp6_ncache_insert (const void *ip, const void *eth);
diff -Naur old/src/pcicmp.c new/src/pcicmp.c
--- old/src/pcicmp.c	2018-05-06 16:56:43.000000000 +0200
+++ new/src/pcicmp.c	2026-04-09 07:18:47.389870496 +0200
@@ -144,10 +144,10 @@
  * Also check for null source address (0.0.0.0).
  * Broadcast destination is already filtered out by icmp_handler().
  */
-static BOOL icmp_check (const in_Header *ip, int type)
+static bool icmp_check (const in_Header *ip, int type)
 {
   DWORD src, dst;
-  BOOL  bcast;
+  bool  bcast;
 
   src   = intel (ip->source);
   dst   = intel (ip->destination);
@@ -377,14 +377,14 @@
 /**
  * Handler for incoming ICMP packets.
  */
-void icmp_handler (const in_Header *ip, BOOL broadcast)
+void icmp_handler (const in_Header *ip, bool broadcast)
 {
   union ICMP_PKT  *icmp;
   const in_Header *orig_ip;
   int              type, code;
   unsigned         len;
   DWORD            delta_time;
-  BOOL             for_me, i_orig;  /* is it for me, did I originate it */
+  bool             for_me, i_orig;  /* is it for me, did I originate it */
   const char      *msg;
 
   DEBUG_RX (NULL, ip);
diff -Naur old/src/pcicmp.h new/src/pcicmp.h
--- old/src/pcicmp.h	2018-05-06 17:08:24.000000000 +0200
+++ new/src/pcicmp.h	2026-04-09 07:18:47.393870564 +0200
@@ -197,7 +197,7 @@
 extern const char *icmp_redirect_str [4];
 extern const char *icmp_exceed_str [2];
 
-extern void icmp_handler (const in_Header *ip, BOOL broadcast);
+extern void icmp_handler (const in_Header *ip, bool broadcast);
 extern void icmp_doredirect (const char *value);
 extern int  icmp_send_timexceed (const in_Header *ip, const void *mac_dest);
 extern int  icmp_send_unreach (const in_Header *ip, int code);
diff -Naur old/src/pcigmp.c new/src/pcigmp.c
--- old/src/pcigmp.c	2018-05-06 16:57:57.000000000 +0200
+++ new/src/pcigmp.c	2026-04-09 07:18:47.393870564 +0200
@@ -64,14 +64,14 @@
 #define IPMULTI_MASK  0x007FFFFFUL  /**< to get the low-order 23 bits      */
 
 static struct MultiCast mcast_list [IPMULTI_SIZE];
-static BOOL             daemon_on = FALSE;
+static bool             daemon_on = FALSE;
 
 static void W32_CALL check_mcast_reports (void);
 
 /*
  * Check the LSB bit 0 of ether destination address.
  */
-static __inline BOOL is_eth_multicast (const in_Header *ip)
+static __inline bool is_eth_multicast (const in_Header *ip)
 {
   const BYTE *eth_dst;
 
@@ -255,11 +255,11 @@
  * Where:
  *   ip - the IP packet in question
  */
-void igmp_handler (const in_Header *ip, BOOL broadcast)
+void igmp_handler (const in_Header *ip, bool broadcast)
 {
   BYTE  i;
   DWORD src_ip, host;
-  BOOL  found = FALSE;
+  bool  found = FALSE;
   WORD  len   = in_GetHdrLen (ip);
   const IGMPv1_packet *igmp = (const IGMPv1_packet*) ((BYTE*)ip + len);
   struct MultiCast    *mc;
diff -Naur old/src/pcigmp.h new/src/pcigmp.h
--- old/src/pcigmp.h	2017-02-16 17:40:26.000000000 +0100
+++ new/src/pcigmp.h	2026-04-09 07:18:47.393870564 +0200
@@ -106,9 +106,9 @@
          eth_address ethaddr;      /**< Ethernet address of group */
          BYTE        processes;    /**< number of interested processes */
          DWORD       reply_timer;  /**< IGMP query reply timer */
-         BOOL        active;       /**< is this an active entry */
+         bool        active;       /**< is this an active entry */
        };
 
-  extern void igmp_handler (const in_Header *ip, BOOL brdcast);
+  extern void igmp_handler (const in_Header *ip, bool brdcast);
 #endif
 #endif
diff -Naur old/src/pcintr.c new/src/pcintr.c
--- old/src/pcintr.c	2013-08-15 13:08:12.000000000 +0200
+++ new/src/pcintr.c	2026-04-09 07:18:47.393870564 +0200
@@ -39,15 +39,15 @@
 
 void (W32_CALL *wintr_chain)(void) = NULL;
 
-static BOOL on_isr8;
+static bool on_isr8;
 
 #if !(DOSX & POWERPAK)
-  static BOOL inside_isr8 = FALSE;
+  static bool inside_isr8 = FALSE;
   static WORD indos_flag_seg, indos_flag_ofs;
   static WORD crit_err_seg, crit_err_ofs;   /* only for DOS 3.1+ */
 
   static void find_DOS_flags (void);
-  static BOOL DOS_is_idle (void);
+  static bool DOS_is_idle (void);
 #endif
 
 #if (DOSX & DJGPP)
@@ -74,9 +74,9 @@
   }
 #endif
 
-BOOL wintr_enable (void)
+bool wintr_enable (void)
 {
-  BOOL old = on_isr8;
+  bool old = on_isr8;
   on_isr8 = TRUE;
   return (old);
 }
@@ -363,7 +363,7 @@
   }
 }
 
-static BOOL DOS_is_idle (void)
+static bool DOS_is_idle (void)
 {
   BYTE indos, crit = 0;
 
diff -Naur old/src/pcpkt2.c new/src/pcpkt2.c
--- old/src/pcpkt2.c	2018-05-06 16:58:25.000000000 +0200
+++ new/src/pcpkt2.c	2026-04-09 07:18:47.393870564 +0200
@@ -248,7 +248,7 @@
 /*
  * Check the rx-buffer header.
  */
-static BOOL check_rx_element (const struct pkt_rx_element FARDATA *buf)
+static bool check_rx_element (const struct pkt_rx_element FARDATA *buf)
 {
   /* We got an upcall with wrong handle. This can happen if we
    * failed to release handle at program exit. But receive it anyway
@@ -471,7 +471,7 @@
           !_dx_call_real (SEG_OFS_ADDR(r.r_cs,r.r_ip), &rmc, 1)
 
 #elif (DOSX == 0) && 0
-  static BOOL DPMI_REAL_CALL (IREGS *r)
+  static bool DPMI_REAL_CALL (IREGS *r)
   {
     void interrupt (*func)(void) = MK_FP (cs, ip);
     (*func)();
diff -Naur old/src/pcpkt.c new/src/pcpkt.c
--- old/src/pcpkt.c	2015-06-28 10:53:44.000000000 +0200
+++ new/src/pcpkt.c	2026-04-09 07:18:47.393870564 +0200
@@ -48,7 +48,7 @@
 WORD _pktdevclass  = PDCLASS_UNKNOWN;  /**< Ethernet, Token, FDDI etc.       */
 WORD _pkt_ip_ofs   = 0;                /**< ofs from link-layer head to ip   */
 WORD _pkt_type_ofs = 0;                /**< ofs from link-layer head to type */
-BOOL _pktserial    = FALSE;            /**< using serial driver, SLIP/PPP    */
+bool _pktserial    = FALSE;            /**< using serial driver, SLIP/PPP    */
 BYTE _pktdevlevel  = 1;                /**< basic unless otherwise specified */
 int  _pkt_rxmode   = RXMODE_BROADCAST; /**< active receive mode              */
 int  _pkt_rxmode0  = -1;               /**< startup receive mode             */
@@ -65,8 +65,8 @@
 static DWORD pkt_drop_cnt    = 0;
 static BYTE  pkt_txretries   = 2;
 static int   pkt_txwait      = 0;       /* # msec to wait if Tx fails */
-static BOOL  pkt_use_near    = FALSE;   /* Use near-ptr if enabled */
-static BOOL  pkt_do_reset    = FALSE;   /* reset handle at exit */
+static bool  pkt_use_near    = FALSE;   /* Use near-ptr if enabled */
+static bool  pkt_do_reset    = FALSE;   /* reset handle at exit */
 static int   pkt_drvr_ver    = 0;       /* Driver internal version */
 static int   pkt_num_rx_bufs = RX_BUFS;
 
@@ -76,7 +76,7 @@
 #endif
 
 static struct PktParameters pkt_params;
-static BOOL   got_params   = FALSE;
+static bool   got_params   = FALSE;
 
 int pkt_release_handle (WORD handle);
 int pkt_reset_handle (WORD handle);
@@ -199,7 +199,7 @@
 static int  find_vector    (int first, int num);
 static int  setup_pkt_inf  (void);
 static void get_rmode_data (void *dest, unsigned size, WORD seg, WORD ofs);
-static BOOL pkt_api_entry  (IREGS *reg, unsigned called_from_line);
+static bool pkt_api_entry  (IREGS *reg, unsigned called_from_line);
 
 #define PKT_API(reg)       pkt_api_entry (reg, __LINE__)
 #define CARRY_BIT          1   /* carry bit in (e)flags register */
@@ -254,7 +254,7 @@
 
 /* Return TRUE is adapter is up.
  */
-BOOL W32_CALL pkt_is_init (void)
+bool W32_CALL pkt_is_init (void)
 {
   return (_pkt_inf != NULL);
 }
@@ -1090,7 +1090,7 @@
 {
   IREGS  regs;
   WORD   tx_seg, tx_ofs;
-  BOOL   fail = FALSE;
+  bool   fail = FALSE;
   int    tx_cnt;
 
   if (!_pkt_inf)
@@ -1505,7 +1505,7 @@
  * at offset 3 into intr-handler.
  */
 #if (DOSX & (PHARLAP|X32VM))
-  static BOOL check_intr_num (WORD intr_num)
+  static bool check_intr_num (WORD intr_num)
   {
     REALPTR rp;
     char    temp[16];
@@ -1521,7 +1521,7 @@
   }
 
 #elif (DOSX & DJGPP)
-  static BOOL check_intr_num (WORD intr_num)
+  static bool check_intr_num (WORD intr_num)
   {
     char  temp[16];
     DWORD rp;
@@ -1539,7 +1539,7 @@
   }
 
 #elif (DOSX & DOS4GW)
-  static BOOL check_intr_num (WORD intr_num)
+  static bool check_intr_num (WORD intr_num)
   {
     const BYTE *addr = (const BYTE*) dpmi_get_real_vector (intr_num);
 
@@ -1549,7 +1549,7 @@
   }
 
 #elif (DOSX & POWERPAK)
-  static BOOL check_intr_num (WORD intr_num)
+  static bool check_intr_num (WORD intr_num)
   {
     char    temp[16];
     DWORD   addr = (DWORD) dpmi_get_real_vector (intr_num);
@@ -1563,7 +1563,7 @@
   }
 
 #else       /* real-mode version */
-  static BOOL check_intr_num (WORD intr_num)
+  static bool check_intr_num (WORD intr_num)
   {
     char _far *addr = (char _far*) getvect (intr_num);
 
@@ -1894,7 +1894,7 @@
  * Returns TRUE if CARRY is clear, otherwise set '_pkt_errno' from DH
  * register and return FALSE.
  */
-static BOOL pkt_api_entry (IREGS *reg, unsigned line)
+static bool pkt_api_entry (IREGS *reg, unsigned line)
 {
   _pkt_errno = 0;
   reg->r_flags = 0;
diff -Naur old/src/pcpkt.h new/src/pcpkt.h
--- old/src/pcpkt.h	2014-09-23 14:17:28.000000000 +0200
+++ new/src/pcpkt.h	2026-04-09 07:18:47.393870564 +0200
@@ -39,7 +39,7 @@
 
 W32_DATA WORD _pktdevclass;       /**< Driver class; \ref <tcp.h> for values */
 W32_DATA WORD _pkt_ip_ofs;        /**< Offset from MAC-header to IP-header */
-W32_DATA BOOL _pktserial;         /**< Driver is a serial (SLIP/PPP) driver */
+W32_DATA bool _pktserial;         /**< Driver is a serial (SLIP/PPP) driver */
 W32_DATA char _pktdrvrname[];     /**< Name of PKDRVR */
 W32_DATA int  _pkt_rxmode;        /**< Current PKTDRVR/WinPcap Rx-mode */
 W32_DATA int  _pkt_rxmode0;       /**< Startup receive mode */
diff -Naur old/src/pcrarp.c new/src/pcrarp.c
--- old/src/pcrarp.c	2005-10-13 15:20:02.000000000 +0200
+++ new/src/pcrarp.c	2026-04-09 07:18:47.397870634 +0200
@@ -49,7 +49,7 @@
 /**
  * Handle incoming RARP packets.
  */
-BOOL _rarp_handler (const rarp_Header *rh, BOOL brdcast)
+bool _rarp_handler (const rarp_Header *rh, bool brdcast)
 {
   SIO_TRACE (("_rarp_handler"));
 
@@ -89,7 +89,7 @@
     {
       const struct rarp_Header *rarp;
       WORD  eth_type;
-      BOOL  bcast;
+      bool  bcast;
 
       if (chk_timeout(rarptimeout))
          return (0);
diff -Naur old/src/pcsarp.c new/src/pcsarp.c
--- old/src/pcsarp.c	2014-05-01 17:55:32.000000000 +0200
+++ new/src/pcsarp.c	2026-04-09 07:18:47.397870634 +0200
@@ -54,7 +54,7 @@
 struct host_list {
        DWORD             ip;     /* on host order */
        eth_address       mac;
-       BOOL              secure;
+       bool              secure;
        DSA              *dsa;
        struct host_list *next;
      };
@@ -117,7 +117,7 @@
   return (1);
 }
 
-static BOOL crypto_load_sarp_file (const char *pem_file, DSA **dsa)
+static bool crypto_load_sarp_file (const char *pem_file, DSA **dsa)
 {
   FILE *fil = fopen (pem_file, "rt");
 
@@ -217,7 +217,7 @@
 /**
  * Check if host should use secure ARP.
  */
-static __inline BOOL is_secure_host (DWORD ip)
+static __inline bool is_secure_host (DWORD ip)
 {
   const struct host_list *host;
 
@@ -230,7 +230,7 @@
 /**
  * Check if host is among the known hosts.
  */
-static __inline BOOL is_known_host (DWORD ip)
+static __inline bool is_known_host (DWORD ip)
 {
   const struct host_list *host;
 
diff -Naur old/src/pcsed.c new/src/pcsed.c
--- old/src/pcsed.c	2016-10-04 10:14:09.000000000 +0200
+++ new/src/pcsed.c	2026-04-09 07:18:47.397870634 +0200
@@ -52,15 +52,15 @@
 mac_address _eth_loop_addr;          /**< Link-layer loopback address */
 mac_address _eth_real_addr;          /**< Our real MAC address */
 BYTE        _eth_mac_len;            /**< Size of a MAC address */
-BOOL        _eth_is_init   = FALSE;  /**< we are initialised */
-BOOL        _ip_recursion  = FALSE;  /**< avoid recursion in arp_resolve() */
-BOOL        _eth_ndis3pkt  = FALSE;  /**< for DOS-programs only */
-BOOL        _eth_SwsVpkt   = FALSE;  /**< for DOS *and* Win32 programs */
-BOOL        _eth_airpcap   = FALSE;  /**< for Win32 using the AirPcap adapter */
-BOOL        _eth_wanpacket = FALSE;  /**< for Win32 using an WanPacket adapter */
-BOOL        _eth_npcap     = FALSE;  /**< for Win32 using an NPcap adapter */
-BOOL        _eth_win10pcap = FALSE;  /**< for Win32 using an Win10Pcap adapter */
-BOOL        _eth_winpcap   = FALSE;  /**< for Win32 using an WinPcap adapter (default) */
+bool        _eth_is_init   = FALSE;  /**< we are initialised */
+bool        _ip_recursion  = FALSE;  /**< avoid recursion in arp_resolve() */
+bool        _eth_ndis3pkt  = FALSE;  /**< for DOS-programs only */
+bool        _eth_SwsVpkt   = FALSE;  /**< for DOS *and* Win32 programs */
+bool        _eth_airpcap   = FALSE;  /**< for Win32 using the AirPcap adapter */
+bool        _eth_wanpacket = FALSE;  /**< for Win32 using an WanPacket adapter */
+bool        _eth_npcap     = FALSE;  /**< for Win32 using an NPcap adapter */
+bool        _eth_win10pcap = FALSE;  /**< for Win32 using an Win10Pcap adapter */
+bool        _eth_winpcap   = FALSE;  /**< for Win32 using an WinPcap adapter (default) */
 
 const char *_eth_not_init = "Packet driver not initialised";
 
@@ -153,7 +153,7 @@
  *
  * \note Loopback device cannot send to itself (potential recursion).
  */
-static int send_loopback (link_Packet pkt, BOOL is_ip6, unsigned *err_line)
+static int send_loopback (link_Packet pkt, bool is_ip6, unsigned *err_line)
 {
   struct pkt_ringbuf *q;
   const  in_Header   *ip;
@@ -310,7 +310,7 @@
 #if defined(USE_DEBUG) || defined(USE_LOOPBACK)
   unsigned errline = 0;
 #endif
-  BOOL send_loopback_to_driver = FALSE;
+  bool send_loopback_to_driver = FALSE;
 
   SIO_TRACE (("_eth_send, len %d", len));
 
@@ -855,7 +855,7 @@
  * Check (and fix?) the received ARCNET header.
  * All frames must have a common layout with headlen ARC_HDRLEN.
  */
-static BOOL fix_arc_head (const arcnet_Header *head, WORD *type)
+static bool fix_arc_head (const arcnet_Header *head, WORD *type)
 {
 #if defined(USE_DEBUG)
   const BYTE *raw = (const BYTE*)head;
@@ -1025,12 +1025,12 @@
  *  \retval Pointer to the IP-packet itself.
  *   IP-protocol is assumed. Can never be link-layer broadcast.
  */
-void * W32_CALL _eth_arrived (WORD *type_ptr, BOOL *broadcast)
+void * W32_CALL _eth_arrived (WORD *type_ptr, bool *broadcast)
 {
   union link_Packet *pkt;
   mac_address       *dst;
   void              *ret = NULL;
-  BOOL               is_bcast = FALSE;
+  bool               is_bcast = FALSE;
   WORD               type = 0;
 
   SIO_TRACE (("_eth_arrived"));
@@ -1243,11 +1243,11 @@
  * \retval 1 The group was joined successfully.
  * \retval 0 Attempt failed.
  */
-BOOL _eth_join_mcast_group (const struct MultiCast *mc)
+bool _eth_join_mcast_group (const struct MultiCast *mc)
 {
   eth_address list [IPMULTI_SIZE];
   int         i, len, nextentry;
-  BOOL        is_mcast1, is_mcast2, is_promis;
+  bool        is_mcast1, is_mcast2, is_promis;
 
   SIO_TRACE (("_eth_join_mcast_group"));
 
@@ -1319,7 +1319,7 @@
  * \retval 1 The group was left successfully.
  * \retval 0 Attempt failed.
  */
-BOOL _eth_leave_mcast_group (const struct MultiCast *mc)
+bool _eth_leave_mcast_group (const struct MultiCast *mc)
 {
   eth_address list[IPMULTI_SIZE];
   int i, len, idx;
diff -Naur old/src/pcsed.h new/src/pcsed.h
--- old/src/pcsed.h	2018-05-06 17:15:02.000000000 +0200
+++ new/src/pcsed.h	2026-04-09 07:18:47.397870634 +0200
@@ -225,8 +225,8 @@
 
 extern struct _eth_last_info _eth_last;
 
-extern BOOL        _ip_recursion, _eth_is_init;
-extern BOOL        _eth_ndis3pkt, _eth_winpcap, _eth_npcap, _eth_win10pcap, _eth_airpcap, _eth_SwsVpkt, _eth_wanpacket;
+extern bool        _ip_recursion, _eth_is_init;
+extern bool        _eth_ndis3pkt, _eth_winpcap, _eth_npcap, _eth_win10pcap, _eth_airpcap, _eth_SwsVpkt, _eth_wanpacket;
 extern BYTE        _eth_mac_len;
 extern const char *_eth_not_init;
 
@@ -238,8 +238,8 @@
 #if defined(USE_MULTICAST)
   #include "pcigmp.h"
 
-  BOOL _eth_join_mcast_group  (const struct MultiCast *mc);
-  BOOL _eth_leave_mcast_group (const struct MultiCast *mc);
+  bool _eth_join_mcast_group  (const struct MultiCast *mc);
+  bool _eth_leave_mcast_group (const struct MultiCast *mc);
 #endif
 
 /*
diff -Naur old/src/pcslip.c new/src/pcslip.c
--- old/src/pcslip.c	2013-08-17 21:19:54.000000000 +0200
+++ new/src/pcslip.c	2026-04-09 07:18:47.401870702 +0200
@@ -60,10 +60,10 @@
 
 /*****************************************************************/
 
-static BOOL modem_command (const char *cmd, const char *resp, int timeout)
+static bool modem_command (const char *cmd, const char *resp, int timeout)
 {
   DWORD timer;
-  BOOL  rc;
+  bool  rc;
   int   len = cmd ? strlen (cmd) : strlen (resp);
 
   if (cmd)
diff -Naur old/src/pcstat.c new/src/pcstat.c
--- old/src/pcstat.c	2015-09-28 14:29:50.000000000 +0200
+++ new/src/pcstat.c	2026-04-09 07:18:47.401870702 +0200
@@ -586,7 +586,7 @@
   const tok_Header       *tok;
   const fddi_Header      *fddi;
   const ICMP_PKT         *icmp;
-  BOOL  got_ip = FALSE;
+  bool  got_ip = FALSE;
   BYTE  type;
 
   for (ps = pkt_get_split_in(); ps->data; ps++)
@@ -726,7 +726,7 @@
   const arp_Header       *arp;
   const in_Header        *ip;
   const ICMP_PKT         *icmp;
-  BOOL  got_ip  = FALSE;
+  bool  got_ip  = FALSE;
   BYTE  type;
   DWORD ofs;
 
diff -Naur old/src/pctcp.c new/src/pctcp.c
--- old/src/pctcp.c	2018-05-06 16:59:12.000000000 +0200
+++ new/src/pctcp.c	2026-04-09 07:18:47.401870702 +0200
@@ -60,12 +60,12 @@
 
 unsigned _mss          = ETH_MAX_DATA - TCP_OVERHEAD;
 unsigned _mtu          = ETH_MAX_DATA;
-BOOL     mtu_discover  = 0;   /**< \todo Add PMTU discovery method */
-BOOL     mtu_blackhole = 0;   /**< \todo Add PMTU blackhole detection */
-BOOL     block_tcp     = 0;   /**< when application handles TCP itself */
-BOOL     block_udp     = 0;   /**< when application handles UDP itself */
-BOOL     block_icmp    = 0;   /**< when application handles ICMP itself */
-BOOL     block_ip      = 0;   /**< when application handles IP itself */
+bool     mtu_discover  = 0;   /**< \todo Add PMTU discovery method */
+bool     mtu_blackhole = 0;   /**< \todo Add PMTU blackhole detection */
+bool     block_tcp     = 0;   /**< when application handles TCP itself */
+bool     block_udp     = 0;   /**< when application handles UDP itself */
+bool     block_icmp    = 0;   /**< when application handles ICMP itself */
+bool     block_ip      = 0;   /**< when application handles IP itself */
 
 DWORD    my_ip_addr    = 0L;          /**< our IP address */
 DWORD    sin_mask      = 0xFFFFFF00L; /**< our net-mask, 255.255.255.0 */
@@ -126,13 +126,13 @@
 
   /** TCP option config flags (RFC 1323).
    */
-  BOOL tcp_opt_ts     = FALSE;
-  BOOL tcp_opt_wscale = FALSE;
-  BOOL tcp_opt_sack   = FALSE;
+  bool tcp_opt_ts     = FALSE;
+  bool tcp_opt_wscale = FALSE;
+  bool tcp_opt_sack   = FALSE;
 
   /** Misc TCP values.
    */
-  BOOL     tcp_nagle      = TRUE;         /**< Nagle algo. is on globally */
+  bool     tcp_nagle      = TRUE;         /**< Nagle algo. is on globally */
   unsigned tcp_keep_idle  = 2*60;         /**< idle time before sending keepalive */
   unsigned tcp_keep_intvl = 30;           /**< time between keepalive probes */
   unsigned tcp_max_idle   = 60;           /**< max idle time before kill */
@@ -143,12 +143,12 @@
   extern int _tcp_fsm (_tcp_Socket **s, const in_Header *ip);
 
   static _tcp_Socket *tcp_findseq (const in_Header *ip, const tcp_Header *tcp);
-  static void         tcp_sockreset (_tcp_Socket *s, BOOL proxy);
+  static void         tcp_sockreset (_tcp_Socket *s, bool proxy);
 
   static void tcp_no_arp   (_tcp_Socket *s);
   static void tcp_rtt_win  (_tcp_Socket *s);
   static void tcp_upd_win  (_tcp_Socket *s, unsigned line);
-  static BOOL tcp_checksum (const in_Header *ip, const tcp_Header *tcp, int len);
+  static bool tcp_checksum (const in_Header *ip, const tcp_Header *tcp, int len);
 #endif
 
 static void udp_close (const _udp_Socket *s);
@@ -196,7 +196,7 @@
 int W32_CALL udp_open (_udp_Socket *s, WORD lport, DWORD ip,
                        WORD port, ProtoHandler handler)
 {
-  BOOL bcast;
+  bool bcast;
 
   SIO_TRACE (("udp_open"));
 
@@ -597,7 +597,7 @@
  * Unthread a socket from the tcp socket list, if it's there.
  * Free Tx-buffer if set in tcp_set_window().
  */
-_tcp_Socket *_tcp_unthread (_tcp_Socket *ds, BOOL free_tx)
+_tcp_Socket *_tcp_unthread (_tcp_Socket *ds, bool free_tx)
 {
   _tcp_Socket *s, *prev;
   _tcp_Socket *next = NULL;
@@ -646,7 +646,7 @@
  *
  * All TCP input processing is done from here.
  */
-_tcp_Socket *_tcp_handler (const in_Header *ip, BOOL broadcast)
+_tcp_Socket *_tcp_handler (const in_Header *ip, bool broadcast)
 {
 #if defined(USE_IPV6)
   const in6_Header *ip6 = (const in6_Header*)ip;
@@ -661,7 +661,7 @@
   DWORD        destin = intel (ip->destination);
   DWORD        seq;
   WORD         dstPort, srcPort;
-  BOOL         is_ip4 = (ip->ver == 4);
+  bool         is_ip4 = (ip->ver == 4);
 
   SIO_TRACE (("_tcp_handler"));
 
@@ -849,7 +849,7 @@
 /**
  * Verify checksum for an UDP packet.
  */
-static BOOL udp_checksum (const in_Header *ip, const udp_Header *udp, int len)
+static bool udp_checksum (const in_Header *ip, const udp_Header *udp, int len)
 {
   tcp_PseudoHeader ph = { 0,0,0,0,0,0 };
 
@@ -874,12 +874,12 @@
  * Demultiplexer for incoming UDP packets.
  * Don't debug packet if no match was found (except if '*udp_err').
  */
-static _udp_Socket *udp_demux (const in_Header *ip, BOOL ip_bcast,
+static _udp_Socket *udp_demux (const in_Header *ip, bool ip_bcast,
                                DWORD destin, WORD srcPort, WORD dstPort,
-                               BOOL *udp_err)
+                               bool *udp_err)
 {
   _udp_Socket *s;
-  BOOL is_ip4 = (ip->ver == 4);
+  bool is_ip4 = (ip->ver == 4);
 
 #if defined(USE_IPV6)
   const in6_Header *ip6 = (const in6_Header*)ip;
@@ -1041,7 +1041,7 @@
 /**
  * Handler for incoming UDP packets.
  */
-_udp_Socket *_udp_handler (const in_Header *ip, BOOL broadcast)
+_udp_Socket *_udp_handler (const in_Header *ip, bool broadcast)
 {
 #if defined(USE_IPV6)
   const in6_Header *ip6 = (const in6_Header*)ip;
@@ -1052,9 +1052,9 @@
   UINT         len = 0;
   WORD         dst_port, src_port;
   DWORD        destin = 0;
-  BOOL         is_ip4 = (ip->ver == 4);
-  BOOL         ip_bcast = 0;
-  BOOL         udp_err;
+  bool         is_ip4 = (ip->ver == 4);
+  bool         ip_bcast = 0;
+  bool         udp_err;
 
   const BYTE       *data;
   const udp_Header *udp = NULL;
@@ -1219,7 +1219,7 @@
  * Called periodically to perform retransmissions.
  * \arg if 'force == 1' do it now.
  */
-void tcp_Retransmitter (BOOL force)
+void tcp_Retransmitter (bool force)
 {
   _tcp_Socket *s, *next;
 
@@ -1436,7 +1436,7 @@
   while (tick_active == 1)
   {
     WORD  eth_type = 0;
-    BOOL  brdcast  = FALSE;
+    bool  brdcast  = FALSE;
     void *packet   = _eth_arrived (&eth_type, &brdcast);
 
     if (!packet)  /* packet points to network layer protocol */
@@ -1641,7 +1641,7 @@
                   const char *msg, const void *arg) /* use a var-arg here ? */
 {
   WORD         src_port, dst_port;
-  BOOL         passive = FALSE;
+  bool         passive = FALSE;
   int          len     = in_GetHdrLen (ip);
   udp_Header  *udp     = (udp_Header*) ((BYTE*)ip + len);
   _udp_Socket *s;
@@ -1720,7 +1720,7 @@
  * Default is to terminate socket on receiving 2nd
  * ICMP error. Use sock_sturdy() to change.
  */
-static BOOL tcp_stress_test (_tcp_Socket *s, const char *msg)
+static bool tcp_stress_test (_tcp_Socket *s, const char *msg)
 {
   if (s->stress++ > s->rigid && s->rigid < 100)  /* halt it */
   {
@@ -1966,7 +1966,7 @@
 /**
  * Resets a TCP connection.
  */
-static void tcp_sockreset (_tcp_Socket *s, BOOL proxy)
+static void tcp_sockreset (_tcp_Socket *s, bool proxy)
 {
   const char *str = proxy ? "Proxy reset connection"
                           : "Remote reset connection";
@@ -2047,7 +2047,7 @@
  * Verify the TCP header checksum.
  * \note Not used for IPv6.
  */
-static BOOL tcp_checksum (const in_Header *ip, const tcp_Header *tcp, int len)
+static bool tcp_checksum (const in_Header *ip, const tcp_Header *tcp, int len)
 {
   tcp_PseudoHeader ph = { 0,0,0,0,0,0 };
 
@@ -2305,7 +2305,7 @@
 /**
  * Append options to output TCP header.
  */
-static __inline int tcp_options (_tcp_Socket *s, BYTE *opt, BOOL is_syn)
+static __inline int tcp_options (_tcp_Socket *s, BYTE *opt, bool is_syn)
 {
   int len = 0;
 
@@ -2358,7 +2358,7 @@
   struct in6_Header *ip6 = NULL;
 #endif
 
-  BOOL         tx_ok;
+  bool         tx_ok;
   BYTE        *data;             /* where to copy user's data */
   mac_address *dst;
   in_Header   *ip = NULL;
@@ -2592,7 +2592,7 @@
 
   BYTE         flags;
   tcp_Header  *tcp;
-  BOOL         is_ip4 = (his_ip->ver == 4);
+  bool         is_ip4 = (his_ip->ver == 4);
   DWORD        acknum, seqnum;
   WORD         tcp_len;
 
@@ -3213,7 +3213,7 @@
   }
 }
 
-BOOL tcp_rtt_get (const _tcp_Socket *s, UINT *rto, UINT *MTU)
+bool tcp_rtt_get (const _tcp_Socket *s, UINT *rto, UINT *MTU)
 {
   struct tcp_rtt *rtt = &rtt_cache [(s->hisaddr & 0xFFFF) % RTTCACHE];
 
diff -Naur old/src/pctcp.h new/src/pctcp.h
--- old/src/pctcp.h	2015-07-15 11:36:22.000000000 +0200
+++ new/src/pctcp.h	2026-04-09 07:18:47.401870702 +0200
@@ -113,13 +113,13 @@
 #define tcp_opt_wscale W32_NAMESPACE (tcp_opt_wscale)
 #define hostname       W32_NAMESPACE (hostname)
 
-extern BOOL mtu_discover;
-extern BOOL mtu_blackhole;
-extern BOOL use_rand_lport;
-extern BOOL tcp_nagle;
-extern BOOL tcp_opt_ts;
-extern BOOL tcp_opt_sack;
-extern BOOL tcp_opt_wscale;
+extern bool mtu_discover;
+extern bool mtu_blackhole;
+extern bool use_rand_lport;
+extern bool tcp_nagle;
+extern bool tcp_opt_ts;
+extern bool tcp_opt_sack;
+extern bool tcp_opt_wscale;
 
 extern char hostname [MAX_HOSTLEN+1];
 
@@ -135,17 +135,17 @@
 extern void _tcp_close     (_tcp_Socket *s);
 extern void  tcp_rtt_add   (const _tcp_Socket *s, UINT rto, UINT MTU);
 extern void  tcp_rtt_clr   (const _tcp_Socket *s);
-extern BOOL  tcp_rtt_get   (const _tcp_Socket *s, UINT *rto, UINT *MTU);
+extern bool  tcp_rtt_get   (const _tcp_Socket *s, UINT *rto, UINT *MTU);
 
 extern int  _tcp_send      (_tcp_Socket *s, const char *file, unsigned line);
 extern int  _tcp_sendsoon  (_tcp_Socket *s, const char *file, unsigned line);
 extern int  _tcp_keepalive (_tcp_Socket *s);
 
-extern void tcp_Retransmitter (BOOL force);
+extern void tcp_Retransmitter (bool force);
 
-extern _udp_Socket *_udp_handler  (const in_Header *ip, BOOL broadcast);
-extern _tcp_Socket *_tcp_handler  (const in_Header *ip, BOOL broadcast);
-extern _tcp_Socket *_tcp_unthread (_tcp_Socket *s, BOOL free_tx);
+extern _udp_Socket *_udp_handler  (const in_Header *ip, bool broadcast);
+extern _tcp_Socket *_tcp_handler  (const in_Header *ip, bool broadcast);
+extern _tcp_Socket *_tcp_unthread (_tcp_Socket *s, bool free_tx);
 extern _tcp_Socket *_tcp_abort    (_tcp_Socket *s, const char *file, unsigned line);
 extern int          _tcp_send_reset (_tcp_Socket *s, const in_Header *ip,
                                      const tcp_Header *tcp, const char *file,
@@ -196,7 +196,7 @@
 /* In ports.c
  */
 extern WORD init_localport  (void);
-extern WORD find_free_port  (WORD oldport, BOOL sleep_msl);
+extern WORD find_free_port  (WORD oldport, bool sleep_msl);
 extern int  grab_localport  (WORD port);
 extern int  reuse_localport (WORD port);
 extern int  maybe_reuse_localport (_tcp_Socket *s);
diff -Naur old/src/ports.c new/src/ports.c
--- old/src/ports.c	2014-07-21 13:56:22.000000000 +0200
+++ new/src/ports.c	2026-04-09 07:18:47.401870702 +0200
@@ -69,7 +69,7 @@
  * Local tcp-ports should not be reused 60sec (2*MSL) after they have
  * been closed. Note this is NOT related to 'sock_delay'.
  */
-WORD find_free_port (WORD port, BOOL linger)
+WORD find_free_port (WORD port, bool linger)
 {
   WORD p, lo_port, hi_port;
 
diff -Naur old/src/powerpak.c new/src/powerpak.c
--- old/src/powerpak.c	2014-09-11 16:44:14.000000000 +0200
+++ new/src/powerpak.c	2026-04-09 07:18:47.401870702 +0200
@@ -38,7 +38,7 @@
 static UINT (WINAPI* _AllocSelector) (UINT);
 static UINT (WINAPI* _FreeSelector) (UINT);
 
-BOOL powerpak_init (void)
+bool powerpak_init (void)
 {
   void *mod;
 
diff -Naur old/src/powerpak.h new/src/powerpak.h
--- old/src/powerpak.h	2006-07-01 12:41:58.000000000 +0200
+++ new/src/powerpak.h	2026-04-09 07:18:47.401870702 +0200
@@ -37,7 +37,7 @@
         REAL_regs rm_regs;
       } DPMI_callback;
 
-extern BOOL    powerpak_init (void);
+extern bool    powerpak_init (void);
 
 extern WORD    dpmi_create_dos_selector (void);
 extern int     dpmi_free_dos_selector   (WORD sel);
diff -Naur old/src/pppoe.c new/src/pppoe.c
--- old/src/pppoe.c	2014-01-03 12:20:30.000000000 +0100
+++ new/src/pppoe.c	2026-04-09 07:18:47.405870771 +0200
@@ -65,10 +65,10 @@
 
 static void (W32_CALL *prev_hook) (const char*, const char*) = NULL;
 
-static BOOL        got_PADO;
-static BOOL        got_PADS;
-static BOOL        got_PADT;
-static BOOL        got_PADM;
+static bool        got_PADO;
+static bool        got_PADS;
+static bool        got_PADT;
+static bool        got_PADM;
 static WORD        session = 0;
 static enum States state   = STATE_NONE;
 static mac_address ac_mac_addr;  /* Access Consentrator's (AC) MAC address */
@@ -107,7 +107,7 @@
  * Loop waiting for timeout or a PAD response.
  * Blocks until some got_X flags are set in by pppoe_handler().
  */
-static BOOL pppoe_wait (int wait_code)
+static bool pppoe_wait (int wait_code)
 {
   DWORD timer = set_timeout (1000 * cfg.timeout);
 
@@ -134,7 +134,7 @@
  * The PPPoE Discovery handler. Send PAD Init, wait for PAD offer,
  * send PAD Request and wait for PAD Session confirmation.
  */
-static BOOL pppoe_discovery (void)
+static bool pppoe_discovery (void)
 {
   int loop;
 
@@ -226,7 +226,7 @@
   const void *src;
   const void *dst;
   WORD  proto, len;
-  BOOL  bcast, delivered = FALSE;
+  bool  bcast, delivered = FALSE;
 
   if (pkt->type != 1 || pkt->ver != 1)
      return (0);
@@ -284,7 +284,7 @@
  * Determine if we have a PPPoE session with the machine given
  * by 'dest'. Can only have a session with the AC.
  */
-BOOL pppoe_is_up (const void *dest)
+bool pppoe_is_up (const void *dest)
 {
 #if 1
   if (!cfg.enable || memcmp(dest,ac_mac_addr,_eth_mac_len))
diff -Naur old/src/pppoe.h new/src/pppoe.h
--- old/src/pppoe.h	2018-05-06 17:32:50.000000000 +0200
+++ new/src/pppoe.h	2026-04-09 07:18:47.405870771 +0200
@@ -55,7 +55,7 @@
 extern void  pppoe_exit (void);
 extern int   pppoe_start (void);
 extern int   pppoe_handler (const pppoe_Packet *pkt);
-extern BOOL  pppoe_is_up (const void *dest);
+extern bool  pppoe_is_up (const void *dest);
 extern int   pppoe_send_sess (const void *sock, const BYTE *buf, WORD len);
 extern void *pppoe_mac_format (union link_Packet *buf);
 
diff -Naur old/src/printk.c new/src/printk.c
--- old/src/printk.c	2017-12-28 23:16:15.000000000 +0100
+++ new/src/printk.c	2026-04-09 07:18:47.405870771 +0200
@@ -181,7 +181,7 @@
   int    c, n, width, prec, fillch;
   int    base, len, neg, quoted, upper;
   long   i;
-  BOOL   right_pad, is_long;
+  bool   right_pad, is_long;
   BYTE  *p;
   char  *str, *f, *buf0 = buf, *_fmt = (char*)fmt;
   char   num[32], ct_buf[30];
diff -Naur old/src/profile.c new/src/profile.c
--- old/src/profile.c	2015-06-29 12:09:08.000000000 +0200
+++ new/src/profile.c	2026-04-09 07:18:47.405870771 +0200
@@ -54,7 +54,7 @@
 /*
  * A simple execution profiler
  */
-BOOL profile_enable = FALSE;
+bool profile_enable = FALSE;
 char profile_file [MAX_PATHLEN+1] = ".\\WATTCP.PRO";
 
 static FILE  *prof_fout;
diff -Naur old/src/profile.h new/src/profile.h
--- old/src/profile.h	2013-08-15 19:56:58.000000000 +0200
+++ new/src/profile.h	2026-04-09 07:18:47.405870771 +0200
@@ -13,7 +13,7 @@
   #define profile_file    W32_NAMESPACE (profile_file)
   #define profile_enable  W32_NAMESPACE (profile_enable)
 
-  extern BOOL profile_enable;
+  extern bool profile_enable;
   extern char profile_file [MAX_PATHLEN+1];
 
   extern void profile_start (const char *where);
diff -Naur old/src/receive.c new/src/receive.c
--- old/src/receive.c	2015-12-30 00:11:32.000000000 +0100
+++ new/src/receive.c	2026-04-09 07:18:47.409870840 +0200
@@ -336,7 +336,7 @@
                         struct sockaddr *from, size_t *fromlen)
 {
   int        ret     = 0;
-  BOOL       got_fin = FALSE; /* got FIN from peer */
+  bool       got_fin = FALSE; /* got FIN from peer */
   DWORD      timer   = 0UL;
   sock_type *sk      = (sock_type*) socket->tcp_sock;
 
@@ -359,7 +359,7 @@
 
   while (1)
   {
-    BOOL ok = (tcp_tick(sk) != 0);
+    bool ok = (tcp_tick(sk) != 0);
 
     tcp_Retransmitter (TRUE);
 
@@ -501,7 +501,7 @@
   while (1)
   {
     sock_type *sk = (sock_type*) socket->udp_sock;
-    BOOL       ok = (tcp_tick(sk) != 0);
+    bool       ok = (tcp_tick(sk) != 0);
 
     WATT_YIELD();
 
diff -Naur old/src/run.c new/src/run.c
--- old/src/run.c	2013-08-15 17:34:04.000000000 +0200
+++ new/src/run.c	2026-04-09 07:18:47.413870908 +0200
@@ -21,7 +21,7 @@
        int         order;
        const char *file;
        unsigned    line;
-       BOOL        running;   /* Prevent reentry. Uses in daemon_list[] only. */
+       bool        running;   /* Prevent reentry. Uses in daemon_list[] only. */
      };
 
 static struct run_list start_list  [40];
diff -Naur old/src/select.c new/src/select.c
--- old/src/select.c	2015-09-30 11:29:04.000000000 +0200
+++ new/src/select.c	2026-04-09 07:18:47.413870908 +0200
@@ -79,7 +79,7 @@
 /*
  * Setup for read/write/except_select()
  */
-static __inline Socket *setup_select (int s, BOOL first_loop)
+static __inline Socket *setup_select (int s, bool first_loop)
 {
   Socket *socket = _socklist_find (s);
 
@@ -119,8 +119,8 @@
 
   int  num_fd    = nfds;
   int  ret_count = 0;
-  BOOL expired   = FALSE;
-  BOOL loop_1st  = TRUE;
+  bool expired   = FALSE;
+  bool loop_1st  = TRUE;
   int  s, loops;
 
 #if defined(USE_DEBUG)
@@ -472,7 +472,7 @@
  * Check if a standard handle is ready.
  * This should return TRUE if handle is redirected.
  */
-static BOOL handle_ready (int hnd)
+static bool handle_ready (int hnd)
 {
   union REGS regs;
 
@@ -713,9 +713,9 @@
 
 int main (int argc, char **argv)
 {
-  BOOL   use_8254  = TRUE;
-  BOOL   use_isr   = FALSE;
-  BOOL   debug     = FALSE;
+  bool   use_8254  = TRUE;
+  bool   use_isr   = FALSE;
+  bool   debug     = FALSE;
   double sel_wait  = 0.5;
   int    sel_size  = 1;
   int    ch;
diff -Naur old/src/signal.c new/src/signal.c
--- old/src/signal.c	2014-01-03 12:20:30.000000000 +0100
+++ new/src/signal.c	2026-04-09 07:18:47.417870977 +0200
@@ -61,8 +61,8 @@
        void (MS_CDECL *old)(int);
        int             sig_num;
        const char     *sig_name;
-       BOOL            caught;  /* we caught this signal */
-       BOOL            exit;    /* exit if app doesn't have a signal-handler */
+       bool            caught;  /* we caught this signal */
+       bool            exit;    /* exit if app doesn't have a signal-handler */
      };
 
 static struct signal_table sig_tab[] = {
diff -Naur old/src/socket.c new/src/socket.c
--- old/src/socket.c	2017-01-09 15:41:01.000000000 +0100
+++ new/src/socket.c	2026-04-09 07:18:47.417870977 +0200
@@ -219,7 +219,7 @@
  *         DOS-handle. This function should only be used when `s'
  *         isn't found in `sk_list'.
  */
-BOOL _sock_dos_fd (int s)
+bool _sock_dos_fd (int s)
 {
   if (s >= fileno(stdin) && s <= fileno(stderr)) /* 0..2 (redirected) */
      return (TRUE);
@@ -328,7 +328,7 @@
  * Traverse socket-list to find other SOCK_STREAM sockets
  * besides '_this' which are also listening.
  */
-static __inline BOOL other_tcp_listeners (const Socket *_this)
+static __inline bool other_tcp_listeners (const Socket *_this)
 {
   const Socket *sock;
 
@@ -349,7 +349,7 @@
  * Traverse socket-list to find other SOCK_RAW sockets
  * besides '_this'. Unhook '_raw_ip?_hook' if none found.
  */
-static __inline BOOL other_raw_socks (const Socket *_this)
+static __inline bool other_raw_socks (const Socket *_this)
 {
   const Socket *sock;
 
@@ -363,13 +363,13 @@
  * Switch back to normal rx-mode (RXMODE_BROADCAST) if no multicasts
  * active and no more IPv6/SOCK_PACKET sockets besides '_this'.
  */
-static BOOL mcast_set  = FALSE;
-static BOOL promis_set = FALSE;
+static bool mcast_set  = FALSE;
+static bool promis_set = FALSE;
 
-BOOL _sock_set_normal_rx_mode (const Socket *_this)
+bool _sock_set_normal_rx_mode (const Socket *_this)
 {
   const Socket *sock;
-  BOOL  rc = TRUE;
+  bool  rc = TRUE;
 
 #if defined(USE_MULTICAST)
   if (num_mcast_active() > 0)
@@ -393,9 +393,9 @@
   return (rc);
 }
 
-BOOL _sock_set_mcast_rx_mode (void)
+bool _sock_set_mcast_rx_mode (void)
 {
-  BOOL rc = TRUE;
+  bool rc = TRUE;
 
   if (!mcast_set)
 #if defined(USE_IPV6)
@@ -408,9 +408,9 @@
   return (rc);
 }
 
-BOOL _sock_set_promisc_rx_mode (void)
+bool _sock_set_promisc_rx_mode (void)
 {
-  BOOL rc = TRUE;
+  bool rc = TRUE;
 
   if (!promis_set)
      rc = pkt_set_rcv_mode (RXMODE_PROMISCOUS);
@@ -460,7 +460,7 @@
 /**
  * Allocate ringbuffer and rx-buffers for a SOCK_PACKET socket.
  */
-static BOOL sock_packet_add (Socket *sock)
+static bool sock_packet_add (Socket *sock)
 {
   sock_packet_pool *p = SOCK_CALLOC (sizeof(*p));
 
@@ -1189,7 +1189,7 @@
   else if (state >= tcp_StateTIMEWT)             /* dying tcp session */
   {
     sock_type *sk = (sock_type*)tcp;
-    BOOL closing  = (sock->so_state & (SS_ISDISCONNECTING | SS_CANTSENDMORE));
+    bool closing  = (sock->so_state & (SS_ISDISCONNECTING | SS_CANTSENDMORE));
 
     sock->so_state &= ~(SS_ISCONNECTED | SS_ISCONNECTING);
     DO_FREE (sock->ip_opt);
@@ -1203,7 +1203,7 @@
 
     if (closing && sk->tcp.ip_type == 0) /* fully closed, refused or aborted */
     {
-      BOOL expired = FALSE;
+      bool expired = FALSE;
 
       if (!sock_rbused(sk))
          _sock_free_rcv_buf (sk);   /* free memory not needed anymore */
@@ -1497,7 +1497,7 @@
  *  - Add our sock_daemon().
  *  - Initialise Fortify malloc debugger.
  */
-static BOOL init_sockets (void)
+static bool init_sockets (void)
 {
   static int sk_result = -1;
 
@@ -1564,7 +1564,7 @@
 /**
  * Allocate raw?_Socket buffers for a SOCK_RAW socket.
  */
-static BOOL sock_raw_add (Socket *sock, int family)
+static bool sock_raw_add (Socket *sock, int family)
 {
   int i;
 
@@ -1620,7 +1620,7 @@
 {
   Socket *sock = SOCK_CALLOC (sizeof(*sock));
   void   *proto_sk;
-  BOOL    okay;
+  bool    okay;
 
   if (!sock)
      return (NULL);
@@ -1699,7 +1699,7 @@
 /**
  * Select (and check) a suitable protocol for socket-type.
  */
-static int set_proto (int type, BOOL is_ip6, int *proto)
+static int set_proto (int type, bool is_ip6, int *proto)
 {
 #if defined(USE_IPV6)
   if (is_ip6 && !_sock_set_mcast_rx_mode())
@@ -1814,7 +1814,7 @@
   char       *err  = NULL;
   const char *fam;
   int         s;
-  BOOL        is_ip6;
+  bool        is_ip6;
 
   if (!init_sockets())
   {
@@ -1929,7 +1929,7 @@
 static int stream_cancel (const _tcp_Socket *tcp, BYTE icmp_type, int error)
 {
   Socket *socket;
-  BOOL    should_drop = (icmp_type == ICMP_UNREACH &&
+  bool    should_drop = (icmp_type == ICMP_UNREACH &&
                          tcp->stress >= tcp->rigid) ||
                         (tcp->ip_type == 0);   /* already aborted */
 
diff -Naur old/src/socket.h new/src/socket.h
--- old/src/socket.h	2018-05-06 16:40:38.000000000 +0200
+++ new/src/socket.h	2026-04-09 07:18:47.417870977 +0200
@@ -14,7 +14,10 @@
   /*
    * Prevent tcc <= 2.01 from even looking at this.
    */
-  #define BOOL int
+  #ifdef bool
+#undef bool
+#endif
+#define bool int
 #else  /* rest of file */
 
 #if defined(__CYGWIN__)
@@ -159,7 +162,7 @@
         int                 so_state;    /* internal state, ref SS_* below */
         int                 so_error;    /* internal socket errno */
         time_t              close_time;  /* we closed at (SOCK_STREAM) */
-        BOOL                linger_on;   /* user specified lingering */
+        bool                linger_on;   /* user specified lingering */
         unsigned            linger_time; /* linger-time (SOCK_STREAM) */
         unsigned            fd_duped;    /* FSEXT reference counting */
         DWORD               keepalive;   /* keepalive timeout */
@@ -184,7 +187,7 @@
 
         unsigned            send_lowat;  /* low-water Tx marks */
         unsigned            recv_lowat;  /* low-water Rx marks */
-        BOOL                msg_nosig;   /* don't raise SIGPIPE */
+        bool                msg_nosig;   /* don't raise SIGPIPE */
         DWORD               cookie;      /* memory cookie / marker */
 
       } Socket;
@@ -371,15 +374,15 @@
 extern void   *_sock_calloc    (size_t size, const char *file, unsigned line);
 extern Socket *_sock_del_fd    (int sock, const char *file, unsigned line);
 extern Socket *_socklist_find  (int s);
-extern BOOL    _sock_dos_fd    (int s);
+extern bool    _sock_dos_fd    (int s);
 extern int     _sock_half_open (const _tcp_Socket *tcp);
 extern int     _sock_append    (_tcp_Socket **tcp);
 extern int     _sock_set_rcv_buf  (sock_type *s, size_t len);
 extern void    _sock_free_rcv_buf (sock_type *s);
 extern void    _sock_set_syn_hook (int (*func)(_tcp_Socket **));
-extern BOOL    _sock_set_promisc_rx_mode (void);
-extern BOOL    _sock_set_mcast_rx_mode   (void);
-extern BOOL    _sock_set_normal_rx_mode  (const Socket *_this);
+extern bool    _sock_set_promisc_rx_mode (void);
+extern bool    _sock_set_mcast_rx_mode   (void);
+extern bool    _sock_set_normal_rx_mode  (const Socket *_this);
 
 /*
  * Interface naming stuff.
diff -Naur old/src/sock_ini.c new/src/sock_ini.c
--- old/src/sock_ini.c	2017-02-16 17:51:04.000000000 +0100
+++ new/src/sock_ini.c	2026-04-09 07:18:47.417870977 +0200
@@ -126,37 +126,37 @@
 typedef void (MS_CDECL *_signal_handler) (int);
 typedef void (MS_CDECL *_atexit_handler) (void);
 
-BOOL _bootp_on       = FALSE; /**< Try booting using BOOTP ? */
-BOOL _dhcp_on        = FALSE; /**< Try booting using DHCP ? */
-BOOL _dhcp6_on       = FALSE; /**< Try booting using DHCP6 ? */
-BOOL _rarp_on        = FALSE; /**< Try booting using RARP ? */
-BOOL _do_mask_req    = FALSE; /**< do an "ICMP Mask Request" when configured */
-BOOL _watt_do_exit   = TRUE;  /**< exit program when all boot attempts failed */
-BOOL _watt_no_config = FALSE; /**< run with no config file (embedded/diskless) */
+bool _bootp_on       = FALSE; /**< Try booting using BOOTP ? */
+bool _dhcp_on        = FALSE; /**< Try booting using DHCP ? */
+bool _dhcp6_on       = FALSE; /**< Try booting using DHCP6 ? */
+bool _rarp_on        = FALSE; /**< Try booting using RARP ? */
+bool _do_mask_req    = FALSE; /**< do an "ICMP Mask Request" when configured */
+bool _watt_do_exit   = TRUE;  /**< exit program when all boot attempts failed */
+bool _watt_no_config = FALSE; /**< run with no config file (embedded/diskless) */
 
 WattUserConfigFunc _watt_user_config_fn = NULL;
 
-BOOL survive_eth = 0; /**< GvB 2002-09, allows us to survive without a
+bool survive_eth = 0; /**< GvB 2002-09, allows us to survive without a
                        *   (working) packet driver at all - in cases where life
                        *   still has a meaning without TCP/IP.
                        */
 
 #if defined(USE_DHCP)
-  BOOL survive_bootp = TRUE;     /**< Survive a failed BOOTP attempt */
+  bool survive_bootp = TRUE;     /**< Survive a failed BOOTP attempt */
 #else
-  BOOL survive_bootp = FALSE;    /**< Don't survive a failed BOOTP attempt */
+  bool survive_bootp = FALSE;    /**< Don't survive a failed BOOTP attempt */
 #endif
 
 #if defined(USE_RARP)
-  BOOL survive_dhcp = TRUE;      /**< Survive a failed DHCP attempt */
+  bool survive_dhcp = TRUE;      /**< Survive a failed DHCP attempt */
 #else
-  BOOL survive_dhcp = FALSE;     /**< Don't survive a failed DHCP attempt */
+  bool survive_dhcp = FALSE;     /**< Don't survive a failed DHCP attempt */
 #endif
 
-BOOL survive_rarp  = FALSE;      /**< Don't survive a failed RARP attempt */
-BOOL _watt_is_init = FALSE;      /**< watt_sock_init() done (but with possible failed boot) */
+bool survive_rarp  = FALSE;      /**< Don't survive a failed RARP attempt */
+bool _watt_is_init = FALSE;      /**< watt_sock_init() done (but with possible failed boot) */
 
-static BOOL tcp_is_init = FALSE; /**< tcp_init() called okay. */
+static bool tcp_is_init = FALSE; /**< tcp_init() called okay. */
 static int  old_break   = -1;    /**< Original state of DOS's BREAK handler. */
 
 /**
@@ -218,7 +218,7 @@
 /**
  * Some target dependent functions. Install signal handlers.
  */
-static BOOL use_except = TRUE;
+static bool use_except = TRUE;
 
 #if (DOSX & (DOS4GW|X32VM))
 
@@ -263,7 +263,7 @@
 #if defined(__DJGPP__)
 static void except_handler (int sig)
 {
-  static BOOL been_here = FALSE;
+  static bool been_here = FALSE;
   static jmp_buf exc_buf;
 
   _watt_fatal_error = TRUE;
@@ -587,7 +587,7 @@
  *
  * \retval 0 on success.
  */
-static int tcp_do_bootp (BOOL try_bootp, BOOL try_dhcp, BOOL try_rarp)
+static int tcp_do_bootp (bool try_bootp, bool try_dhcp, bool try_rarp)
 {
   if (try_bootp)
   {
@@ -763,7 +763,7 @@
  * Called only once (during program startup).
  * sock_init() is a macro in <tcp.h>.
  */
-static BOOL sock_init_called = FALSE;
+static bool sock_init_called = FALSE;
 
 int W32_CALL watt_sock_init (size_t tcp_Sock_size, size_t udp_Sock_size, size_t time_t_size)
 {
diff -Naur old/src/sock_ini.h new/src/sock_ini.h
--- old/src/sock_ini.h	2015-06-18 11:57:18.000000000 +0200
+++ new/src/sock_ini.h	2026-04-09 07:18:47.417870977 +0200
@@ -31,12 +31,12 @@
 extern int  _dhcp_on;
 extern int  _dhcp6_on;
 extern int  _rarp_on;
-extern BOOL _do_mask_req;
-extern BOOL _watt_do_exit;
-extern BOOL _watt_is_init;
+extern bool _do_mask_req;
+extern bool _watt_do_exit;
+extern bool _watt_is_init;
 
-extern BOOL survive_eth,  survive_bootp;
-extern BOOL survive_dhcp, survive_rarp;
+extern bool survive_eth,  survive_bootp;
+extern bool survive_dhcp, survive_rarp;
 
 extern WattUserConfigFunc _watt_user_config_fn;
 
diff -Naur old/src/sockopt.c new/src/sockopt.c
--- old/src/sockopt.c	2017-02-16 19:50:42.000000000 +0100
+++ new/src/sockopt.c	2026-04-09 07:18:47.417870977 +0200
@@ -52,8 +52,8 @@
 static int get_tcp_opt  (_tcp_Socket *tcp, int opt, void *val, int *len);
 static int get_udp_opt  (_udp_Socket *udp, int opt, void *val, int *len);
 
-static int set_recv_buf (sock_type *s, DWORD size, BOOL is_tcp);
-static int set_xmit_buf (sock_type *s, DWORD size, BOOL is_tcp);
+static int set_recv_buf (sock_type *s, DWORD size, bool is_tcp);
+static int set_xmit_buf (sock_type *s, DWORD size, bool is_tcp);
 static int raw_rx_buf   (_raw_Socket  *raw, DWORD size);
 static int raw_tx_buf   (_raw_Socket  *raw, DWORD size);
 static int raw6_rx_buf  (_raw6_Socket *raw, DWORD size);
@@ -514,7 +514,7 @@
  */
 static int set_tcp_opt (_tcp_Socket *tcp, int opt, const void *val, int len)
 {
-  BOOL on = *(BOOL*)val;
+  bool on = *(bool*)val;
   UINT MSS;
 
   switch (opt)
@@ -618,7 +618,7 @@
   struct ip_mreq *mc_req;
   DWORD  ip;
 #endif
-  BOOL on = *(BOOL*)val;
+  bool on = *(bool*)val;
 
   switch (opt)
   {
@@ -797,7 +797,7 @@
  * Maximum size accepted for TCP is '64k * (2 << TCP_MAX_WINSHIFT)' = 1MByte.
  * Minimum size is 1 byte.
  */
-static int set_recv_buf (sock_type *s, DWORD size, BOOL is_tcp)
+static int set_recv_buf (sock_type *s, DWORD size, bool is_tcp)
 {
   BYTE  *buf;
   DWORD  len;
@@ -840,7 +840,7 @@
  * Max size accepted is 'max'.
  * size == 0 is legal, but how should we handle that?
  */
-static int set_xmit_buf (sock_type *s, DWORD size, BOOL is_tcp)
+static int set_xmit_buf (sock_type *s, DWORD size, bool is_tcp)
 {
 #if 0 /* not yet */
   BYTE  *buf;
diff -Naur old/src/stkwalk.cpp new/src/stkwalk.cpp
--- old/src/stkwalk.cpp	2014-10-27 14:18:08.000000000 +0100
+++ new/src/stkwalk.cpp	2026-04-09 07:18:47.421871046 +0200
@@ -106,7 +106,7 @@
 
 #if API_VERSION_NUMBER < 9
 
-typedef BOOL (__stdcall *PREAD_PROCESS_MEMORY_ROUTINE64)(
+typedef bool (__stdcall *PREAD_PROCESS_MEMORY_ROUTINE64)(
     HANDLE      hProcess,
     DWORD64     qwBaseAddress,
     PVOID       lpBuffer,
@@ -206,8 +206,8 @@
     ADDRESS64   AddrBStore;           // backing store pointer
     PVOID       FuncTableEntry;       // pointer to pdata/fpo or NULL
     DWORD64     Params[4];            // possible arguments to the function
-    BOOL        Far;                  // WOW far call
-    BOOL        Virtual;              // is this a virtual frame?
+    bool        Far;                  // WOW far call
+    bool        Virtual;              // is this a virtual frame?
     DWORD64     Reserved[3];
     KDHELP64    KdHelp;
 } STACKFRAME64, *LPSTACKFRAME64;
@@ -519,7 +519,7 @@
 // AllocHashRemove
 //   Return: FALSE (0) : Key was found and removed/marked
 //           TRUE (!=0): Key was not found
-static BOOL AllocHashRemove (long lRequestID)
+static bool AllocHashRemove (long lRequestID)
 {
   AllocHashEntryType *pHashEntry, *pHashEntryLast;
   ULONG HashIdx = AllocHashFunction(lRequestID);   // get the Hash-Value
@@ -575,7 +575,7 @@
 
 // ReadProcMemoryFromHash
 //   Callback-Funtion for StackWalk for my own CallStack from the Hash-Table-Entries
-static BOOL __stdcall ReadProcMemoryFromHash (HANDLE hRequestID, DWORD64 lpBaseAddress, PVOID lpBuffer, DWORD nSize, PDWORD lpNumberOfBytesRead) {
+static bool __stdcall ReadProcMemoryFromHash (HANDLE hRequestID, DWORD64 lpBaseAddress, PVOID lpBuffer, DWORD nSize, PDWORD lpNumberOfBytesRead) {
   // Try to find the RequestID
   AllocHashEntryType *pHashEntry;
   *lpNumberOfBytesRead = 0;
@@ -722,7 +722,7 @@
 
 
 // Global data:
-static BOOL g_bInitialized = FALSE;
+static bool g_bInitialized = FALSE;
 static HINSTANCE g_hImagehlpDll = NULL;
 
 static DWORD g_dwShowCount = 0;  // increase at every ShowStack-Call
@@ -820,7 +820,7 @@
   if (nAllocType == _HOOK_FREE) {   // freeing:
     if (lRequest != 0)              // RequestID was found
     {
-      BOOL bRet;
+      bool bRet;
       // Try to find the RequestID in the Hash-Table, mark it that it was freed
       bRet = AllocHashRemove(lRequest);
       if(g_ulShowStackAtAlloc > 0) {
@@ -846,7 +846,7 @@
     // Try to get the header information
     if (_CrtIsValidHeapPointer(pvData))   // it is a valid Heap-Pointer
     {
-      BOOL bRet;
+      bool bRet;
       LONG lReallocRequest;
       _CrtMemBlockHeader *pHead = pHdr (pvData);  // get a pointer to memory block header
 
@@ -950,7 +950,7 @@
 
 
 // SymCleanup()
-typedef BOOL (__stdcall *tSC)( IN HANDLE hProcess );
+typedef bool (__stdcall *tSC)( IN HANDLE hProcess );
 static tSC pSC = NULL;
 
 // SymFunctionTableAccess64()
@@ -958,7 +958,7 @@
 static tSFTA pSFTA = NULL;
 
 // SymGetLineFromAddr64()
-typedef BOOL (__stdcall *tSGLFA)( IN HANDLE hProcess, IN DWORD64 dwAddr,
+typedef bool (__stdcall *tSGLFA)( IN HANDLE hProcess, IN DWORD64 dwAddr,
   OUT PDWORD pdwDisplacement, OUT PIMAGEHLP_LINE64 Line );
 static tSGLFA pSGLFA = NULL;
 
@@ -967,7 +967,7 @@
 static tSGMB pSGMB = NULL;
 
 // SymGetModuleInfo64()
-typedef BOOL (__stdcall *tSGMI)( IN HANDLE hProcess, IN DWORD64 dwAddr, OUT PIMAGEHLP_MODULE64 ModuleInfo );
+typedef bool (__stdcall *tSGMI)( IN HANDLE hProcess, IN DWORD64 dwAddr, OUT PIMAGEHLP_MODULE64 ModuleInfo );
 static tSGMI pSGMI = NULL;
 
 // SymGetOptions()
@@ -975,12 +975,12 @@
 static tSGO pSGO = NULL;
 
 // SymGetSymFromAddr64()
-typedef BOOL (__stdcall *tSGSFA)( IN HANDLE hProcess, IN DWORD64 dwAddr,
+typedef bool (__stdcall *tSGSFA)( IN HANDLE hProcess, IN DWORD64 dwAddr,
   OUT PDWORD64 pdwDisplacement, OUT PIMAGEHLP_SYMBOL64 Symbol );
 static tSGSFA pSGSFA = NULL;
 
 // SymInitialize()
-typedef BOOL (__stdcall *tSI)( IN HANDLE hProcess, IN PSTR UserSearchPath, IN BOOL fInvadeProcess );
+typedef bool (__stdcall *tSI)( IN HANDLE hProcess, IN PSTR UserSearchPath, IN bool fInvadeProcess );
 static tSI pSI = NULL;
 
 // SymLoadModule64()
@@ -993,7 +993,7 @@
 static tSSO pSSO = NULL;
 
 // StackWalk64()
-typedef BOOL (__stdcall *tSW)(
+typedef bool (__stdcall *tSW)(
   DWORD MachineType,
   HANDLE hProcess,
   HANDLE hThread,
@@ -1049,9 +1049,9 @@
   // CreateToolhelp32Snapshot()
   typedef HANDLE (__stdcall *tCT32S)(DWORD dwFlags, DWORD th32ProcessID);
   // Module32First()
-  typedef BOOL (__stdcall *tM32F)(HANDLE hSnapshot, LPMODULEENTRY32 lpme);
+  typedef bool (__stdcall *tM32F)(HANDLE hSnapshot, LPMODULEENTRY32 lpme);
   // Module32Next()
-  typedef BOOL (__stdcall *tM32N)(HANDLE hSnapshot, LPMODULEENTRY32 lpme);
+  typedef bool (__stdcall *tM32N)(HANDLE hSnapshot, LPMODULEENTRY32 lpme);
 
   // try both dlls...
   const char *dllname[] = { "kernel32.dll", "tlhelp32.dll" };
@@ -1114,16 +1114,16 @@
 } MODULEINFO, *LPMODULEINFO;
 
 
-static BOOL GetModuleListPSAPI (ModuleList &modules, DWORD pid, HANDLE hProcess, FILE *fLogFile)
+static bool GetModuleListPSAPI (ModuleList &modules, DWORD pid, HANDLE hProcess, FILE *fLogFile)
 {
   // EnumProcessModules()
-  typedef BOOL (__stdcall *tEPM)(HANDLE hProcess, HMODULE *lphModule, DWORD cb, LPDWORD lpcbNeeded );
+  typedef bool (__stdcall *tEPM)(HANDLE hProcess, HMODULE *lphModule, DWORD cb, LPDWORD lpcbNeeded );
   // GetModuleFileNameEx()
   typedef DWORD (__stdcall *tGMFNE)(HANDLE hProcess, HMODULE hModule, LPSTR lpFilename, DWORD nSize );
   // GetModuleBaseName()
   typedef DWORD (__stdcall *tGMBN)(HANDLE hProcess, HMODULE hModule, LPSTR lpFilename, DWORD nSize );
   // GetModuleInformation()
-  typedef BOOL (__stdcall *tGMI)(HANDLE hProcess, HMODULE hModule, LPMODULEINFO pmi, DWORD nSize );
+  typedef bool (__stdcall *tGMI)(HANDLE hProcess, HMODULE hModule, LPMODULEINFO pmi, DWORD nSize );
 
   HINSTANCE  hPsapi;
   tEPM       pEPM;
@@ -1196,7 +1196,7 @@
   return (modules.size() != 0);
 }
 
-static BOOL GetModuleList (ModuleList &modules, DWORD pid, HANDLE hProcess, FILE *fLogFile)
+static bool GetModuleList (ModuleList &modules, DWORD pid, HANDLE hProcess, FILE *fLogFile)
 {
   if (GetModuleListTH32(modules, pid, fLogFile))   // first try toolhelp32
      return (TRUE);
@@ -1303,7 +1303,7 @@
 }
 
 static char s_szExceptionLogFileName[_MAX_PATH] = "\\exceptions.log";  // default
-static BOOL s_bUnhandledExeptionFilterSet = FALSE;
+static bool s_bUnhandledExeptionFilterSet = FALSE;
 
 static char global_szModName[_MAX_PATH] = "";
 static char global_szAppName[_MAX_PATH] = "";
@@ -1338,7 +1338,7 @@
   return (lRet);
 }
 
-int InitAllocCheck (enum eAllocCheckOutput eOutput, BOOL bSetUnhandledExeptionFilter, ULONG ulShowStackAtAlloc)
+int InitAllocCheck (enum eAllocCheckOutput eOutput, bool bSetUnhandledExeptionFilter, ULONG ulShowStackAtAlloc)
 {
   if (GetModuleFileName(NULL, global_szModName, sizeof(global_szModName)) != 0)
   {
diff -Naur old/src/stkwalk.h new/src/stkwalk.h
--- old/src/stkwalk.h	2014-10-03 02:01:24.000000000 +0200
+++ new/src/stkwalk.h	2026-04-09 07:18:47.421871046 +0200
@@ -34,7 +34,7 @@
                       ULONG ulShowStackAtAlloc);
 
 int InitAllocCheck (enum eAllocCheckOutput eOutput,
-                    BOOL bSetUnhandledExeptionFilter,
+                    bool bSetUnhandledExeptionFilter,
                     ULONG ulShowStackAtAlloc);
 
 ULONG       DeInitAllocCheck (void);
diff -Naur old/src/swsvpkt.c new/src/swsvpkt.c
--- old/src/swsvpkt.c	2018-05-06 20:53:23.000000000 +0200
+++ new/src/swsvpkt.c	2026-04-09 07:18:47.433871252 +0200
@@ -51,7 +51,7 @@
 static void  WINAPI StartReceivingApc (ULONG_PTR);
 static void  WINAPI ReceiveComplete (DWORD, DWORD, OVERLAPPED*);
 static void  WINAPI ResubmitRxBufferApc (ULONG_PTR);
-static BOOL         QueueBuffer (struct SwsVpktUsr*, struct SRxBuffer*);
+static bool         QueueBuffer (struct SwsVpktUsr*, struct SRxBuffer*);
 
 /*
  * Open a handle to the packet driver
@@ -138,10 +138,10 @@
 /*
  * Release packet driver resources
  */
-BOOL SwsVpktClose (struct SwsVpktUsr *pPacket)
+bool SwsVpktClose (struct SwsVpktUsr *pPacket)
 {
   char res[100] = { '\0' };
-  BOOL rc = FALSE;
+  bool rc = FALSE;
 
   winpkt_trace_func = "SwsVpktClose";
 
@@ -187,7 +187,7 @@
                   const void *pvBuf, UINT uLen)
 {
   DWORD sent = 0;
-  BOOL  bRet, overlapped = FALSE;
+  bool  bRet, overlapped = FALSE;
 
   pPacket->txOverlap.Offset = 0;
   pPacket->txOverlap.OffsetHigh = 0;
@@ -218,7 +218,7 @@
 }
 
 
-BOOL SwsVpktGetStatistics (const struct SwsVpktUsr *usr, struct PktStats *stats)
+bool SwsVpktGetStatistics (const struct SwsVpktUsr *usr, struct PktStats *stats)
 {
   stats->in_packets  = sws_stat.rx_pkts;
   stats->in_bytes    = sws_stat.rx_bytes;
@@ -230,11 +230,11 @@
   return (TRUE);
 }
 
-BOOL SwsVpktGetAdapterState (const struct SwsVpktUsr *usr,
+bool SwsVpktGetAdapterState (const struct SwsVpktUsr *usr,
                              struct SwsVpktAdapterState *state)
 {
   IOCTL_INFO info;
-  BOOL  rc;
+  bool  rc;
 
   memset (&info, '\0', sizeof(info));
 
@@ -263,7 +263,7 @@
 /*
  * Get the adapter's MAC address
  */
-BOOL SwsVpktGetMacAddress (const struct SwsVpktUsr *pPacket, mac_address *pMac)
+bool SwsVpktGetMacAddress (const struct SwsVpktUsr *pPacket, mac_address *pMac)
 {
   /* Get the device type etc */
   return SwsVpktDeviceRequest (
@@ -279,7 +279,7 @@
  * Get the adapter's description.
  * Note: This IOCTL was added in ver 1.0.0.5
  */
-BOOL SwsVpktGetDescription (const struct SwsVpktUsr *pPacket, char *descr, size_t max)
+bool SwsVpktGetDescription (const struct SwsVpktUsr *pPacket, char *descr, size_t max)
 {
   return SwsVpktDeviceRequest (
            pPacket->hDevice,             /* Device handle */
@@ -294,7 +294,7 @@
 /*
  * Get the NDIS version.
  */
-BOOL SwsVpktGetNDISversion (const struct SwsVpktUsr *pPacket, DWORD *ver)
+bool SwsVpktGetNDISversion (const struct SwsVpktUsr *pPacket, DWORD *ver)
 {
   return SwsVpktDeviceRequest (
            pPacket->hDevice,             /* Device handle */
@@ -350,7 +350,7 @@
 /*
  * Post a receive buffer
  */
-static BOOL QueueBuffer (struct SwsVpktUsr *pPacket, struct SRxBuffer *pBuffer)
+static bool QueueBuffer (struct SwsVpktUsr *pPacket, struct SRxBuffer *pBuffer)
 {
   if (pPacket->iWorkerExit)
      return (FALSE);
@@ -431,13 +431,13 @@
  * Make a synchronous DeviceIoControl call to a handle opened
  * using FILE_FLAG_OVERLAPPED
  */
-BOOL SwsVpktDeviceRequest2 (HANDLE hDevice, DWORD dwIoControlCode,
+bool SwsVpktDeviceRequest2 (HANDLE hDevice, DWORD dwIoControlCode,
                            const void *lpInBuffer, DWORD nInBufferSize,
                            void *lpOutBuffer, DWORD nOutBufferSize,
                            DWORD *lpBytesReturned,
                            const char *file, unsigned line)
 {
-  BOOL       bResult;
+  bool       bResult;
   HANDLE     hEvent;
   OVERLAPPED overlap;
   DWORD      bytesReturned, err;
diff -Naur old/src/swsvpkt.h new/src/swsvpkt.h
--- old/src/swsvpkt.h	2013-01-24 21:27:30.000000000 +0100
+++ new/src/swsvpkt.h	2026-04-09 07:18:47.433871252 +0200
@@ -40,10 +40,10 @@
         /* lpvOutBuffer -> IOCTL_INFO */
 
 typedef struct IOCTL_INFO {
-        BOOL bPowerOn;
-        BOOL bMediaDisconnected;
-        BOOL bWan;               /* Adapter is a WAN */
-        BOOL bWanDown;           /* WAN is not connected */
+        bool bPowerOn;
+        bool bMediaDisconnected;
+        bool bWan;               /* Adapter is a WAN */
+        bool bWanDown;           /* WAN is not connected */
       } IOCTL_INFO;
 
 #define IOCTL_GETMULTICASTLIST \
@@ -104,30 +104,30 @@
        const char *,                     /* IN: Adapter name; \\.\SwsVpkt0..31 */
        const struct SwsVpktOpenInfo *);  /* IN: Open info */
 
-extern BOOL SwsVpktClose (struct SwsVpktUsr *usr);
+extern bool SwsVpktClose (struct SwsVpktUsr *usr);
 
-extern BOOL SwsVpktGetAdapterState (const struct SwsVpktUsr    *usr,
+extern bool SwsVpktGetAdapterState (const struct SwsVpktUsr    *usr,
                                     struct SwsVpktAdapterState *state);
 
-extern BOOL SwsVpktGetStatistics (const struct SwsVpktUsr *usr,
+extern bool SwsVpktGetStatistics (const struct SwsVpktUsr *usr,
                                   struct PktStats         *stats);
 
 extern UINT SwsVpktSend (struct SwsVpktUsr *usr,
                          const void        *buf,
                          UINT               buf_len);
 
-extern BOOL SwsVpktGetMacAddress (const struct SwsVpktUsr *usr,
+extern bool SwsVpktGetMacAddress (const struct SwsVpktUsr *usr,
                                   mac_address             *addr);
 
-extern BOOL SwsVpktGetDescription (const struct SwsVpktUsr *usr,
+extern bool SwsVpktGetDescription (const struct SwsVpktUsr *usr,
                                    char *descr, size_t descr_len);
 
-extern BOOL SwsVpktGetNDISversion (const struct SwsVpktUsr *usr,
+extern bool SwsVpktGetNDISversion (const struct SwsVpktUsr *usr,
                                    DWORD *ver);
 
 extern const char *SwsVpktGetDriverVersion (void);
 
-extern BOOL SwsVpktDeviceRequest2 (HANDLE hDevice,
+extern bool SwsVpktDeviceRequest2 (HANDLE hDevice,
                                    DWORD dwIoControlCode,
                                    const void *lpInBuffer,
                                    DWORD nInBufferSize,
diff -Naur old/src/syslog.c new/src/syslog.c
--- old/src/syslog.c	2026-04-09 07:13:58.552819378 +0200
+++ new/src/syslog.c	2026-04-09 07:18:47.433871252 +0200
@@ -56,7 +56,7 @@
 static sock_type *log_sock   = NULL;     /* UDP socket for log */
 static FILE      *log_file   = NULL;     /* FILE* for log */
 static DWORD      log_host   = 0UL;      /* IP-address of host (host order) */
-static BOOL       log_opened = FALSE;    /* have called openlog() (error or not) */
+static bool       log_opened = FALSE;    /* have called openlog() (error or not) */
 static int        log_stat   = 0;        /* status bits, set by openlog() */
 static char      *log_tag    = NULL;     /* string to tag the entry with */
 static int        log_mask   = LOG_INFO; /* default is LOG_EMERG - LOGINFO */
@@ -325,8 +325,8 @@
 
 void W32_CALL openlog (const char *ident, int logstat, int logfac)
 {
-  static BOOL done = FALSE;
-  BOOL   fail  = FALSE;
+  static bool done = FALSE;
+  bool   fail  = FALSE;
   char  *error = NULL;
 
   if (ident)
diff -Naur old/src/target.h new/src/target.h
--- old/src/target.h	2018-05-06 16:21:44.000000000 +0200
+++ new/src/target.h	2026-04-09 07:18:47.433871252 +0200
@@ -266,7 +266,10 @@
   #define POKEB(s,o,x)    _farpokeb (_dos_ds, (o)+((s)<<4), x) /**< poke a BYTE to DOS memory */
   #define POKEW(s,o,x)    _farpokew (_dos_ds, (o)+((s)<<4), x) /**< poke a WORD to DOS memory */
   #define POKEL(s,o,x)    _farpokel (_dos_ds, (o)+((s)<<4), x) /**< poke a DWORD to DOS memory */
-  #define BOOL            int
+  #ifdef bool
+#undef bool
+#endif
+#define bool            int
 
 #elif (DOSX & DOS4GW)      /* Watcom targets normally uses this section */
   #if defined(__DJGPP__)
@@ -290,8 +293,11 @@
   #define POKEB(s,o,x)    DOSMEM(s,o,BYTE)  = (BYTE)(x)
   #define POKEW(s,o,x)    DOSMEM(s,o,WORD)  = (WORD)(x)
   #define POKEL(s,o,x)    DOSMEM(s,o,DWORD) = (DWORD)(x)
-  #undef  BOOL
-  #define BOOL int
+  #undef  bool
+  #ifdef bool
+#undef bool
+#endif
+#define bool int
 
 #elif (DOSX & X32VM)
   #include <x32.h>
@@ -321,8 +327,11 @@
     #define POKEL(s,o,x)  PokeRealDWord(((s) << 16) + (o), x)
   #endif
 
-  #undef  BOOL
-  #define BOOL int
+  #undef  bool
+  #ifdef bool
+#undef bool
+#endif
+#define bool int
 
 #elif (DOSX & POWERPAK)   /* Borland 32-bit DOS normally uses this section */
   #include <dos.h>
@@ -336,12 +345,15 @@
   #define POKEB(s,o,x)    _farpokeb (_watt_dos_ds, (o)+((s)<<4), x)
   #define POKEW(s,o,x)    _farpokew (_watt_dos_ds, (o)+((s)<<4), x)
   #define POKEL(s,o,x)    _farpokel (_watt_dos_ds, (o)+((s)<<4), x)
-  #undef  BOOL
-  #define BOOL int
+  #undef  bool
+  #ifdef bool
+#undef bool
+#endif
+#define bool int
 
 #elif !(DOSX & WINWATT)   /* All real-mode and non-Windows targets */
   #include <dos.h>
-  #define  BOOL           int
+  #define  bool           int
 
   #if defined(__WATCOMC__) || defined(_MSC_VER)
     #define PEEKB(s,o)    (*((BYTE  __far*) MK_FP(s,o)))
@@ -563,7 +575,10 @@
   #if defined(__MSDOS__)
     #define getvect(a)      _dos_getvect (a)
     #define setvect(a,b)    _dos_setvect (a, b)
-    #define BOOL            int
+    #ifdef bool
+#undef bool
+#endif
+#define bool            int
 
     extern void ENABLE  (void);
     extern void DISABLE (void);
@@ -603,7 +618,10 @@
     #include <dos.h>
 
     #define cdecl           _cdecl
-    #define BOOL            int
+    #ifdef bool
+#undef bool
+#endif
+#define bool            int
     #define ENABLE()        asm sti
     #define DISABLE()       asm cli
   #endif
diff -Naur old/src/tcp_fsm.c new/src/tcp_fsm.c
--- old/src/tcp_fsm.c	2017-02-16 19:45:12.000000000 +0100
+++ new/src/tcp_fsm.c	2026-04-09 07:18:47.433871252 +0200
@@ -78,7 +78,7 @@
        tcp_timewt_state,   /* tcp_StateTIMEWT  : dally after sending final FIN+ACK */
      };
 
-static BOOL  is_ip4;         /* TRUE: input packet is IPv4, else IPv6 */
+static bool  is_ip4;         /* TRUE: input packet is IPv4, else IPv6 */
 static DWORD acknum, seqnum; /* ACK/SEQ of current segment */
 
 #if defined(USE_NEW_TCP_REASM)  /* not yet */
@@ -337,7 +337,7 @@
   _tcp_Socket *s   = *sp;
   int   len;
   long  ldiff;      /* how much still ACK'ed */
-  BOOL  did_tx;
+  bool  did_tx;
 
   /* handle lost SYN
    */
diff -Naur old/src/tcp_md5.c new/src/tcp_md5.c
--- old/src/tcp_md5.c	2014-01-03 12:20:30.000000000 +0100
+++ new/src/tcp_md5.c	2026-04-09 07:18:47.433871252 +0200
@@ -119,7 +119,7 @@
 /*
  * Check incoming TCP segment for MD5 option.
  */
-BOOL W32_CALL check_md5_signature (const _tcp_Socket *s, const in_Header *ip)
+bool W32_CALL check_md5_signature (const _tcp_Socket *s, const in_Header *ip)
 {
   const struct tcp_Header *tcp;
   const BYTE  *recv_sign, *data, *opt;
diff -Naur old/src/tests/packet.c new/src/tests/packet.c
--- old/src/tests/packet.c	2015-06-22 18:28:02.000000000 +0200
+++ new/src/tests/packet.c	2026-04-09 07:18:47.477872008 +0200
@@ -67,7 +67,7 @@
 
 int main (int argc, char **argv)
 {
-  BOOL quit = FALSE;
+  bool quit = FALSE;
   int  s1, s2;
 
   printf ("Linux style AF_PACKET example. Press ESC to quit\n");
diff -Naur old/src/timer.c new/src/timer.c
--- old/src/timer.c	2016-11-24 17:52:33.000000000 +0100
+++ new/src/timer.c	2026-04-09 07:18:47.441871389 +0200
@@ -52,20 +52,20 @@
     WINBASEAPI VOID WINAPI GetNativeSystemInfo (LPSYSTEM_INFO);
   #endif
 
-  BOOL has_rdtsc = TRUE;           /**< Never set in Watt-32. Some users may need it. */
-  BOOL use_rdtsc = TRUE;           /**< Ditto */
+  bool has_rdtsc = TRUE;           /**< Never set in Watt-32. Some users may need it. */
+  bool use_rdtsc = TRUE;           /**< Ditto */
 
 #else
-  BOOL has_8254   = FALSE;         /**< Do we have a working 8254 timer chip? */
-  BOOL has_rdtsc  = FALSE;         /**< Do we have a CPU with RDTSC instruction? */
-  BOOL use_rdtsc  = FALSE;         /**< Do not use RDTSC by default */
-  BOOL using_int8 = FALSE;         /**< init_timer_isr() called */
+  bool has_8254   = FALSE;         /**< Do we have a working 8254 timer chip? */
+  bool has_rdtsc  = FALSE;         /**< Do we have a CPU with RDTSC instruction? */
+  bool use_rdtsc  = FALSE;         /**< Do not use RDTSC by default */
+  bool using_int8 = FALSE;         /**< init_timer_isr() called */
 #endif
 
 DWORD    clocks_per_usec  = 0L;    /**< Measured CPU speed (MHz) */
 time_t   user_tick_base   = 0UL;   /* time() when user-tick started */
 DWORD    user_tick_msec   = 0UL;   /* milli-sec user-tick counter */
-BOOL     user_tick_active = FALSE;
+bool     user_tick_active = FALSE;
 unsigned num_cpus         = 1;
 
 static DWORD date    = 0;
@@ -414,7 +414,7 @@
    * The following version was suggested by Andrew Paulsen
    * <andrew.paulsen@ndsu.nodak.edu>. Seem to work fine.
    */
-  static BOOL started = FALSE;
+  static bool started = FALSE;
 
   PUSHF_CLI();
 
@@ -541,7 +541,7 @@
  *  \retval \li  TRUE  timer expired
  *          \li  FALSE not expired (or value not set)
  */
-BOOL W32_CALL chk_timeout (DWORD value)
+bool W32_CALL chk_timeout (DWORD value)
 {
   if (user_tick_active)      /* using timer ISR or user-timer */
   {
@@ -727,7 +727,7 @@
   static char buf[30];
   WORD   hour, min, msec;
   DWORD  sec;
-  BOOL   msec_clock = 1;
+  bool   msec_clock = 1;
 
 #ifdef __MSDOS__
   msec_clock = (has_8254 || has_rdtsc);
@@ -904,9 +904,9 @@
 }
 #endif
 
-static BOOL get_processor_freq (uint64 *Hz)
+static bool get_processor_freq (uint64 *Hz)
 {
-  BOOL  rc = FALSE;
+  bool  rc = FALSE;
   DWORD MHz, size = sizeof(MHz);
   HKEY  key = NULL;
   LONG  status = RegOpenKeyEx (HKEY_LOCAL_MACHINE,
@@ -938,7 +938,7 @@
 {
   static LARGE_INTEGER qpc;
   static uint64        Hz;
-  static BOOL          done = FALSE;
+  static bool          done = FALSE;
   static uint64        rc = U64_SUFFIX(0);
 
   if (done)
diff -Naur old/src/timer.h new/src/timer.h
--- old/src/timer.h	2015-09-29 19:44:16.000000000 +0200
+++ new/src/timer.h	2026-04-09 07:18:47.441871389 +0200
@@ -29,7 +29,7 @@
   #define exit_timer_isr()   ((void)0)
   #define win_get_perf_count W32_NAMESPACE (win_get_perf_count)
 
-  W32_DATA BOOL has_rdtsc, use_rdtsc;
+  W32_DATA bool has_rdtsc, use_rdtsc;
   extern uint64 win_get_perf_count (void);
 
 #else
@@ -46,8 +46,8 @@
   #define ms_clock        W32_NAMESPACE (ms_clock)
   #define hires_timer     W32_NAMESPACE (hires_timer)
 
-  extern BOOL has_8254, using_int8;
-  extern BOOL has_rdtsc, use_rdtsc;
+  extern bool has_8254, using_int8;
+  extern bool has_rdtsc, use_rdtsc;
 
   extern DWORD clockbits      (void);
   extern int   hires_timer    (int on);
@@ -56,7 +56,7 @@
   extern void  exit_timer_isr (void);
 #endif
 
-W32_DATA BOOL     user_tick_active;
+W32_DATA bool     user_tick_active;
 W32_DATA DWORD    clocks_per_usec, user_tick_msec;
 W32_DATA time_t   user_tick_base;
 W32_DATA DWORD    start_time, start_day;
diff -Naur old/src/transmit.c new/src/transmit.c
--- old/src/transmit.c	2017-07-05 19:35:38.000000000 +0200
+++ new/src/transmit.c	2026-04-09 07:18:47.441871389 +0200
@@ -47,7 +47,7 @@
 
 static int transmit (const char *func, int s, const void *buf, unsigned len,
                      int flags, const struct sockaddr *to, int tolen,
-                     BOOL have_remote_addr);
+                     bool have_remote_addr);
 
 int W32_CALL sendto (int s, const void *buf, int len, int flags,
                      const struct sockaddr *to, socklen_t tolen)
@@ -175,7 +175,7 @@
  */
 static int transmit (const char *func, int s, const void *buf, unsigned len,
                      int flags, const struct sockaddr *to, int tolen,
-                     BOOL have_remote_addr)  /* for sendto() and sendmsg() */
+                     bool have_remote_addr)  /* for sendto() and sendmsg() */
 {
   Socket *socket = _socklist_find (s);
   int     rc;
@@ -330,7 +330,7 @@
   const struct sockaddr_in *peer = (const struct sockaddr_in*) to;
   DWORD keepalive = socket->keepalive;
   WORD  lport     = 0;
-  BOOL  is_ip6    = (socket->so_family == AF_INET6);
+  bool  is_ip6    = (socket->so_family == AF_INET6);
   BYTE *rdata     = NULL;
   int   rc;
 
@@ -433,7 +433,7 @@
  * If '*len > room', modify '*len' on output to 'room' (the size of
  * bytes left in Tx-buf).
  */
-static __inline BOOL check_non_block_tx (Socket *socket, unsigned *len)
+static __inline bool check_non_block_tx (Socket *socket, unsigned *len)
 {
   sock_type *sk;
   unsigned   room;
@@ -588,8 +588,8 @@
 static int udp_transmit (Socket *socket, const void *buf, unsigned len)
 {
   sock_type  *sk     = (sock_type*) socket->udp_sock;
-  BOOL        is_ip6 = (socket->so_family == AF_INET6);
-  BOOL        is_bcast, is_mcast;
+  bool        is_ip6 = (socket->so_family == AF_INET6);
+  bool        is_bcast, is_mcast;
   unsigned    tx_room;
   int         rc;
   const void *dest;
diff -Naur old/src/udp_rev.c new/src/udp_rev.c
--- old/src/udp_rev.c	2014-01-03 12:20:30.000000000 +0100
+++ new/src/udp_rev.c	2026-04-09 07:18:47.441871389 +0200
@@ -51,7 +51,7 @@
 #include "bsdname.h"
 #include "pcdns.h"
 
-static BOOL  read_response (sock_type *s, char *name, size_t size);
+static bool  read_response (sock_type *s, char *name, size_t size);
 static BYTE *dns_labels (BYTE *bp, BYTE *p, BYTE *ep, char *result);
 static BYTE *dns_resource (WORD req_qtype, BYTE *bp, BYTE *p,
                            BYTE *ep, char *result);
@@ -97,12 +97,12 @@
  * Translate an IPv4/IPv6 address into a host name.
  * Returns 1 on success, 0 on error or timeout.
  */
-static BOOL reverse_lookup (const struct DNS_query *q, size_t qlen,
+static bool reverse_lookup (const struct DNS_query *q, size_t qlen,
                             char *name, size_t size, DWORD nameserver)
 {
-  BOOL        ret;
-  BOOL        ready = FALSE;
-  BOOL        quit  = FALSE;
+  bool        ret;
+  bool        ready = FALSE;
+  bool        quit  = FALSE;
   WORD        sec;
   DWORD       timer;
   _udp_Socket dom_sock;
@@ -316,7 +316,7 @@
 #endif   /* USE_IPV6 */
 
 
-static BOOL read_response (sock_type *s, char *name, size_t size)
+static bool read_response (sock_type *s, char *name, size_t size)
 {
   struct {
      struct DNS_Header head;
diff -Naur old/src/wattcp.h new/src/wattcp.h
--- old/src/wattcp.h	2018-05-06 17:00:23.000000000 +0200
+++ new/src/wattcp.h	2026-04-09 07:18:47.453871595 +0200
@@ -580,7 +580,7 @@
         WORD            hisport;           /* peer's source port */              \
         WORD            myport;            /* my source port */                  \
         DWORD           locflags;          /* local option flags */              \
-        BOOL            is_ip6;            /* TRUE if IPv6 socket */             \
+        bool            is_ip6;            /* TRUE if IPv6 socket */             \
         int             rx_datalen;        /* Rx length, must be signed */       \
         UINT            max_rx_data;       /* Last index for rx_data[] */        \
         BYTE           *rx_data            /* Rx data buffer (default rx_buf[]) */
@@ -691,7 +691,7 @@
 typedef struct _raw_Socket {
         struct _raw_Socket *next;
         WORD   ip_type;                  /**< same ofs as for udp/tcp Socket */
-        BOOL   used;                     /**< used flag; packet not read yet */
+        bool   used;                     /**< used flag; packet not read yet */
         DWORD  seq_num;                  /**< counter for finding oldest pkt */
         struct in_Header ip;
         BYTE   rx_data [MAX_FRAG_SIZE];  /**< room for 1 jumbo IP packet */
@@ -705,7 +705,7 @@
 typedef struct _raw6_Socket {
         struct _raw6_Socket *next;
         WORD   ip_type;
-        BOOL   used;
+        bool   used;
         DWORD  seq_num;
         struct in6_Header ip6;
         BYTE   rx_data [MAX_IP6_DATA];
diff -Naur old/src/wdpmi.c new/src/wdpmi.c
--- old/src/wdpmi.c	2014-01-03 12:20:30.000000000 +0100
+++ new/src/wdpmi.c	2026-04-09 07:18:47.453871595 +0200
@@ -104,7 +104,7 @@
 #if (!(DOSX & POWERPAK))
 
 WORD causeway_ver = 0;        /* major=MSB, minor=LSB */
-BOOL dos32a_nullptr_chk = 0;  /* NULL-ptr traps active */
+bool dos32a_nullptr_chk = 0;  /* NULL-ptr traps active */
 
 #define DOS32A_SIGN  (('I'<<24) + ('D'<<16) + ('3'<<8) + ('2'<<0))
 #define PMODEW_SIGN  (('P'<<24) + ('M'<<16) + ('D'<<8) + ('W'<<0))
@@ -121,7 +121,7 @@
  * WDOSX and Pmode/W uses same detect function with
  * unique signatures.
  */
-static BOOL is_wdosx_or_pmodew (DWORD sign)
+static bool is_wdosx_or_pmodew (DWORD sign)
 {
   union REGS reg;
 
@@ -135,17 +135,17 @@
   return (EAX_REG(reg) == sign);
 }
 
-BOOL dpmi_is_wdosx (void)
+bool dpmi_is_wdosx (void)
 {
   return is_wdosx_or_pmodew (WDOSX_SIGN);
 }
 
-BOOL dpmi_is_pmodew (void)
+bool dpmi_is_pmodew (void)
 {
   return is_wdosx_or_pmodew (PMODEW_SIGN);
 }
 
-BOOL dpmi_is_dos32a (void)
+bool dpmi_is_dos32a (void)
 {
   union REGS reg;
 
@@ -165,7 +165,7 @@
   return (FALSE);
 }
 
-BOOL dpmi_is_dos4gw (void)
+bool dpmi_is_dos4gw (void)
 {
   union REGS reg;
 
@@ -181,7 +181,7 @@
   return (EAX_REG(reg) == 0xFFFF3447);
 }
 
-BOOL dpmi_is_causeway (void)
+bool dpmi_is_causeway (void)
 {
 #if defined(__DJGPP__)
   union  REGS  r;
@@ -229,7 +229,7 @@
   return (FALSE);
 }
 
-BOOL dpmi_is_hxdos (void)
+bool dpmi_is_hxdos (void)
 {
   union REGS reg;
   char  buf[128] = { '\0' };
@@ -640,7 +640,7 @@
   DWORD eip   = start;
   DWORD limit = get_cs_limit();
   const DWORD *context = (const DWORD*) base; /* EBP increases as we rewind */
-  BOOL  reg_calls = FALSE;
+  bool  reg_calls = FALSE;
 
 #if defined(__SW_3R)
   reg_calls = TRUE;
@@ -692,7 +692,7 @@
 
 
 #if defined(WATCOM386)
-BOOL dpmi_init (void)
+bool dpmi_init (void)
 {
   switch (_Extender)
   {
diff -Naur old/src/wdpmi.h new/src/wdpmi.h
--- old/src/wdpmi.h	2011-03-02 19:25:02.000000000 +0100
+++ new/src/wdpmi.h	2026-04-09 07:18:47.453871595 +0200
@@ -4,7 +4,7 @@
 #define _w32_WDPMI_H
 
 #if defined(WATCOM386)
-  extern BOOL dpmi_init (void);
+  extern bool dpmi_init (void);
 #endif
 
 #if (DOSX & (DOS4GW|POWERPAK))
@@ -29,7 +29,7 @@
          DWORD  ssAddr;
          WORD   fault_num;
          DWORD  code;
-         BOOL   mode;  /* 0: exception in pmode, 1: real-mode */
+         bool   mode;  /* 0: exception in pmode, 1: real-mode */
        };
 
   #include <sys/pack_off.h>
@@ -49,12 +49,12 @@
 #endif
 
 #if (DOSX & DOS4GW)
-  extern BOOL  dpmi_is_wdosx    (void);
-  extern BOOL  dpmi_is_pmodew   (void);
-  extern BOOL  dpmi_is_dos32a   (void);
-  extern BOOL  dpmi_is_dos4gw   (void);
-  extern BOOL  dpmi_is_causeway (void);
-  extern BOOL  dpmi_is_hxdos    (void);
+  extern bool  dpmi_is_wdosx    (void);
+  extern bool  dpmi_is_pmodew   (void);
+  extern bool  dpmi_is_dos32a   (void);
+  extern bool  dpmi_is_dos4gw   (void);
+  extern bool  dpmi_is_causeway (void);
+  extern bool  dpmi_is_hxdos    (void);
 
   typedef void (*exceptionHook) (const struct FAULT_STRUC *);
 
diff -Naur old/src/winadinf.c new/src/winadinf.c
--- old/src/winadinf.c	2018-07-26 09:06:34.000000000 +0200
+++ new/src/winadinf.c	2026-04-09 07:18:47.457871665 +0200
@@ -107,7 +107,7 @@
 typedef DWORD (WINAPI *func_GetIfTable) (
         __out    MIB_IFTABLE *pIfTable,
         __inout  ULONG       *pdwSize,
-        __in     BOOL         bOrder);
+        __in     bool         bOrder);
 
 typedef DWORD (WINAPI *func_GetIfTable2) (
         __out   MIB_IF_TABLE2 **table);
@@ -122,7 +122,7 @@
 typedef DWORD (WINAPI *func_GetIpNetTable) (
         __out    MIB_IPNETTABLE *table,
         __inout  ULONG          *size,
-        __in     BOOL            sort);
+        __in     bool            sort);
 
 typedef DWORD (WINAPI *func_GetIpNetTable2) (
         __in  u_short            family,
@@ -134,7 +134,7 @@
 typedef DWORD (WINAPI *func_GetIpAddrTable) (
         __out   MIB_IPADDRTABLE *table,
         __inout ULONG           *size,
-        __in    BOOL             order);
+        __in    bool             order);
 
 typedef DWORD (WINAPI *func_GetBestRoute) (
         __in   DWORD             dest_addr,
@@ -217,7 +217,7 @@
         __in       const GUID       *pInterfaceGuid,
         __in_opt   const DOT11_SSID *pDot11Ssid,
         __in       DOT11_BSS_TYPE    dot11BssType,
-        __in       BOOL              bSecurityEnabled,
+        __in       bool              bSecurityEnabled,
         __reserved VOID             *pReserved,
         __out      WLAN_BSS_LIST   **WlanBssList);
 
@@ -254,7 +254,7 @@
 W32_FUNC const char *W32_CALL _w32_inet_ntop (int af, const void *src,
                                               char *dst, size_t size);
 static int  verbose_level = 0;
-static BOOL ipv4_only_iface = TRUE;
+static bool ipv4_only_iface = TRUE;
 static char work_buf [10000];
 
 static void print_wlan_bss_list (const WLAN_BSS_LIST *bss_list);
@@ -463,7 +463,7 @@
 /*
  * Return TRUE if block at 'p' (with legnth 'len') contains only '0' bytes.
  */
-static BOOL all_zeroes (const void *p, size_t len)
+static bool all_zeroes (const void *p, size_t len)
 {
   const BYTE *x = (const BYTE*)p;
   size_t      i;
@@ -656,7 +656,7 @@
   return (rc);
 }
 
-static BOOL skip_this_iface (const IP_ADAPTER_ADDRESSES *addr, const MIB_IFROW *if_row, const void *_if_row2)
+static bool skip_this_iface (const IP_ADAPTER_ADDRESSES *addr, const MIB_IFROW *if_row, const void *_if_row2)
 {
   if (verbose_level >= 2)
      return (FALSE);
@@ -692,7 +692,7 @@
  * Skip interfaces that has a description ending in "-000x".
  * Handle both ASCII and UNICODE descriptions.
  */
-static BOOL skip_filter_iface (const BYTE *descr_a, const wchar_t *descr_w)
+static bool skip_filter_iface (const BYTE *descr_a, const wchar_t *descr_w)
 {
   const char    *end_a;
   const wchar_t *end_w;
@@ -1161,7 +1161,7 @@
 
 #else
   MIB_IF_TABLE2 *if_table2 = NULL;
-  BOOL           rc;
+  bool           rc;
   ULONG          i;
 
   (*_printf) ("\nFrom GetIfTable2():\n");
@@ -1375,7 +1375,7 @@
   return (0);
 #else
   MIB_IF_TABLE2 *if_table2 = NULL;
-  BOOL           rc;
+  bool           rc;
   ULONG          i;
 
   (*_printf) ("\nFrom GetIfTable2Ex():\n");
@@ -2025,7 +2025,7 @@
  *
  *  [1] https://msdn.microsoft.com/en-us/library/windows/desktop/ms706765(v=vs.85).aspx
  */
-static BOOL wlan_query (HANDLE           client,
+static bool wlan_query (HANDLE           client,
                         const GUID      *guid,
                         WLAN_INTF_OPCODE opcode,
                         size_t           data_size,
@@ -2091,10 +2091,10 @@
     const GUID                  *guid         = &if_info->InterfaceGuid;
     WLAN_AVAILABLE_NETWORK_LIST *network_list = NULL;
     WLAN_BSS_LIST               *bss_list     = NULL;
-    BOOL                         auto_conf;
-    BOOL                         bkg_scan;
-    BOOL                         str_mode;
-    BOOL                         safe_mode;
+    bool                         auto_conf;
+    bool                         bkg_scan;
+    bool                         str_mode;
+    bool                         safe_mode;
     ULONG                        ch_number, op_mode, rssi;
     WLAN_RADIO_STATE             radio_state;
     WLAN_STATISTICS              wlan_stats;
diff -Naur old/src/winadinf.h new/src/winadinf.h
--- old/src/winadinf.h	2018-05-24 19:14:47.000000000 +0200
+++ new/src/winadinf.h	2026-04-09 07:18:47.457871665 +0200
@@ -425,7 +425,7 @@
 
   typedef struct _WLAN_INTERFACE_CAPABILITY {
           WLAN_INTERFACE_TYPE interfaceType;
-          BOOL                bDot11DSupported;
+          bool                bDot11DSupported;
           DWORD               dwMaxDesiredSsidListSize;
           DWORD               dwMaxDesiredBssidListSize;
           DWORD               dwNumberOfSupportedPhys;
@@ -551,8 +551,8 @@
         } WLAN_ASSOCIATION_ATTRIBUTES;
 
   typedef struct _WLAN_SECURITY_ATTRIBUTES {
-          BOOL                   bSecurityEnabled;
-          BOOL                   bOneXEnabled;
+          bool                   bSecurityEnabled;
+          bool                   bOneXEnabled;
           DOT11_AUTH_ALGORITHM   dot11AuthAlgorithm;
           DOT11_CIPHER_ALGORITHM dot11CipherAlgorithm;
         } WLAN_SECURITY_ATTRIBUTES;
@@ -582,7 +582,7 @@
           DOT11_SSID     dot11Ssid;
           DOT11_BSS_TYPE dot11BssType;
           ULONG          uNumberOfBssids;
-          BOOL           bNetworkConnectable;
+          bool           bNetworkConnectable;
           DWORD          wlanNotConnectableReason;
           ULONG          uNumberOfPhyTypes;
           DOT11_PHY_TYPE dot11PhyTypes[WLAN_MAX_PHY_TYPE_NUMBER];
@@ -592,9 +592,9 @@
            * In this case, uNumerOfPhyTypes is WLAN_MAX_PHY_TYPE_NUMBER and the
            * first WLAN_MAX_PHY_TYPE_NUMBER PHY types are returned.
            */
-          BOOL                   bMorePhyTypes;
+          bool                   bMorePhyTypes;
           ULONG                  wlanSignalQuality;
-          BOOL                   bSecurityEnabled;
+          bool                   bSecurityEnabled;
           DOT11_AUTH_ALGORITHM   dot11DefaultAuthAlgorithm;
           DOT11_CIPHER_ALGORITHM dot11DefaultCipherAlgorithm;
           DWORD                  dwFlags;
diff -Naur old/src/win_dll.h new/src/win_dll.h
--- old/src/win_dll.h	2017-11-23 11:49:47.000000000 +0100
+++ new/src/win_dll.h	2026-04-09 07:18:47.453871595 +0200
@@ -82,7 +82,7 @@
  * so that load_dynamic_table() can see the function pointers.
  */
 
-typedef BOOL  (WINAPI *func_QueryThreadCycleTime) (
+typedef bool  (WINAPI *func_QueryThreadCycleTime) (
         IN     HANDLE   thread_handle,
         OUT    ULONG64 *cycle_time);
 
@@ -111,18 +111,18 @@
  * When using 'p_GetThreadInformation()', cast 2nd parameter
  * [1] http://msdn.microsoft.com/en-us/library/windows/desktop/hh448382(v=vs.85).aspx
  */
-typedef BOOL (WINAPI *func_GetThreadInformation) (
+typedef bool (WINAPI *func_GetThreadInformation) (
         IN        HANDLE   thread_handle,
         IN        int      thread_information_class,  /* Really is 'THREAD_INFORMATION_CLASS' */
         IN OUT    void    *thread_information,
         IN        DWORD    thread_information_length);
 
 typedef DWORD (WINAPI *func_GetFileVersionInfoSizeA) (char *, DWORD*);
-typedef BOOL  (WINAPI *func_GetFileVersionInfoA) (char *, DWORD, DWORD, void *);
-typedef BOOL  (WINAPI *func_GetFileVersionInfoExA) (DWORD, LPCTSTR, DWORD, DWORD, void*);
-typedef BOOL  (WINAPI *func_VerQueryValueA) (const void **, char *, void **, UINT *);
-typedef BOOL  (WINAPI *func_GetComputerNameExA) (int, char*, DWORD*);
-typedef BOOL  (WINAPI *func_IsWow64Process) (HANDLE, BOOL*);
+typedef bool  (WINAPI *func_GetFileVersionInfoA) (char *, DWORD, DWORD, void *);
+typedef bool  (WINAPI *func_GetFileVersionInfoExA) (DWORD, LPCTSTR, DWORD, DWORD, void*);
+typedef bool  (WINAPI *func_VerQueryValueA) (const void **, char *, void **, UINT *);
+typedef bool  (WINAPI *func_GetComputerNameExA) (int, char*, DWORD*);
+typedef bool  (WINAPI *func_IsWow64Process) (HANDLE, bool*);
 typedef void  (WINAPI *func_GetSystemTimePreciseAsFileTime) (FILETIME *);
 
 extern func_GetFileVersionInfoSizeA        p_GetFileVersionInfoSizeA;
@@ -181,17 +181,17 @@
 /* "func_": WanPacket-function typedefs.
  * These are *not* WINAPI, but cdecl.
  */
-typedef BOOL         (cdecl *func_WanPacketSetBpfFilter) (WAN_ADAPTER *, PUCHAR, DWORD);
+typedef bool         (cdecl *func_WanPacketSetBpfFilter) (WAN_ADAPTER *, PUCHAR, DWORD);
 typedef WAN_ADAPTER *(cdecl *func_WanPacketOpenAdapter) (void);
-typedef BOOL         (cdecl *func_WanPacketCloseAdapter) (WAN_ADAPTER *);
-typedef BOOL         (cdecl *func_WanPacketSetBufferSize)(WAN_ADAPTER *, DWORD);
+typedef bool         (cdecl *func_WanPacketCloseAdapter) (WAN_ADAPTER *);
+typedef bool         (cdecl *func_WanPacketSetBufferSize)(WAN_ADAPTER *, DWORD);
 typedef DWORD        (cdecl *func_WanPacketReceivePacket) (WAN_ADAPTER *, PUCHAR, DWORD);
-typedef BOOL         (cdecl *func_WanPacketSetMinToCopy) (WAN_ADAPTER *, DWORD);
-typedef BOOL         (cdecl *func_WanPacketGetStats) (WAN_ADAPTER *, struct bpf_stat *);
-typedef BOOL         (cdecl *func_WanPacketSetReadTimeout) (WAN_ADAPTER *, DWORD);
-typedef BOOL         (cdecl *func_WanPacketSetMode) (WAN_ADAPTER *, DWORD);
+typedef bool         (cdecl *func_WanPacketSetMinToCopy) (WAN_ADAPTER *, DWORD);
+typedef bool         (cdecl *func_WanPacketGetStats) (WAN_ADAPTER *, struct bpf_stat *);
+typedef bool         (cdecl *func_WanPacketSetReadTimeout) (WAN_ADAPTER *, DWORD);
+typedef bool         (cdecl *func_WanPacketSetMode) (WAN_ADAPTER *, DWORD);
 typedef HANDLE       (cdecl *func_WanPacketGetReadEvent) (WAN_ADAPTER *);
-typedef BOOL         (cdecl *func_WanPacketTestAdapter) (void);
+typedef bool         (cdecl *func_WanPacketTestAdapter) (void);
 
 extern func_WanPacketSetBpfFilter   p_WanPacketSetBpfFilter;
 extern func_WanPacketOpenAdapter    p_WanPacketOpenAdapter;
@@ -226,11 +226,11 @@
   typedef void    (APIENTRY *func_BT_SetSupportEMail)     (const char *supportEmail);
   typedef INT_PTR (APIENTRY *func_BT_OpenLogFile)         (const char *logFileName, BUGTRAP_LOGFORMAT logFormat);
   typedef void    (APIENTRY *func_BT_SetFlags)            (DWORD dwFlags);
-  typedef BOOL    (APIENTRY *func_BT_SetLogSizeInEntries) (INT_PTR handle, DWORD logSizeInEntries);
-  typedef BOOL    (APIENTRY *func_BT_SetLogFlags)         (INT_PTR handle, DWORD flags);
-  typedef BOOL    (APIENTRY *func_BT_SetLogEchoMode)      (INT_PTR handle, DWORD logEchoMode);
-  typedef BOOL    (APIENTRY *func_BT_InsLogEntryF)        (INT_PTR handle, BUGTRAP_LOGLEVEL logLevel, LPCTSTR format, ...);
-  typedef BOOL    (APIENTRY *func_BT_CloseLogFile)        (INT_PTR handle);
+  typedef bool    (APIENTRY *func_BT_SetLogSizeInEntries) (INT_PTR handle, DWORD logSizeInEntries);
+  typedef bool    (APIENTRY *func_BT_SetLogFlags)         (INT_PTR handle, DWORD flags);
+  typedef bool    (APIENTRY *func_BT_SetLogEchoMode)      (INT_PTR handle, DWORD logEchoMode);
+  typedef bool    (APIENTRY *func_BT_InsLogEntryF)        (INT_PTR handle, BUGTRAP_LOGLEVEL logLevel, LPCTSTR format, ...);
+  typedef bool    (APIENTRY *func_BT_CloseLogFile)        (INT_PTR handle);
   typedef void    (APIENTRY *func_BT_AddLogFile)          (const char *logFile);
 
   extern func_BT_InstallSehFilter    p_BT_InstallSehFilter;
diff -Naur old/src/winmisc.c new/src/winmisc.c
--- old/src/winmisc.c	2018-01-11 18:30:24.000000000 +0100
+++ new/src/winmisc.c	2026-04-09 07:18:47.457871665 +0200
@@ -46,10 +46,10 @@
 #define STATUS_SUCCESS 0
 
 CRITICAL_SECTION _watt_crit_sect;
-BOOL             _watt_is_win9x    = FALSE;  /*!< Process is running under Win9x/ME. */
-BOOL             _watt_is_wow64    = FALSE;  /*!< Process is running under WOW64. */
-BOOL             _watt_use_bugtrap = TRUE;
-BOOL             _watt_is_gui_app  = FALSE;
+bool             _watt_is_win9x    = FALSE;  /*!< Process is running under Win9x/ME. */
+bool             _watt_is_wow64    = FALSE;  /*!< Process is running under WOW64. */
+bool             _watt_use_bugtrap = TRUE;
+bool             _watt_is_gui_app  = FALSE;
 
 HANDLE stdin_hnd  = INVALID_HANDLE_VALUE;
 HANDLE stdout_hnd = INVALID_HANDLE_VALUE;
@@ -99,12 +99,12 @@
 /*
  * Various stuff to initialise.
  */
-static BOOL get_win_version (WORD *ver, BOOL *is_win9x)
+static bool get_win_version (WORD *ver, bool *is_win9x)
 {
   OSVERSIONINFO ovi;
   DWORD os_ver = GetVersion();
   DWORD major_ver = LOBYTE (LOWORD(os_ver));
-  BOOL  rc = FALSE;
+  bool  rc = FALSE;
 
   *is_win9x = (os_ver >= 0x80000000 && major_ver >= 4);
 
@@ -132,7 +132,7 @@
 /*
  * Check if program is a GUI app with no stdout handle.
  */
-static BOOL is_gui_app (void)
+static bool is_gui_app (void)
 {
   const IMAGE_DOS_HEADER *dos;
   const IMAGE_NT_HEADERS *nt;
@@ -225,7 +225,7 @@
 /**
  * Returns the version of a PE image (.sys, .dll or .exe).
  */
-BOOL get_file_version (const char *file_name,
+bool get_file_version (const char *file_name,
                        char       *version_buf,
                        size_t      version_buf_len)
 {
@@ -412,11 +412,11 @@
 /*
  * Called from init_misc() to initialise Win32/win64 specific things.
  */
-BOOL init_win_misc (void)
+bool init_win_misc (void)
 {
   char env[20];
-  BOOL wow64 = FALSE;
-  BOOL rc = FALSE;
+  bool wow64 = FALSE;
+  bool rc = FALSE;
 
 #if defined(__LCC__)
   InitializeCriticalSection ((struct _CRITICAL_SECTION*)&_watt_crit_sect);
@@ -473,12 +473,12 @@
  * Perform a query on the WinDns cache. We ask only for
  * 'type'; An address (A/AAAA records) or a name (PTR record).
  */
-static BOOL WinDnsQueryCommon (WORD type, const void *what,
+static bool WinDnsQueryCommon (WORD type, const void *what,
                                void *result, size_t size)
 {
   DNS_RECORD *rr, *dr = NULL;
   DNS_STATUS  rc;
-  BOOL   found;
+  bool   found;
   DWORD  opt = DNS_QUERY_NO_WIRE_QUERY | /* query the cache only */
                DNS_QUERY_NO_NETBT |      /* no NetBT names */
                DNS_QUERY_NO_HOSTS_FILE;  /* no Winsock hosts file */
@@ -553,7 +553,7 @@
 #endif  /* HAVE_WINDNS_H */
 
 
-BOOL WinDnsQuery_A4 (const char *name, DWORD *ip)
+bool WinDnsQuery_A4 (const char *name, DWORD *ip)
 {
 #if defined(HAVE_WINDNS_H)
   if (!(dns_windns & WINDNS_QUERY_A4))
@@ -566,7 +566,7 @@
 #endif
 }
 
-BOOL WinDnsQuery_A6 (const char *name, void *ip)
+bool WinDnsQuery_A6 (const char *name, void *ip)
 {
 #if defined(HAVE_WINDNS_H)
   if (!(dns_windns & WINDNS_QUERY_A6))
@@ -579,7 +579,7 @@
 #endif
 }
 
-BOOL WinDnsQuery_PTR4 (DWORD ip, TCHAR *name, size_t size)
+bool WinDnsQuery_PTR4 (DWORD ip, TCHAR *name, size_t size)
 {
 #if defined(HAVE_WINDNS_H)
   if (!(dns_windns & WINDNS_QUERY_PTR4))
@@ -593,7 +593,7 @@
 #endif
 }
 
-BOOL WinDnsQuery_PTR6 (const void *ip, TCHAR *name, size_t size)
+bool WinDnsQuery_PTR6 (const void *ip, TCHAR *name, size_t size)
 {
 #if defined(HAVE_WINDNS_H)
   if (!(dns_windns & WINDNS_QUERY_PTR6))
@@ -612,7 +612,7 @@
  * local cache, but do a complete registration with the
  * Winsock registered DNS server(s). Hence off by default.
  */
-BOOL WinDnsCachePut_A4 (const char *name, DWORD ip4)
+bool WinDnsCachePut_A4 (const char *name, DWORD ip4)
 {
 #if defined(HAVE_WINDNS_H)
   DNS_RECORD rr;
@@ -657,7 +657,7 @@
 #endif  /* HAVE_WINDNS_H */
 }
 
-BOOL WinDnsCachePut_A6 (const char *name, const void *ip6)
+bool WinDnsCachePut_A6 (const char *name, const void *ip6)
 {
   if (!(dns_windns & WINDNS_CACHEPUT_A6))
      return (FALSE);
@@ -703,7 +703,7 @@
 }
 
 #ifdef NOT_USED
-static BOOL CALLBACK callback (LPSTR cp)
+static bool CALLBACK callback (LPSTR cp)
 {
   printf ("\t callback(): cp: \"%s\", valid: %d\n",
           cp, IsValidCodePage(atoi(cp)));
diff -Naur old/src/winpkt.c new/src/winpkt.c
--- old/src/winpkt.c	2018-01-11 16:57:17.000000000 +0100
+++ new/src/winpkt.c	2026-04-09 07:18:47.457871665 +0200
@@ -84,7 +84,7 @@
 
 WORD _pktdevclass = PDCLASS_UNKNOWN;
 WORD _pkt_ip_ofs  = 0;
-BOOL _pktserial   = FALSE;
+bool _pktserial   = FALSE;
 int  _pkt_errno   = 0;
 int  _pkt_rxmode  =  RXMODE_DEFAULT;  /**< active receive mode */
 int  _pkt_rxmode0 = -1;               /**< startup receive mode */
@@ -110,8 +110,8 @@
 static DWORD pkt_drop_cnt  = 0UL;
 static int   pkt_txretries = 2;
 static int   thr_realtime  = 0;
-static BOOL  thr_stopped   = FALSE;
-static BOOL  config_pass_1_done = FALSE;
+static bool  thr_stopped   = FALSE;
+static bool  config_pass_1_done = FALSE;
 static char  dump_fname [MAX_PATHLEN] = "$(TEMP)\\winpkt_dump.txt";
 
 #define NdisMediumNull  -1
@@ -171,9 +171,9 @@
 static enum eth_init_result open_wanpacket_adapter (const char *name);
 static enum eth_init_result open_swsvpkt_adapter (const char *name);
 
-static BOOL get_if_stat_swsvpkt (const struct SwsVpktUsr *usr, BOOL *is_up);
-static BOOL get_perm_mac_address (void *mac);
-static BOOL get_interface_mtu (DWORD *mtu);
+static bool get_if_stat_swsvpkt (const struct SwsVpktUsr *usr, bool *is_up);
+static bool get_perm_mac_address (void *mac);
+static bool get_interface_mtu (DWORD *mtu);
 static void show_link_details (void);
 static void set_txmode (const char *value);
 
@@ -237,7 +237,7 @@
 /**
  * WATTCP.CFG parser for "WINPKT." keywords in WATTCP.CFG.
  */
-static BOOL parse_config_pass_1 (void)
+static bool parse_config_pass_1 (void)
 {
   static const struct config_table pkt_init_cfg[] = {
                    { "WINPKT.DEVICE",   ARG_STRCPY, (void*)&_pktdrvrname },
@@ -267,7 +267,7 @@
  * Traverse 'adapters_list' and select 1st device with an IPv4
  * address. Assuming that device is a physical adapter we can use.
  */
-static BOOL find_winpcap_adapter (char *aname, size_t size)
+static bool find_winpcap_adapter (char *aname, size_t size)
 {
   const ADAPTER_INFO *ai;
   int   i;
@@ -293,10 +293,10 @@
  * Loop through "\\.\SwsVpkt<0-31>" and return TRUE on 1st available
  * device.
  */
-static BOOL find_swsvpkt_adapter (char *aname, size_t size)
+static bool find_swsvpkt_adapter (char *aname, size_t size)
 {
   char name [sizeof("\\\\.\\SwsVpktXX")];
-  BOOL found;
+  bool found;
   int  i;
 
   for (i = 0, found = FALSE; i <= 31 && !found; i++)
@@ -326,9 +326,9 @@
  * Only called if user didn't specify a device in WATTCP.CFG
  * (with "winpkt.device").
  */
-static BOOL find_adapter (char *aname, size_t size)
+static bool find_adapter (char *aname, size_t size)
 {
-  BOOL rc;
+  bool rc;
 
   rc = find_winpcap_adapter (aname, size);
   if (rc)
@@ -372,7 +372,7 @@
  * Check receive-mode 'bits' for an illegal bit by turning all
  * legal bits off.
  */
-static BOOL legal_recv_mode (WORD bits)
+static bool legal_recv_mode (WORD bits)
 {
   if (bits & RXMODE_OFF)
      bits &= ~RXMODE_OFF;
@@ -407,7 +407,7 @@
 {
   enum eth_init_result rc;
   DWORD thread_id;
-  BOOL  is_up;
+  bool  is_up;
   char  descr [512];
 
   if (_watt_is_win9x)  /**< \todo Support Win-9x/ME/CE too */
@@ -458,7 +458,7 @@
   if (!_pktdrvrname[0] && !find_adapter(_pktdrvrname,sizeof(_pktdrvrname)))
   {
     const char *problem = "";
-    BOOL  env_ok = (getenv("WATTCP.CFG") || getenv("WATTCP_CFG"));
+    bool  env_ok = (getenv("WATTCP.CFG") || getenv("WATTCP_CFG"));
 
     if (!env_ok)
        problem = "Seems you have not defined a valid %WATTCP.CFG%. Check the INSTALL instructions.\n";
@@ -698,7 +698,7 @@
 
 /* Return TRUE is adapter is up.
  */
-BOOL W32_CALL pkt_is_init (void)
+bool W32_CALL pkt_is_init (void)
 {
   return (_pkt_inf != NULL);
 }
@@ -707,7 +707,7 @@
  * Check 'my_ip' against the IPv4 address(es) Winsock is using for
  * this adapter. Only applies to WinPcap adapters.
  */
-BOOL W32_CALL pkt_check_address (DWORD my_ip)
+bool W32_CALL pkt_check_address (DWORD my_ip)
 {
   const ADAPTER_INFO *ai;
   int   i;
@@ -939,7 +939,7 @@
   struct pkt_ringbuf    *q;
   struct pkt_rx_element *rc = NULL;
   const char  *out;
-  BOOL  empty;
+  bool  empty;
 
   if (!_pkt_inf)
      return (NULL);
@@ -1031,7 +1031,7 @@
     PACKET_OID_DATA oidData;
     DWORD           filter;
   } oid;
-  BOOL  rc;
+  bool  rc;
   const ADAPTER *adapter;
 
   if (!_pkt_inf || _eth_SwsVpkt || _eth_airpcap)
@@ -1065,7 +1065,7 @@
     DWORD           filter;
   } oid;
   DWORD mode;
-  BOOL  rc;
+  bool  rc;
   const ADAPTER *adapter;
 
   if (!_pkt_inf || _eth_SwsVpkt || _eth_airpcap)
@@ -1084,7 +1084,7 @@
   return (rc);
 }
 
-static BOOL get_stats_pcap (const ADAPTER *adapter,
+static bool get_stats_pcap (const ADAPTER *adapter,
                             struct PktStats *stats,
                             struct PktStats *total)
 {
@@ -1148,7 +1148,7 @@
   return (TRUE);
 }
 
-static BOOL get_stats_swsvpkt (const struct SwsVpktUsr *usr,
+static bool get_stats_swsvpkt (const struct SwsVpktUsr *usr,
                                struct PktStats *stats,
                                struct PktStats *total)
 {
@@ -1228,7 +1228,7 @@
 /*
  * Low-level NDIS/Mini-driver functions.
  */
-static BOOL get_perm_mac_address (void *mac)
+static bool get_perm_mac_address (void *mac)
 {
   if (!_pkt_inf || !_pkt_inf->get_mac_op)
      return (FALSE);
@@ -1236,7 +1236,7 @@
   return (*_pkt_inf->get_mac_op) (_pkt_inf->adapter, mac);
 }
 
-static BOOL get_if_mtu_pcap (const ADAPTER *adapter, DWORD *mtu)
+static bool get_if_mtu_pcap (const ADAPTER *adapter, DWORD *mtu)
 {
   struct {
     PACKET_OID_DATA oidData;
@@ -1253,21 +1253,21 @@
   return (TRUE);
 }
 
-static BOOL get_if_mtu_generic (const void *adapter, DWORD *mtu)
+static bool get_if_mtu_generic (const void *adapter, DWORD *mtu)
 {
   *mtu = ETH_MAX;
   ARGSUSED (adapter);
   return (TRUE);
 }
 
-static BOOL get_interface_mtu (DWORD *mtu)
+static bool get_interface_mtu (DWORD *mtu)
 {
   if (!_pkt_inf || !_pkt_inf->get_if_mtu_op)
      return (FALSE);
   return (*_pkt_inf->get_if_mtu_op) (_pkt_inf->adapter, mtu);
 }
 
-static BOOL get_if_type_pcap (const ADAPTER *adapter, WORD *type)
+static bool get_if_type_pcap (const ADAPTER *adapter, WORD *type)
 {
   struct {
     PACKET_OID_DATA oidData;
@@ -1284,14 +1284,14 @@
   return (TRUE);
 }
 
-static BOOL get_if_type_swsvpkt (const struct SwsVpktUsr *usr, WORD *type)
+static bool get_if_type_swsvpkt (const struct SwsVpktUsr *usr, WORD *type)
 {
   *type = PDCLASS_ETHER;   /** \todo Fix me */
   ARGSUSED (usr);
   return (TRUE);
 }
 
-static BOOL get_if_type_airpcap (const AIR_ADAPTER *adapter, WORD *type)
+static bool get_if_type_airpcap (const AIR_ADAPTER *adapter, WORD *type)
 {
   *type = PDCLASS_ETHER;   /** \todo Fix me */
   ARGSUSED (adapter);
@@ -1300,7 +1300,7 @@
 
 /* Query the NIC driver for the adapter description
  */
-static BOOL get_descr_pcap (const ADAPTER *adapter, char *buf, size_t max)
+static bool get_descr_pcap (const ADAPTER *adapter, char *buf, size_t max)
 {
   struct {
     PACKET_OID_DATA oidData;
@@ -1317,12 +1317,12 @@
   return (TRUE);
 }
 
-static BOOL get_descr_swsvpkt (const struct SwsVpktUsr *usr, char *buf, size_t max)
+static bool get_descr_swsvpkt (const struct SwsVpktUsr *usr, char *buf, size_t max)
 {
   return SwsVpktGetDescription (usr, buf, max);
 }
 
-static BOOL get_if_speed_pcap (const ADAPTER *adapter, DWORD *Mbit_s)
+static bool get_if_speed_pcap (const ADAPTER *adapter, DWORD *Mbit_s)
 {
   struct {
     PACKET_OID_DATA oidData;
@@ -1340,7 +1340,7 @@
   return (TRUE);
 }
 
-static BOOL get_if_speed_airpcap (const AIR_ADAPTER *adapter, DWORD *Mbit_s)
+static bool get_if_speed_airpcap (const AIR_ADAPTER *adapter, DWORD *Mbit_s)
 {
   *Mbit_s = 54;        /* fixed at 54MB/s? */
   ARGSUSED (adapter);
@@ -1348,14 +1348,14 @@
 }
 
 #if defined(USE_DEBUG)
-static BOOL get_interface_speed (DWORD *speed)
+static bool get_interface_speed (DWORD *speed)
 {
   if (!_pkt_inf || !_pkt_inf->get_if_speed_op)
      return (FALSE);
   return (*_pkt_inf->get_if_speed_op) (_pkt_inf->adapter, speed);
 }
 
-static BOOL get_phys_media (int *media)
+static bool get_phys_media (int *media)
 {
   struct {
     PACKET_OID_DATA oidData;
@@ -1378,7 +1378,7 @@
 }
 #endif
 
-static BOOL get_if_stat_pcap (const ADAPTER *adapter, BOOL *is_up)
+static bool get_if_stat_pcap (const ADAPTER *adapter, bool *is_up)
 {
   struct {
     PACKET_OID_DATA oidData;
@@ -1395,7 +1395,7 @@
   return (TRUE);
 }
 
-static BOOL get_if_stat_swsvpkt (const struct SwsVpktUsr *usr, BOOL *is_up)
+static bool get_if_stat_swsvpkt (const struct SwsVpktUsr *usr, bool *is_up)
 {
   struct SwsVpktAdapterState state;
 
@@ -1415,14 +1415,14 @@
  *
  * No need when using RXMODE_DEFAULT (9).
  */
-static BOOL ndis_set_loopback (BOOL enable)
+static bool ndis_set_loopback (bool enable)
 {
   struct {
     PACKET_OID_DATA oidData;
     DWORD  options;
   } oid;
   const ADAPTER *adapter;
-  BOOL  rc;
+  bool  rc;
   DWORD options;
   DWORD gen_oid = OID_GEN_MAC_OPTIONS;
   DWORD opt_bit = NDIS_MAC_OPTION_NO_LOOPBACK;
@@ -1466,7 +1466,7 @@
  */
 int W32_CALL pkt_get_api_ver (WORD *ver_p)
 {
-  BOOL rc = FALSE;
+  bool rc = FALSE;
 
   if (!_pkt_inf)
      return (0);
@@ -1767,7 +1767,7 @@
 #if defined(USE_DEBUG)
   const ADAPTER_INFO *ai;
   int   i, phy;
-  BOOL  is_up;
+  bool  is_up;
   DWORD MTU, speed;
   WORD  ver;
   mac_address mac;
diff -Naur old/src/winpkt.h new/src/winpkt.h
--- old/src/winpkt.h	2018-05-06 20:49:08.000000000 +0200
+++ new/src/winpkt.h	2026-04-09 07:18:47.457871665 +0200
@@ -42,17 +42,17 @@
   extern int pkt_set_multicast_list (const void *listbuf, int len);
 #endif
 
-typedef BOOL        (*func_init)        (void);
+typedef bool        (*func_init)        (void);
 typedef void       *(*func_open)        (const char *name, ...);
-typedef BOOL        (*func_close)       (void *a);
+typedef bool        (*func_close)       (void *a);
 typedef UINT        (*func_send)        (const void *a, const void *buf, UINT buf_len); /* returns length of buf sent */
-typedef BOOL        (*func_get_mac)     (const void *a, const void *mac);
-typedef BOOL        (*func_get_stats)   (const void *a, struct PktStats *stats, struct PktStats *total);
-typedef BOOL        (*func_get_if_stat) (const void *a, BOOL *up);
-typedef BOOL        (*func_get_if_type) (const void *a, WORD *type);
-typedef BOOL        (*func_get_if_mtu)  (const void *a, DWORD *Mbit_s);
-typedef BOOL        (*func_get_if_speed)(const void *a, DWORD *Mbit_s);
-typedef BOOL        (*func_get_descr)   (const void *a, char *descr, size_t max);
+typedef bool        (*func_get_mac)     (const void *a, const void *mac);
+typedef bool        (*func_get_stats)   (const void *a, struct PktStats *stats, struct PktStats *total);
+typedef bool        (*func_get_if_stat) (const void *a, bool *up);
+typedef bool        (*func_get_if_type) (const void *a, WORD *type);
+typedef bool        (*func_get_if_mtu)  (const void *a, DWORD *Mbit_s);
+typedef bool        (*func_get_if_speed)(const void *a, DWORD *Mbit_s);
+typedef bool        (*func_get_descr)   (const void *a, char *descr, size_t max);
 typedef const char *(*func_get_drv_ver) (void);
 
 /* This func-ptr is used in 'winpcap_recv_thread()' outside of the
@@ -74,8 +74,8 @@
        HANDLE        recv_thread;        /* WinPcap: thread for capturing */
        WORD          pkt_ip_ofs;         /* store length of MAC-header */
        WORD          pkt_type_ofs;       /* offset to MAC-type */
-       volatile BOOL stop_thread;        /* signal thread to stop */
-       volatile BOOL thread_stopped;     /* did it stop gracefully? */
+       volatile bool stop_thread;        /* signal thread to stop */
+       volatile bool thread_stopped;     /* did it stop gracefully? */
 
        const char *api_name;         /* "WinPcap", "AirPcap", "SwsVpkt" etc. */
        const char *sys_drvr_name;    /* "NPF.sys", "airpcap.sys", "SwsVpkt.sys" etc. */
